# ToyC → RISC-V32 高性能编译器实践实现指南

> 本文依据仓库中的《编译系统实践(1).pdf》编写，并结合当前代码状态给出一套可直接实施的 C++20 方案。目标不是套用 LLVM/Clang，而是自行完成从 ToyC 源程序到 RISC-V32 汇编的完整编译流程。

## 1. 项目目标与硬性约束

编译器应完成以下任务：

```text
ToyC source
    ↓ Lexer
Token stream
    ↓ Parser
AST
    ↓ Semantic Analyzer
Typed AST + Symbol Table
    ↓ CG（结构化控制流生成）
CFG<HIR>
    ↓ Basic-block DAG + local optimization
Optimized CFG<HIR>
    ↓ TCG（Three-address Code Generator）
SSA/TAC IR CFG
    ↓ middle-end optimization
Optimized SSA IR CFG
    ↓ lowering + instruction selection
RISC-V Machine IR
    ↓ instruction scheduling
Scheduled Machine IR
    ↓ register allocation + frame lowering + peephole
RISC-V32 assembly
```

PDF 中必须满足的约束：

- 输入语言是 ToyC：只有 `int`/`void`、函数、标量变量/常量、表达式、分支和循环，没有数组、指针和 I/O。
- 编译器实现语言选择 C++20；不能套用 Clang、LLVM 等现成编译框架。
- 可以使用 CMake 或 Make；建议使用 CMake。
- 编译器从 `stdin` 读取完整源程序，将最终汇编写到 `stdout`；调试和错误信息写到 `stderr`。
- 接受可选命令行参数 `-opt`。建议无参数运行正确性优先的基础流水线，`-opt` 启用性能优化流水线。
- 输出目标是 RISC-V32 汇编。
- 程序结果取 `main` 的进程退出码，即 `a0` 的低 8 位最终会被评测系统观察。
- 功能测试要求正确；性能测试以 `gcc -O2` 的执行时间作为基准并封顶。最终成绩中性能占评测分的 25%，因此后端质量和循环代码质量很重要。
- 源程序没有语法错误、满足语义约束且不含未定义行为，但编译器仍应实现语义检查，避免内部结构依赖未验证的假设。

### 1.1 对 CG、DAG、TCG 和 IR 的统一定义

为了避免不同成员对术语理解不一致，本项目固定采用以下含义：

- **CG（Control-flow Generation）**：把语义检查后的 AST 降低为带基本块和终结指令的结构 CFG。此时块内可以暂时存放较高层的 HIR 语句/表达式。
- **DAG**：每个基本块内部的表达式值图，用于常量折叠、代数化简、局部公共子表达式消除和无用表达式删除。DAG 是 CFG 的块内分析视图，不是取代 CFG 的第二套全程序控制流。
- **TCG（Three-address Code Generator）**：把优化过的 HIR/DAG 线性化为三地址码，然后转为 SSA 形式。
- **IR**：中端使用的、带显式基本块、虚拟寄存器、Phi 和终结指令的 SSA 三地址中间表示。
- **Machine IR（MIR）**：指令选择后的 RISC-V 伪指令/真实指令表示。它仍然使用虚拟寄存器，等寄存器分配完成后才绑定物理寄存器或栈槽。

关键原则是：**控制关系由 CFG 表示，块内值依赖由 DAG/def-use 表示，机器约束由 MIR 表示**。不要让一个类同时承担三者。

### 1.2 阶段、技术和交付物总表

| 阶段 | 主要技术 | 产出 | 阶段验收方式 |
|---|---|---|---|
| Lexer | DFA 思想、最长匹配、位置跟踪 | Token 流 | token golden test |
| Parser | 递归下降、优先级分层、lookahead | AST | AST pretty-print 对拍 |
| Semantic | 嵌套符号表、名称解析、常量求值、返回路径分析 | Typed AST、Symbol/Func ID | 正反语义用例 |
| CG | 基本块划分、结构化控制流降低、短路求值 | CFG&lt;HIR&gt; | CFG verifier、Graphviz/dump |
| DAG | 值编号、哈希驻留、依赖拓扑排序 | 优化的块内 HIR | 优化前后解释执行对比 |
| TCG/SSA | 三地址码、支配树、支配边界、Phi、SSA rename | SSA IR CFG | SSA verifier |
| Middle-end | SCCP、GVN、DCE、CFG 化简、LICM | 优化 SSA IR | pass 单测、差分测试 |
| Lowering/ISel | Phi 消除、critical-edge split、RISC-V pattern matching | 虚拟寄存器 MIR | MIR verifier |
| Schedule | 数据依赖 DAG、关键路径、list scheduling | 已调度 MIR | def-use 与副作用检查 |
| RegAlloc | 活跃性、冲突图、合并、着色、spill、rematerialization | 物理寄存器 MIR | ABI/压力专项测试 |
| Emit | frame lowering、块布局、peephole、汇编打印 | `.s` 文本 | 汇编、链接、运行退出码 |

## 2. 推荐工程结构

当前仓库只有 `lexer.cpp`、`lexer.hpp` 和 `token.hpp`。建议演进为：

```text
compiler/
├── CMakeLists.txt
├── README.md
├── include/
│   └── toyc/
│       ├── frontend/
│       │   ├── token.hpp
│       │   ├── lexer.hpp
│       │   ├── ast.hpp
│       │   ├── parser.hpp
│       │   └── semantic.hpp
│       ├── ir/
│       │   ├── hir.hpp
│       │   ├── cfg.hpp
│       │   ├── dag.hpp
│       │   ├── ir.hpp
│       │   ├── dominator.hpp
│       │   └── verifier.hpp
│       ├── passes/
│       │   ├── pass_manager.hpp
│       │   ├── simplify_cfg.hpp
│       │   ├── sccp.hpp
│       │   ├── gvn.hpp
│       │   ├── dce.hpp
│       │   ├── loop_info.hpp
│       │   └── licm.hpp
│       ├── backend/
│       │   ├── mir.hpp
│       │   ├── riscv32.hpp
│       │   ├── isel.hpp
│       │   ├── scheduler.hpp
│       │   ├── liveness.hpp
│       │   ├── regalloc.hpp
│       │   ├── frame.hpp
│       │   ├── peephole.hpp
│       │   └── asm_printer.hpp
│       └── support/
│           ├── diagnostic.hpp
│           ├── arena.hpp
│           └── ids.hpp
├── src/
│   ├── main.cpp
│   ├── frontend/...
│   ├── ir/...
│   ├── passes/...
│   └── backend/...
├── tests/
│   ├── lexer/
│   ├── parser/
│   ├── semantic/
│   ├── ir/
│   ├── codegen/
│   └── performance/
└── scripts/
    ├── run_case.ps1
    └── differential_test.ps1
```

如果不想一开始移动现有文件，可以先保留根目录源码，待前端测试稳定后再机械迁移。架构上的模块边界比目录形式更重要。

### 2.1 顶层编译器驱动

`main.cpp` 只负责组织阶段，不应包含具体编译算法：

```cpp
int main(int argc, char** argv) {
    const CompilerOptions options = parseOptions(argc, argv);
    const std::string source = readAll(std::cin);

    DiagnosticEngine diagnostics;
    Lexer lexer(source);
    Parser parser(lexer, diagnostics);
    auto ast = parser.parseCompUnit();

    SemanticAnalyzer sema(diagnostics);
    SemanticResult semantic = sema.analyze(*ast);

    HIRModule hir = CFGBuilder(semantic).build(*ast);
    LocalDAGOptimizer().run(hir);
    IRModule ir = ThreeAddressGenerator().lower(hir);

    if (options.optimize)
        buildOptimizingPipeline().run(ir);
    else
        buildCorrectnessPipeline().run(ir);

    MachineModule mir = RISCVInstructionSelector().lower(ir);
    if (options.optimize)
        MachineScheduler().run(mir);
    RegisterAllocator(options.optimize).run(mir);
    FrameLowering().run(mir);
    PeepholeOptimizer(options.optimize).run(mir);
    AsmPrinter(std::cout).print(mir);
}
```

每个阶段结束后在 Debug 构建中调用 verifier。越早发现“不存在终结指令”“Phi 前驱不匹配”“虚拟寄存器先用后定义”，越容易定位错误。

## 3. 核心表示与全局约定

### 3.1 统一 ID，减少悬空指针

AST 可以使用 `std::unique_ptr` 表达树所有权；CFG/IR 是图，建议使用稳定整数 ID：

```cpp
using NodeId  = std::uint32_t;
using SymbolId = std::uint32_t;
using FuncId  = std::uint32_t;
using BlockId = std::uint32_t;
using InstId  = std::uint32_t;
using ValueId = std::uint32_t;
using VReg    = std::uint32_t;
```

模块分别用 `std::vector` 或 arena 保存实体，实体间只引用 ID。这样增添基本块或指令时不会因 `vector` 扩容产生悬空指针，也便于序列化调试输出。

### 3.2 整数和布尔约定

- ToyC 运行时值统一为 32 位有符号整数。
- 比较和逻辑运算的结果必须规范化为 `0` 或 `1`。
- 条件判断只关心是否为 0。
- 字面量和常量求值阶段可暂用 `int64_t`，最终检查/截断到 `int32_t`。源程序保证没有未定义行为，但 `-2147483648` 最好解析为“一元负号 + 2147483648 的暂存字面量”，不要让 lexer 提前溢出。
- RISC-V32 寄存器天然为 32 位；不要在宿主 C++ 中依赖有符号溢出。需要回绕时先转 `uint32_t` 计算，再按位解释为 `int32_t`。

### 3.3 副作用模型

ToyC 没有指针和数组，别名分析非常简单：

- 纯算术、比较、读取局部 SSA 值没有副作用。
- 读全局变量依赖“全局内存版本”。
- 写全局变量改变“全局内存版本”。
- 函数调用可能读写所有可见全局变量，是默认的内存屏障。
- 函数调用本身不能因返回值未使用而删除，因为被调用函数可能修改全局变量或进行深层调用。

这个简化模型会显著降低 DAG、GVN、LICM 和 DCE 的实现难度。

## 4. 词法分析 Lexer

### 4.1 当前实现可以保留的部分

现有 lexer 已经支持：

- 标识符与关键字；
- 十进制整数；
- 空白、`//` 和 `/* ... */` 注释；
- 大部分单字符/双字符运算符；
- 行列号和词法错误。

`Token::lexeme` 使用 `std::string_view` 指向 `Lexer::source`。只要 Lexer 的生命周期覆盖 Parser 使用 Token 的全过程就安全；AST 中不要继续保存该 view，应把标识符复制到字符串池，或尽早转换为 `SymbolId`。

### 4.2 必须先修复的问题

当前 `lexer.cpp` 的 `!` 分支错误：

```cpp
case '!':
    if (match('='))
        return makeToken(TokenType::NotEqual);
    return makeToken(TokenType::Not);
```

现有代码错误地返回了 `EqualEqual` 和 `Assign`，会导致 `!x`、`x != y` 全部解析错误。

另外建议：

- 明确包含 `<utility>`，因为构造函数使用了 `std::move`。
- `-` 始终作为 `Minus` token 更方便处理优先级；PDF 中 `NUMBER` 的正则允许前导负号，但语法本身又有一元 `-`。统一在 Parser 中构造成 `UnaryExpr(Neg, ...)`，可正确处理 `a-1`、`--1` 和 `-2147483648`。
- 对单独的 `&`、`|` 保持报错。
- 增加 `peek()`/单 token 缓冲应由 Parser 完成，不必把 Lexer 改成可回退字符流。

### 4.3 Lexer 测试

至少覆盖：所有关键字、最长匹配、相邻运算符、注释行列号、未闭合注释、超大字面量、`!`/`!=`、`&&`/`||`、下划线标识符。Lexer 单测应比较 token 类型、lexeme、整数值和位置。

## 5. 递归下降 Parser 与 AST

ToyC 文法规模小，手写递归下降最直观，也无需引入 Flex/Bison。表达式使用分层优先级解析；左递归规则改写为循环。

### 5.1 AST 设计

推荐基类 + `std::variant` 或枚举 tag。核心节点如下：

```text
CompUnit
├── Decl {isConst, name, init}
└── Function {returnType, name, params, body}

Stmt
├── BlockStmt {items}
├── EmptyStmt
├── ExprStmt {expr}
├── AssignStmt {name, value}
├── DeclStmt {decl}
├── IfStmt {condition, thenStmt, elseStmt?}
├── WhileStmt {condition, body}
├── BreakStmt / ContinueStmt
└── ReturnStmt {value?}

Expr
├── IntegerExpr {wideValue}
├── NameExpr {name; resolvedSymbol?}
├── UnaryExpr {op, operand}
├── BinaryExpr {op, lhs, rhs}
└── CallExpr {calleeName; resolvedFunction?; arguments}
```

每个节点携带 `SourceRange`，至少包含起始 `SourceLocation`。语义分析后，`NameExpr` 和赋值左值保存解析后的 `SymbolId`，调用保存 `FuncId`，不要在后续阶段重复按字符串查表。

### 5.2 Parser 接口与 token 缓冲

```cpp
class Parser {
public:
    std::unique_ptr<CompUnit> parseCompUnit();
private:
    Token current_;
    void advance();
    bool consume(TokenType);
    Token expect(TokenType, std::string_view message);

    Decl parseDecl();
    FunctionDecl parseFunction();
    StmtPtr parseStmt();
    ExprPtr parseExpr();
    ExprPtr parseLogicalOr();
    ExprPtr parseLogicalAnd();
    ExprPtr parseRelational();
    ExprPtr parseAdditive();
    ExprPtr parseMultiplicative();
    ExprPtr parseUnary();
    ExprPtr parsePrimary();
};
```

顶层读到 `int ID` 时需要区分变量声明和函数定义：看到 ID 后再看下一个 token，若为 `(` 则是函数，否则应为 `=`。可用两个 token 的 lookahead，或者先消费公共前缀后分支。

赋值语句与表达式语句也有公共前缀。因为 ToyC 赋值左值只能是 ID，可以在语句开头用两 token lookahead 判断 `Identifier Assign`；其余情况解析为普通表达式语句。

### 5.3 表达式解析伪代码

```cpp
ExprPtr Parser::parseAdditive() {
    auto lhs = parseMultiplicative();
    while (current_.type == TokenType::Plus ||
           current_.type == TokenType::Minus) {
        const auto op = current_.type;
        advance();
        auto rhs = parseMultiplicative();
        lhs = makeBinary(op, std::move(lhs), std::move(rhs));
    }
    return lhs;
}
```

`parseLogicalOr` 到 `parseMultiplicative` 同样使用循环保证左结合；`parseUnary` 使用递归保证 `!!x`、`-+x` 右结合；`parsePrimary` 在读到 ID 后根据是否紧跟 `(` 区分变量引用和函数调用。

不要在 Parser 中实现短路求值、常量折叠或符号解析。Parser 只负责忠实构造语法树。

## 6. 语义分析

建议分两部分：符号解析/类型检查，以及常量求值/控制流约束。

### 6.1 符号表与作用域

```cpp
struct Symbol {
    SymbolId id;
    std::string name;
    bool isConst;
    bool isGlobal;
    std::optional<std::int32_t> constValue;
};

struct Scope {
    Scope* parent;
    std::unordered_map<std::string, SymbolId> names;
};
```

- 编译单元创建全局作用域。
- 每个函数体创建函数作用域，参数先插入，再遍历函数体。
- 每个 `{...}` 再创建嵌套作用域。
- 声明的初始化表达式应先按题意确定可见性：最稳妥的做法是先分析初始化式，再把新符号插入当前作用域，因此 `int x = x;` 不会错误引用自身；外层同名 `x` 仍可被找到。若评测定义另有说明，再以测试为准。
- 当前作用域内不允许重复声明；内层可以屏蔽外层。
- 每次名称查找从内向外进行，解析成功后把 `SymbolId` 写回 AST。

函数必须先声明后调用，但没有独立函数声明语法。因此顺序遍历顶层：遇到函数定义时先将函数签名加入全局函数表，再分析函数体，这样支持递归；尚未出现的其他函数不能被调用。

### 6.2 必须检查的语义规则

- 恰好有合法的 `int main()` 入口。
- 函数名唯一，且函数只出现在全局。
- 实参与形参数量一致；`void` 调用不能用于需要值的上下文。
- `return` 是否与函数返回类型匹配。
- `break`/`continue` 只能位于 `while` 内。分析器维护 `loopDepth`。
- 赋值左值必须是变量，不能是常量。
- 名称使用在声明之后。
- 常量初始化必须是编译期常量。
- `int` 函数所有可达路径都返回值。

最后一条可先用结构化 AST 分析实现：

```text
mustReturn(return)             = true
mustReturn(block)              = 从前到后，出现 mustReturn 的可达语句后为 true
mustReturn(if c then A else B) = 有 else 且 mustReturn(A) 且 mustReturn(B)
mustReturn(while ...)          = 默认 false
```

更可靠的最终检查是在 CFG 建成后检查函数退出块：若 `int` 函数存在不经过 `return value` 到达退出的边，则报错。即使 `while(1)` 永不退出，也可在常量折叠后由可达性分析正确识别。

### 6.3 常量求值

实现独立的 `ConstEvaluator`，只接受：数字、已解析的常量、纯一元/二元表达式。逻辑与/或也必须短路，避免错误求值 `0 && (1 / 0)`。函数调用、变量读取和赋值不属于常量表达式。

全局常量不必占内存，直接在 IR 中替换为立即数。全局变量若初始化可静态求值，输出到 `.data`；若课程测试允许非常量全局初始化，可生成一个隐藏初始化函数并在 `main` 正文前调用。由于 PDF 声明 ToyC 文件可直接被 C 编译器编译，正常测试中的全局初始化原则上应满足 C 的静态初始化要求。

## 7. CG：从 AST 构造结构 CFG

### 7.1 基本块不变量

一个基本块满足：

- 只有第一条指令能作为外部跳转入口；
- 除最后一条终结指令外没有控制转移；
- 终结指令只能是 `Jump`、`CondJump`、`Return` 或 `Unreachable`；
- 每个可达块恰好有一个 terminator；
- 前驱/后继集合双向一致。

```cpp
struct BasicBlock {
    BlockId id;
    std::vector<HIRStmt> body;
    HIRTerminator terminator;
    std::vector<BlockId> predecessors;
    std::vector<BlockId> successors;
};

struct HIRFunction {
    FuncId id;
    BlockId entry;
    std::vector<BasicBlock> blocks;
};
```

CFGBuilder 维护 `currentBlock` 和循环目标栈：

```cpp
struct LoopTargets {
    BlockId continueTarget; // while 条件块
    BlockId breakTarget;    // while 退出块
};
std::vector<LoopTargets> loopStack;
```

### 7.2 控制语句降低规则

`if (cond) A else B`：

```text
        current
        /     \
   thenBB    elseBB
        \     /
         mergeBB
```

若分支以 `return`/`break` 等结束，不要再从该分支连到 merge。若 merge 无前驱，可删除或标成不可达。

`while (cond) body`：

```text
 current → condBB ─false→ exitBB
             │true
             ↓
           bodyBB ───────→ condBB
```

- `continue` 跳到 `condBB`。
- `break` 跳到 `exitBB`。
- body 中已经终结的块不能再追加回边。

### 7.3 逻辑短路必须用 CFG 表示

不能先计算 `lhs` 和 `rhs` 再做普通 `and/or`，因为右侧可能包含函数调用。

`emitCondition(expr, trueBB, falseBB)` 递归降低：

```text
A && B: A 为真跳到 evalB，A 为假直接 falseBB
A || B: A 为真直接 trueBB，A 为假跳到 evalB
!A:     交换 trueBB 与 falseBB
其他:   计算值后 CondJump(value != 0, trueBB, falseBB)
```

当逻辑表达式出现在值上下文，例如 `x = a && f()`，创建 true/false/merge 三类块，在 merge 处产生 `phi [1, trueBB], [0, falseBB]`，或在前 SSA 阶段给临时变量分别赋 1/0。

### 7.4 return 和统一退出块

简单实现可让每个 `return` 直接成为 terminator。为便于尾声生成，也可以所有 `return value` 跳向统一 exit block，并给返回值建立 Phi。后者便于只生成一份栈恢复代码，推荐采用。

## 8. 基本块 DAG 与局部优化

### 8.1 DAG 节点

```cpp
struct DAGKey {
    HIROp op;
    NodeId lhs;
    NodeId rhs;
    std::uint32_t memoryVersion;
};

struct DAGNode {
    NodeId id;
    HIROp op;
    std::vector<NodeId> inputs;
    std::optional<std::int32_t> constant;
    std::vector<SymbolId> labels; // 当前值等于此节点的局部变量
    bool hasSideEffect;
};
```

每进入一个基本块就建立新 DAG。块边界处停止局部值编号；跨块公共表达式交给 SSA GVN 处理。

### 8.2 构造与优化算法

顺序扫描块内 HIR：

1. 常量建立唯一叶节点。
2. 变量读取连接到该变量当前版本节点。
3. 对纯运算建立 `(opcode, operands, flags)` 键；已存在则复用节点，实现局部 CSE。
4. 对交换律运算 `+`、`*`、`==`、`!=` 可按 NodeId 规范化操作数顺序。
5. 赋值只移动变量标签，不一定生成机器动作。
6. 写全局、调用和控制相关操作保留顺序；调用后递增全局内存版本，使调用两侧的全局读取不能错误合并。
7. 从块的活跃输出、terminator 操作数和所有副作用节点反向标记，未标记节点删除。

可安全实施的代数规则包括：

```text
x + 0 → x        x - 0 → x        x * 1 → x
x * 0 → 0        x / 1 → x        x % 1 → 0
x && 0 → 0       x || 1 → 1       !!x → (x != 0)
x - x → 0        x == x → 1       x != x → 0
```

需要谨慎：

- `0 / x` 不能无条件变成 0，除非已证明 `x != 0`；否则可能删除应发生的除零行为。虽然测试不含未定义行为，优化器仍应保持语义边界。
- 不要重新排序两个函数调用。
- 不要跨调用复用全局变量读取。
- `x * 0 → 0` 只适用于 `x` 已作为纯值计算完成的 IR；若源表达式 `f() * 0`，不能因此删除有副作用的 `f()`。

### 8.3 DAG 输出

按依赖关系拓扑排序，将保留节点写回块内 HIR。副作用节点之间额外加入顺序边。DAG 优化后的 CFG 仍保持原控制流结构，只是块内计算更少。

## 9. TCG：三地址 IR 与 SSA

### 9.1 IR 指令集合

IR 指令应小而稳定：

```text
const i32
copy value
add/sub/mul/sdiv/srem lhs, rhs
icmp eq/ne/slt/sle/sgt/sge lhs, rhs
not value                    ; 结果规范化为 0/1
phi [(pred, value), ...]
load_global symbol
store_global symbol, value
call function, [args...]
jump block
branch condition, trueBlock, falseBlock
return [value]
```

局部变量在 SSA 化后不需要 `load_local/store_local`。全局变量必须保留显式内存操作。每条产生值的指令只产生一个 `ValueId`：

```text
%4 = add %2, %3
%5 = icmp slt %4, 10
branch %5, bb.then, bb.exit
```

### 9.2 从 DAG 生成三地址码

按 DAG 拓扑顺序遍历：

- 常量变为立即数 Value；
- 每个非叶节点最多发出一条三地址指令；
- 多个变量标签可以映射到同一个 Value；
- 节点被多个后继使用时仍只计算一次；
- terminator 最后发出。

### 9.3 SSA 构造

推荐先输出带 `read_var/write_var` 的非 SSA TAC，再使用经典 dominance frontier 算法转 SSA：

1. 从 entry 做 DFS，计算 reverse postorder。
2. 用 Lengauer-Tarjan 或迭代算法计算 immediate dominator；项目规模不大，迭代算法更容易验证。
3. 计算每个块的 dominance frontier。
4. 对每个局部变量，在定义块的 dominance frontier 中插入 Phi，迭代直到闭包。
5. 沿 dominator tree DFS，用每个变量的值栈完成重命名。
6. 删除 `read_var/write_var` 伪指令。

也可以使用 sealed-block SSA 构造法减少中间伪指令，但调试难度更高。第一版优先采用 dominance frontier。

### 9.4 IR Verifier

每次中端 pass 后检查：

- 每个可达块有且只有一个 terminator；
- CFG 前驱/后继互相匹配；
- Phi 必须位于块首，且每个前驱恰有一个输入；
- 每个 Value 只有一个定义；
- 普通使用点被定义点支配；Phi 的输入定义应支配对应前驱边；
- `void` call 没有结果 Value；
- 返回值类型匹配。

Verifier 是整个项目最值得投入的调试设施之一。

## 10. 中端优化流水线

### 10.1 `-opt` 关闭时

仍需运行保证可生成代码的规范化 pass：

```text
RemoveUnreachable
SimplifyCFG
SSA construction
TrivialPhiElimination
DeadInstructionElimination
```

关闭优化不等于保留无效 IR，否则会放大后端压力。

### 10.2 `-opt` 开启时的推荐顺序

```text
SimplifyCFG
SCCP
SimplifyCFG
InstCombine
GVN
DCE
LoopAnalysis
LICM
InductionVariableSimplify
StrengthReduction
GVN
DCE
SimplifyCFG
```

可以循环执行 `SCCP → SimplifyCFG → DCE` 直到没有变化，但应设置最大迭代次数（如 4），防止 pass bug 导致不收敛。

### 10.3 优先级最高的优化

#### SCCP：稀疏条件常量传播

为每个 SSA Value 维护三态格：`Unknown`、`Constant(c)`、`Overdefined`，同时维护可执行 CFG 边。它能同时完成：

- 跨基本块常量传播；
- 常量条件折叠；
- 不可达分支发现；
- Phi 简化。

相比只在块内做常量折叠，SCCP 对循环外条件和常量参数效果更好。

#### GVN：全局值编号

以 `(op, operand value numbers)` 建表达式哈希表，并结合 dominator tree，只复用支配当前点的纯表达式。对 commutative op 规范化操作数。第一版不要把 load/call 纳入 GVN；后续可给 `load_global` 增加 memory version。

#### DCE：死代码删除

从 `return`、branch 条件、store、call 等根节点反向标记有用值，删除未标记且无副作用的指令。SSA def-use 链使 DCE 很容易实现。

#### SimplifyCFG

- 删除不可达块；
- 合并“单后继且后继只有单前驱”的块；
- 折叠常量分支；
- 跳转穿透：`A → B` 且 B 只有无条件跳转时改连最终目标；
- 删除空块，同时正确更新 Phi 输入；
- 相同 true/false 目标的条件跳转改成无条件跳转。

### 10.4 循环优化

根据 dominator 找回边 `tail → header`（header 支配 tail），构造 natural loop，并创建/识别 preheader。

LICM 只外提满足以下条件的指令：

- 操作数在循环外定义或本身已判定为 loop invariant；
- 指令纯且不会 trap；
- 定义支配循环内所有使用；
- 外提不会改变执行次数导致副作用变化。

第一版只外提常量、加减乘和比较；除法/取模除非能证明安全，不外提。ToyC 无指针使全局 load 的分析较简单，但若循环中有任何可能写该全局的 store/call，就不能外提。

强度削弱重点处理：

- 乘以 2 的幂 → 左移；
- 无符号语义明确时除以 2 的幂 → 右移；但 ToyC 是有符号除法且向零截断，不能简单用算术右移替代负数除法。
- 循环中反复计算 `i * constant` 可改为归纳值递增，不过该优化复杂度较高，放在基础正确性稳定之后。

### 10.5 可选高收益优化

- 小函数内联：只内联无递归、指令数小、调用热度高的函数；设置代码膨胀预算。
- 尾递归/尾调用消除：参数搬运和 ABI 处理正确后再做。
- 分支方向规范化：让较可能执行的路径 fall-through，减少跳转。
- 简单循环展开：只有性能测试证明收益时启用，并限制倍数与代码尺寸。

不要先做复杂内联或展开。高质量 SCCP、GVN、DCE、LICM 和寄存器分配通常更稳、更有普遍收益。

## 11. 从 SSA IR 降低到 Machine IR

### 11.1 先处理 Phi

Machine IR 不保留 Phi。在每条前驱边插入 parallel copy：

```text
bb3:
  %x = phi [bb1: %a], [bb2: %b]
```

变为 `bb1` 边上的 `%x ← %a` 和 `bb2` 边上的 `%x ← %b`。若前驱有多个后继，需要先 split critical edge，避免 copy 在错误路径执行。

并行复制可能形成环：

```text
a ← b, b ← a
```

用临时虚拟寄存器拆为 `tmp ← a; a ← b; b ← tmp`。随后 copy coalescing 会尽量消除这些移动。

### 11.2 Machine IR 数据结构

```cpp
enum class MOpcode { ADD, ADDI, SUB, MUL, DIV, REM, SLT, SLTIU,
                     XOR, XORI, AND, OR, SLLI, SRAI,
                     LW, SW, LUI, AUIPC,
                     BEQ, BNE, BLT, BGE, JAL, JALR, COPY, RET };

using MOperand = std::variant<VReg, PhysReg, Immediate,
                              StackSlot, GlobalSymbol, MBlockId>;

struct MInstruction {
    MOpcode opcode;
    std::vector<MOperand> defs;
    std::vector<MOperand> uses;
    RegMask implicitDefs; // call 会破坏 caller-saved 寄存器
    RegMask implicitUses;
};
```

将 def/use 明确分开，活跃性和寄存器分配就不必理解每条指令的文本格式。

## 12. RISC-V32 指令选择

### 12.1 ABI 约定

按常见 RISC-V ILP32 ABI：

- `a0-a7`：前 8 个整数参数，`a0` 同时保存整数返回值；
- `t0-t6`：caller-saved 临时寄存器；
- `s0-s11`：callee-saved；使用后必须在函数序言保存、尾声恢复；
- `ra`：返回地址，非叶函数必须保存；
- `sp`：栈指针，调用边界保持 16 字节对齐；
- `zero/x0`：常数 0。

超过 8 个的参数按 ABI 放入调用者栈参数区。函数调用指令必须显式标注 caller-saved clobber，寄存器分配器才能把跨调用活跃值放到 `s` 寄存器或 spill。

应从评测工具链确认具体 `-march`/`-mabi`。实现可配置为 `rv32im/ilp32`；只有确认支持 M 扩展后才直接使用 `mul/div/rem`。若仅支持 RV32I，需要提供软件辅助函数。

### 12.2 选择规则

常见映射：

```text
add x, constant[-2048, 2047] → addi
add x, y                       → add
sub x, constant               → addi x, -constant（可表示时）
mul/div/rem                   → mul/div/rem（M 扩展）
x < y                         → slt
x > y                         → slt（交换操作数）
x != 0                        → snez（最终可打印为 sltu rd, zero, rs）
x == 0                        → seqz（最终可打印为 sltiu rd, rs, 1）
x == y                        → xor tmp, x, y; seqz dst, tmp
x != y                        → xor tmp, x, y; snez dst, tmp
```

立即数超过 12 位时通过 `lui + addi` 或汇编器伪指令 `li` 物化。内部 MIR 最好展开成真实指令，这样调度和寄存器压力估计更准确；最终打印允许先使用 `li`、`mv`、`j`、`ret` 等 GNU assembler 伪指令简化开发。

### 12.3 比较与分支融合

若比较结果只被一个 branch 使用，不要生成布尔寄存器：

```text
%c = icmp slt %a, %b
branch %c, T, F
```

直接选择为：

```asm
blt a_reg, b_reg, .L_T
j .L_F
```

这会少一条 `slt`，也减少一个虚拟寄存器。`eq/ne/ge/lt` 都有对应分支；`>`/`<=` 可交换操作数或反转分支。

### 12.4 全局变量与重定位

全局数据建议输出：

```asm
.data
.align 2
.globl g
.type g, @object
.size g, 4
g:
    .word 42
```

访问全局地址时使用工具链接受的 `%hi/%lo` 组合或 `la` 伪指令，再 `lw/sw`。由于每个访问通常需要地址物化，优化器可在基本块内复用同一全局地址寄存器；跨调用时仍需考虑寄存器和位置独立代码约束。

## 13. 指令调度

建议在指令选择后、寄存器分配前做基本块内 list scheduling。此时有虚拟寄存器，数据依赖清晰，也没有 spill 指令干扰。

### 13.1 依赖图

每个块建立有向无环依赖图：

- RAW：读取依赖其最近定义；
- WAR/WAW：对物理固定寄存器或保守内存次序建边；
- load/store/call：按副作用模型建立内存顺序边；
- terminator 固定在最后；
- call 的参数准备必须在 call 前，返回值读取在 call 后。

给指令设置近似 latency，例如 ALU=1、load=2、mul=3、div/rem=高延迟、call=高延迟。反向计算 critical-path height，ready queue 优先选择高度最大的节点，同时用寄存器压力作为次级代价。

### 13.2 调度策略

- 尽量把独立 ALU 指令放到 load 与其使用之间；
- 把独立计算穿插在长延迟 mul/div 周围；
- 不越过可能别名的 store/call 移动 load；
- 不跨基本块调度，第一版保持简单；
- 调度后重新验证每个虚拟寄存器都先定义后使用。

若没有真实目标 CPU 的 latency 数据，保持保守。错误的激进调度不如不调度。

## 14. 活跃性分析与寄存器分配

### 14.1 数据流活跃性

对每个块计算 `use`、`def`，逆向迭代：

```text
liveOut[B] = union(liveIn[S]) for S in successors[B]
liveIn[B]  = use[B] ∪ (liveOut[B] - def[B])
```

迭代到不再变化。Phi 已在 lowering 时消除，因此无需在这里特殊处理 Phi 边语义。

### 14.2 两阶段实现策略

第一阶段先实现线性扫描，确保整个编译器打通；第二阶段在 `-opt` 下启用图着色。

线性扫描：

- 按 reverse postorder 编号指令；
- 计算每个 VReg 的 live interval；
- 活跃区间开始时分配空闲物理寄存器；
- 无寄存器时 spill 结束位置最远、代价最低的区间。

图着色（推荐最终版本）：

1. 由活跃性建立 interference graph。
2. `COPY src,dst` 建 move relation，尝试 Briggs/George coalescing。
3. Simplify：度数 `< K` 的节点入栈。
4. Freeze：冻结无法继续合并的 move。
5. Spill select：选择代价最低节点。
6. Select：反向弹栈着色。
7. 若实际 spill，重写程序并重新进行活跃性和着色。

### 14.3 Spill 代价

简单且有效的权重：

```text
spillCost(v) = Σ(use/def weight) / degree(v)
weight = 10 ^ min(loopDepth, 3)
```

循环越深的值越不应 spill。常量和全局地址可 rematerialize：需要时重新发出 `li/la`，不必 `sw` 后再 `lw`。

### 14.4 caller/callee saved 决策

- 不跨函数调用的短生命值优先使用 `t`/`a` 寄存器。
- 跨调用活跃的值优先使用 `s` 寄存器，代价是函数入口/出口各一次保存恢复。
- 只调用一次且跨调用值很少时，也可在 call 周围局部 spill；比较总代价后决定。
- 给预着色参数寄存器、返回寄存器和固定 ABI 操作数建立冲突，不能把它们当普通 VReg 随意改色。

## 15. 栈帧与函数序言/尾声

寄存器分配之后才能确定最终 frame：

```text
高地址
┌─────────────────────────┐
│ 调用者传入的栈参数       │
├─────────────────────────┤
│ 保存的 ra / s0-s11      │
├─────────────────────────┤
│ spill slots             │
├─────────────────────────┤
│ outgoing arguments area │
└─────────────────────────┘ ← sp（16 字节对齐）
低地址
```

- 叶函数且无 spill、无 callee-saved 使用时可以完全省略栈帧。
- 只保存实际使用的 `s` 寄存器。
- 非叶函数保存 `ra`。
- frame 大小向上对齐到 16。
- 如果 offset 超出 load/store 的 12 位立即数范围，先用临时寄存器计算地址。
- 多个 return 应尽量汇合到单一 epilogue，避免重复恢复代码。

序言示意：

```asm
addi sp, sp, -32
sw   ra, 28(sp)
sw   s0, 24(sp)
```

尾声顺序相反：恢复寄存器、恢复 `sp`、`ret`。

## 16. 汇编级优化与输出

最后一轮 peephole 可处理：

- `mv r, r` 删除；
- `addi r, r, 0` 删除或改为 move；
- 无条件跳到紧邻下一块的指令删除；
- branch + jump 结合块布局反转条件，保留一个 fall-through；
- 相邻且安全的地址计算合并；
- 未被引用的本地 label/空块删除；
- 函数末尾统一打印 `.size`。

块布局建议采用 CFG trace layout：从入口开始优先放置预计热的未布局后继。没有 profile 时可使用启发式：循环回边/循环体较热，常量推导可达边优先，`while` body 通常比 exit 热。使热路径 fall-through，冷路径承担额外跳转。

AsmPrinter 只做格式化，不能在打印时偷偷改变语义。所有 rewrite 都应发生在可验证的 MIR pass 中。

## 17. 端到端示例

输入：

```c
int main() {
    int x = 1 + 2 * 3;
    if (x > 5 && x != 7)
        return 1;
    return x;
}
```

AST 保留原表达式结构。块内 DAG 将 `1 + 2 * 3` 折叠为 7。短路 CFG 仍会忠实表达 `&&`，随后 SCCP 推导条件恒假并删除 then 块。优化 IR 近似为：

```text
entry:
    return 7
```

最终汇编可近似为：

```asm
.text
.align 2
.globl main
.type main, @function
main:
    li a0, 7
    ret
.size main, .-main
```

这个例子体现了分层的价值：Parser 不做优化，CFG 先保证短路语义正确，中端再通过常量传播删除控制流，后端最后得到极短代码。

## 18. 构建、接口与调试输出

推荐 `CMakeLists.txt` 关键设置：

```cmake
cmake_minimum_required(VERSION 4.0)
project(toyc_compiler LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
add_executable(compiler ...)
target_compile_options(compiler PRIVATE
    $<$<CXX_COMPILER_ID:GNU,Clang>:-Wall;-Wextra;-Wpedantic>)
```

正式接口：

```text
compiler < input.tc > output.s
compiler -opt < input.tc > output.s
```

所有 IR dump 必须写 `stderr`，且只由调试参数启用，例如：

```text
--dump-ast
--dump-cfg
--dump-ir
--dump-mir
--verify-each
```

不要把调试内容混入 `stdout`，否则汇编器会拒绝输出。

## 19. 测试体系

### 19.1 分层测试

- Lexer：token golden tests。
- Parser：AST pretty-printer golden tests。
- Semantic：合法/非法程序和精确诊断位置。
- CFG：文本 dump + verifier；重点测短路、嵌套循环、break/continue、双分支 return。
- DAG/IR pass：输入手写 IR，比较 pass 后 IR，并运行 verifier。
- 后端：IR 到 MIR/汇编 golden tests；重点测大立即数、超过 8 参数、递归、跨调用活跃值、大栈帧。
- 端到端：生成汇编，交叉编译并在 RISC-V 模拟器运行，比较退出码。

### 19.2 差分测试

ToyC 是 C 的子集，可以把同一 `.tc` 文件：

1. 用宿主/参考 C 编译器编译运行；
2. 用本项目编译器生成 RISC-V 汇编，再用交叉工具链链接并在模拟器运行；
3. 比较两者退出码的低 8 位。

随机程序生成器应控制：除数非零、无有符号溢出、循环必终止、函数路径必返回、递归有边界。这样能快速发现手写样例没有覆盖的 CFG/寄存器错误。

### 19.3 性能测试

性能测量至少记录：

- 动态指令条数；
- 分支条数和函数调用次数；
- load/store 数量；
- spill load/store 数量；
- 最大寄存器压力；
- 汇编代码尺寸；
- 与 `gcc -O0`、`gcc -O2` 的运行时间比值。

每次优化提交都应同时跑 correctness suite 和 performance suite。若某优化让单项 benchmark 更快但整体回退，要检查代码膨胀、寄存器压力或分支布局，而不是只看 IR 指令数。

## 20. 推荐实现顺序

### 里程碑 1：最小正确闭环

1. 修复 Lexer 并补单测。
2. 完成 Parser、AST pretty-printer。
3. 完成符号表、语义检查、常量求值。
4. 直接生成无优化 TAC/CFG。
5. 用最简单的 stack-based 后端或线性扫描生成 RV32 汇编。
6. 跑通 `return 0`、算术、局部变量、if/while、函数调用和递归。

此时目标是所有语言特性都能正确运行，而不是立刻追求最佳汇编。

### 里程碑 2：规范 IR 与稳健后端

1. CFG verifier、dominator、SSA、Phi 消除。
2. 完整 ABI：参数、返回值、callee/caller saved、栈对齐。
3. 活跃性与可重复 spill 的寄存器分配。
4. 大立即数、全局变量、大 frame。
5. 差分测试和随机测试。

### 里程碑 3：高收益优化

1. DAG 局部 CSE/constant folding。
2. SCCP、SimplifyCFG、DCE。
3. GVN/InstCombine。
4. 比较-分支融合、copy coalescing、rematerialization。
5. 循环分析和 LICM。

### 里程碑 4：性能冲刺

1. 图着色寄存器分配和 loop-weighted spill。
2. 块布局与局部调度。
3. 基于 benchmark 决定是否做小函数内联、归纳变量优化或有限循环展开。
4. 与 `gcc -O2` 对比热点汇编，逐项解释差距。

不要四个人各自独立写一条端到端流水线。按稳定接口拆分模块，并用 dump 文本作为阶段契约，更容易并行开发和集成。

## 21. 四人协作建议

一种低冲突分工：

| 成员 | 主责 | 同时负责的验证设施 |
|---|---|---|
| A | Lexer、Parser、AST | token/AST golden tests |
| B | 语义分析、CFG、DAG | scope/CFG verifier |
| C | SSA IR、中端优化 | IR parser/printer/verifier |
| D | 指令选择、调度、寄存器分配、汇编 | MIR verifier、ABI tests |

接口文件由全组评审后再修改。每个阶段都必须提供稳定文本 dump；这样后端可以用手写 IR 开发，不必等待前端全部完成，中端也可用手写 CFG 测试。

每周集成一次完整闭环，避免直到最后才发现 AST、IR 与 MIR 的数据模型不兼容。

## 22. 当前仓库的第一批具体任务

按现在的代码状态，建议立即完成：

1. 修复 `lexer.cpp` 中 `!` 和 `!=` 的 token 类型。
2. 给 lexer 增加覆盖全部 token 的单元测试。
3. 新建 `CMakeLists.txt` 和 `main.cpp`，先实现 stdin 读取。
4. 新建 `ast.hpp/parser.hpp/parser.cpp`，完成表达式和最小 `main` 解析。
5. 实现 AST dump，先用文本验证优先级：
   - `1 + 2 * 3`；
   - `a - b - c`；
   - `!a == b`；
   - `a || b && c`；
   - 函数调用参数嵌套。
6. 再进入语义和 CFG，避免在 lexer/parser 未稳定时同时调试后端。

当前仓库尚无构建配置、入口程序、Parser、AST、测试或后端，因此本指南描述的是目标架构，不代表这些模块已经存在。

## 23. 常见失败点清单

- 把 `&&`/`||` 当普通二元运算，破坏短路和函数调用副作用。
- 只记录 CFG 后继，不同步维护前驱，导致 Phi 和活跃性错误。
- 删除未使用返回值的 call，遗漏调用副作用。
- CFG 合并/删边后忘记同步修改 Phi 输入。
- Phi lowering 未拆 critical edge，使 copy 在错误路径执行。
- call clobber 未进入寄存器冲突，跨调用值被静默覆盖。
- 栈未保持 16 字节对齐，简单函数正常、复杂调用失败。
- `a0` 既作参数又作返回寄存器时并行搬运顺序错误。
- 直接用算术右移优化有符号除以 2 的幂，负数结果与向零截断不同。
- 为减少 IR 指令而显著提高寄存器压力，最终 spill 更多、运行更慢。
- 将 IR dump 打到 stdout，污染最终汇编。
- 使用宿主机 64 位有符号行为模拟 RV32，常量折叠结果不一致。

## 24. 完成定义（Definition of Done）

项目完成时应同时满足：

- PDF 中全部 ToyC 语法和语义特性均有端到端测试。
- 编译器严格通过 stdin/stdout 工作，并支持 `-opt`。
- 所有 CFG/SSA/MIR verifier 在测试集上通过。
- RISC-V32 ABI 的参数、返回、递归、保存寄存器和栈对齐均有专项测试。
- 优化前后程序退出码一致；随机差分测试达到足够样本量。
- 性能报告包含基准环境、测试程序、运行次数、统计方法、与 `gcc -O2` 的对比和热点分析。
- 实践报告能够解释每个 pass 的输入、输出、不变量、算法复杂度、正确性依据与实际性能收益。

如果工期有限，优先顺序始终是：**语义正确与 ABI 正确 > verifier 和测试 > SCCP/GVN/DCE > 寄存器分配质量 > 循环/调度等高级优化**。一个稳定、少 spill、分支简洁的后端，通常比堆叠许多不稳的高级 pass 更接近性能目标。
