int state = 1;

int work(int count) {
    int index = 0;
    while (index < count) {
        state = (state * 17 + index) % 1000003;
        index = index + 1;
    }
    return state;
}

int main() {
    return work(20000);
}
