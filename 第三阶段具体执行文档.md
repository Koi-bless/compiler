# ToyC 编译器第三阶段具体执行文档

> 阶段主题：SSA 高收益优化与面向 RISC-V32 的低成本代码质量改进
>
> 当前代码基线：提交 `ce7ab42931a1a82245c871c9cad55115cb57fb2c`（2026-08-12，提交说明“第二阶段”）
>
> 文档依据：`编译系统实践实现指南.md`、第一/第二阶段执行文档、当前仓库源码与 2026-08-12 实测结果
>
> 使用方式：严格按“步骤 0 → 步骤 12”实施；每一步通过本节验收后再进入下一步，不要同时改多个 pass 后再集中调试。

---

## 1. 文档用途

第二阶段已经把编译器从“CFG 直接走简单后端”升级为以下稳定流水线：

```text
source
  -> Lexer / Parser / Semantic
  -> non-SSA CFG/TAC
  -> SSA IR
  -> RISC-V32 MIR
  -> Phi/parallel-copy lowering
  -> liveness
  -> linear-scan register allocation + spill
  -> frame lowering
  -> assembly
```

第三阶段不再重写前端、SSA 构造或 ABI，而是在现有 SSA 和 MIR 边界上增加真正可工作的优化流水线。本文件必须回答四个问题：

1. 当前代码真实具备什么、缺少什么；
2. 第三阶段具体修改哪些文件、增加哪些接口；
3. 每个优化 pass 如何实现，尤其是 CFG/Phi/Value ID 如何保持合法；
4. 怎样证明优化前后语义一致，并证明 `-opt` 不再是空开关。

文档中的接口名和目录可以直接采用。若实现时调整命名，必须保持相同模块边界和不变量。

---

## 2. 第三阶段范围

### 2.1 阶段目标

第三阶段完成后，编译器应形成下面的优化路径：

```text
CFG -> SSA construction
    -> mandatory canonicalization
    -> Local DAG / InstCombine
    -> SCCP
    -> SimplifyCFG
    -> DCE
    -> GVN
    -> loop analysis / LICM
    -> GVN + DCE + SimplifyCFG cleanup
    -> instruction selection
    -> compare-branch fusion
    -> copy-aware linear scan
    -> rematerializing spill rewrite
    -> frame lowering / assembly
```

本阶段的核心成果不是“pass 名称齐全”，而是：

- `-opt` 真实改变中端和后端行为；
- 每个 pass 后的 IR 都能通过 verifier；
- 常量分支、冗余表达式、死计算和安全循环不变量确实被消除；
- 优化不跨越 call/global memory 的副作用边界；
- 优化前、优化后和 GCC 参考结果持续一致；
- 优化结果可 dump、可统计、可复现、可单独测试。

### 2.2 本阶段必须完成

按优先级分三层。

第一层是必须稳定交付的 SSA 优化基础：

- IR 变换公共工具：use list、全局替换、CFG 重建、不可达块删除、ID 压紧；
- pass runner、`--verify-each` 接线、逐 pass dump/统计；
- 临时块内 DAG/局部值编号与 InstCombine；
- SCCP；
- SimplifyCFG；
- DCE；
- GVN；
- 优化单测、端到端测试和随机差分。

第二层是本阶段应完成的循环优化：

- IR 支配关系复用；
- natural loop 分析；
- preheader 识别或拆边创建；
- 保守 LICM，只外提明确纯且不会 trap 的运算。

第三层是后端低风险收益项：

- 单用途比较结果与条件分支融合；
- 保守 copy coalescing/寄存器分配偏好；
- spilled 常量 rematerialization；
- 优化前后汇编结构与性能统计。

### 2.3 本阶段明确不做

以下内容留给第四阶段性能冲刺，不能阻塞第三阶段：

- 图着色寄存器分配；
- 全局指令调度；
- 大规模函数内联；
- 循环展开；
- 完整归纳变量识别和强度削弱；
- 基于 profile 的块布局；
- 跨过程副作用/别名分析；
- 跨 call 的 global load 公共子表达式消除；
- 为追求个别 benchmark 而改变 ToyC 语言语义。

如果第一层没有全部通过，不得用 LICM、内联或更复杂寄存器分配掩盖基础问题。

---

## 3. 当前代码分析

### 3.1 当前工程结构

当前与第三阶段直接相关的文件如下：

```text
include/toyc/ir/
  cfg.hpp              非 SSA CFG
  cfg_utils.hpp        CFG reachable/RPO/remove/splitEdge
  dominator.hpp        仅接受 CFGFunction 的支配关系
  ir.hpp               SSA IR 数据结构
  ir_printer.hpp        SSA dump
  ssa_builder.hpp       CFG -> SSA
  verifier.hpp          CFG/SSA verifier

include/toyc/backend/
  isel.hpp              SSA IR -> MIR
  mir.hpp               MIR opcode/operand/function
  liveness.hpp          活跃性与 live interval
  regalloc.hpp          线性扫描
  frame.hpp             frame lowering
  asm_printer.hpp       纯汇编打印

src/main.cpp            Driver 和选项解析
tests/test_support.hpp  测试用完整流水线
scripts/                e2e、随机差分、benchmark 脚本
```

当前没有 `include/toyc/opt/`、`src/opt/` 或任何优化 pass 文件。第三阶段应新建独立 `opt` 模块，不要继续把优化逻辑塞进 `ssa_builder.cpp`、`isel.cpp` 或 `main.cpp`。

### 3.2 当前 SSA IR 的能力与约束

`include/toyc/ir/ir.hpp` 已有以下操作：

```text
Param, Constant, Copy,
Add, Sub, Mul, SDiv, SRem,
ICmpLT, ICmpGT, ICmpLE, ICmpGE, ICmpEQ, ICmpNE,
LogicalNot, Phi,
LoadGlobal, StoreGlobal, Call
```

控制流终结指令为：

```text
IRJump
BranchValue
ReturnValue
IRUnreachable
```

这套 IR 足够完成 SCCP、局部 DAG、GVN、DCE 和基础 LICM，不需要为前三个 pass 扩充 opcode。需要注意：

- 局部变量已经被 SSA 化，Phi 位于块首；
- 全局变量仍是显式 `LoadGlobal/StoreGlobal`；
- call 默认有任意可观察副作用；
- `IRFunction::blocks`、ValueId、InstId 都要求从 0 连续编号；
- `valueCount` 必须恰好覆盖所有定义，不能留下“洞”；
- `instructionCount` 必须等于现存指令数；
- Phi 输入顺序必须与排序后的 predecessor 顺序完全一致；
- 普通 use 必须被 definition 支配，Phi use 按对应前驱边检查。

这些严格约束对发现 bug 很有价值，但也意味着 pass 删除块或指令后不能只 `erase`：必须统一更新 CFG、Phi、Value 和 InstId。

### 3.3 当前 verifier 的能力与第三阶段缺口

当前 `verifyIR` 已检查：

- block ID、instruction ID、value definition 的连续性和唯一性；
- terminator 与 successor 一致；
- successor/predecessor 对称；
- Phi 位于块首、输入数和 predecessor 对齐；
- opcode 的结果数、操作数数目、global/callee 合法性；
- SSA definition dominates use；
- return 类型匹配。

第三阶段需要补强：

- 明确拒绝 IR 中残留不可达块；
- 检查 entry predecessor 为空；
- 检查 predecessor 也不存在重复项；
- 检查 Phi predecessor 集合，而不仅依赖相同下标；
- 检查 `Param` 只在 entry，参数编号合法且不重复；允许优化后删除未使用参数，因此最终优化 IR 不要求参数编号仍连续；
- 检查 `Constant`/`Param` 必有 immediate，其他 opcode 不携带无关字段；
- 检查产生 bool 的比较和 `LogicalNot` 仍只作为 i32 0/1 使用（类型层面暂不新增 bool 类型）；
- 在测试中构造损坏 IR，保证每条新规则确实会失败。

### 3.4 当前 CFG 工具不能直接用于 IR pass

`cfg_utils.cpp` 的 `computeReachable`、`computeReversePostOrder`、`removeUnreachable` 和 `replaceSuccessor` 只接受 `CFGFunction`。当前只有 `splitEdge` 接受 `IRFunction`。

第三阶段不能通过把 `IRFunction` 临时转换回 `CFGFunction` 来复用工具，因为这样会丢失 Phi 和 Value。应新增原生 IR 工具，或把只读图算法抽象为共享内部实现。

### 3.5 当前支配关系只支持非 SSA CFG

`DominatorInfo` 构造函数只接受 `CFGFunction`。SSA 构造时够用，但 GVN 和 LICM 直接操作 `IRFunction`，必须增加 IR 重载：

```cpp
explicit DominatorInfo(const CFGFunction& function);
explicit DominatorInfo(const IRFunction& function);
```

两种构造函数应转交同一个私有图实现，避免维护两份支配算法。

### 3.6 当前 `-opt` 是空开关

`src/main.cpp` 能解析 `-opt`，但流水线末尾只有：

```cpp
(void)options.optimize;
```

即 `-opt` 没有驱动任何 pass。实测一个包含 `x + 0`、`2 * 3` 和常量分支的程序，普通模式和 `-opt` 输出汇编逐字节一致，IR 中仍保留 add、mul 和所有可达路径。

因此第三阶段最直观的功能门禁是：

```text
同一优化专项输入：
  普通模式与 -opt 运行结果相同；
  -opt 的 IR/汇编结构更简单；
  -opt 输出不再与普通模式逐字节恒等。
```

### 3.7 当前后端可承载优化，但仍有低成本缺口

当前 `isel.cpp` 总是把比较结果物化为 VReg，再由 `BRCOND` 对该 VReg 生成 `bnez + j`。若比较只用于分支，可直接生成 `beq/bne/blt/bge + j`，少一次布尔物化。

当前线性扫描：

- 依据 live interval 分配 caller/callee-saved；
- 能 spill 并使用 `t5/t6` 重写；
- 只在分配后删除物理寄存器相同的 COPY；
- 不利用虚拟 COPY 作为分配偏好；
- spilled 常量仍会先 `li`、写 spill slot，使用时再 `lw`，没有 rematerialization。

这些适合作为第三阶段末尾的 MIR/RA 改进，但必须在 SSA 优化正确后实施。

### 3.8 2026-08-12 实测基线

已在当前工作区实际执行：

```text
Windows build-stage2-final Release：11/11 C++ 测试通过
WSL build-wsl：构建通过
WSL CTest：12/12 通过（含 e2e_tests）
随机差分：seed 0..99，100/100 通过
```

`stateful_loop.tc` 当前一次基准记录：

```text
variant      text_bytes  median_ms
toyc               240         28
toyc-opt           240         27
gcc-o2             252         28
```

这里 `toyc` 与 `toyc-opt` 的 text size 相同，1 ms 差异属于 QEMU/计时噪声，不能视为优化收益。第三阶段必须以汇编/IR 结构、静态大小和多次统计共同判断，不以单次 wall time 下结论。

### 3.9 当前仓库卫生问题

当前 `git status --short` 显示：

```text
?? scripts/
```

但 `README.md`、CTest 和第二阶段验收已经依赖这些脚本。第三阶段步骤 0 必须先确认脚本应纳入版本控制并提交；否则干净 clone 无法复现第二阶段声称的 e2e、差分和 benchmark 能力。

---

## 4. 第二阶段实际修改了什么

第三阶段实施前，应把第二阶段产物视为冻结接口：

- 前端：Lexer、Parser、AST、语义分析和诊断可用；
- CFG：非 SSA TAC、短路控制流、break/continue、全局 load/store 可用；
- SSA：支配边界插 Phi、rename、trivial Phi 清理可用；
- verifier：CFG/SSA 结构和 dominance use 检查可用；
- MIR：虚拟寄存器、栈槽、物理寄存器、call clobber 可表达；
- Phi lowering：critical edge 拆分、parallel copy 解环可用；
- 后端：RV32IM 指令选择、线性扫描、spill、frame lowering、ABI 可用；
- Driver：stdin/stdout、各阶段 dump、`--verify-each` 可用；
- 测试：Windows 单测、WSL e2e、随机差分和性能脚本可用。

第三阶段允许扩展这些接口，但不允许破坏普通模式的语义、ABI 或现有 dump 的确定性。

---

## 5. 第三阶段将修改什么

### 5.1 现有文件修改表

| 文件 | 修改内容 |
|---|---|
| `CMakeLists.txt` | 加入 `src/opt/*.cpp` 和新的 backend 优化源文件 |
| `include/toyc/ir/cfg_utils.hpp` | 增加 IR reachable/RPO/CFG 重建/不可达删除接口，或把 IR 接口放入 `opt/ir_utils.hpp` |
| `src/ir/cfg_utils.cpp` | 抽取共享图遍历，保持 CFG 旧接口行为 |
| `include/toyc/ir/dominator.hpp` | 增加 `IRFunction` 构造重载 |
| `src/ir/dominator.cpp` | CFG/IR 共用支配实现 |
| `src/ir/verifier.cpp` | 增强不可达、entry、Param、字段合法性等检查 |
| `include/toyc/backend/mir.hpp` | 增加融合分支 opcode；必要时增加 rematerialization 元信息 |
| `src/backend/isel.cpp` | 在优化模式识别“单用途 compare -> branch” |
| `src/backend/mir_verifier.cpp` | 验证新分支 opcode 的 operands/targets/stage |
| `src/backend/mir_printer.cpp` | 稳定打印新 MIR 指令 |
| `src/backend/asm_printer.cpp` | 输出 `beq/bne/blt/bge` 等融合分支 |
| `include/toyc/backend/regalloc.hpp` | 增加 RA 选项、copy hint 和 rematerialization 结果 |
| `src/backend/regalloc.cpp` | 实现保守 coalescing preference 和常量重物化 |
| `src/main.cpp` | 接入优化流水线、dump、统计与优化后 verifier |
| `tests/test_support.hpp` | 支持 normal/opt 构建和单独运行 IR pass |
| `tests/CMakeLists.txt` | 注册新增优化、循环和后端优化测试 |
| `scripts/benchmark.sh` | 增加静态指令分类、优化前后变化和参数记录 |
| `README.md` | 补充 `-opt` 真实流水线、dump 和复验命令 |

### 5.2 建议新增文件

```text
include/toyc/opt/
  pass.hpp
  ir_utils.hpp
  local_dag.hpp
  instcombine.hpp
  sccp.hpp
  simplify_cfg.hpp
  dce.hpp
  gvn.hpp
  loop_analysis.hpp
  licm.hpp
  pipeline.hpp

src/opt/
  ir_utils.cpp
  local_dag.cpp
  instcombine.cpp
  sccp.cpp
  simplify_cfg.cpp
  dce.cpp
  gvn.cpp
  loop_analysis.cpp
  licm.cpp
  pipeline.cpp

tests/
  ir_test_builder.hpp
  ir_utils_tests.cpp
  local_dag_tests.cpp
  instcombine_tests.cpp
  sccp_tests.cpp
  simplify_cfg_tests.cpp
  dce_tests.cpp
  gvn_tests.cpp
  loop_tests.cpp
  licm_tests.cpp
  backend_opt_tests.cpp

tests/cases/e2e/
  opt_constant_branch.tc
  opt_dead_code.tc
  opt_redundant_expr.tc
  opt_global_barrier.tc
  opt_loop_invariant.tc

tests/cases/bench/
  constant_branch.tc
  redundant_expr.tc
  loop_invariant.tc
```

文件较多但职责单一。不要建立一个数千行的 `optimizer.cpp`。

---

## 6. 第三阶段固定设计决策

### 6.1 SSA IR 是主要优化层

总指南中的“DAG 局部优化”在当前工程中采用临时 DAG/局部值编号 pass 实现：DAG 只在处理一个 SSA 基本块时存在，不新增一个长期存活、需要再次 lowering 的第四种 IR。

原因：当前 non-SSA CFG 已立即进入 SSA，而 SSA 上的 ValueId 天然就是 DAG 输入边。临时 `DAGKey -> dominating ValueId` 能完成块内 CSE 和 constant folding；随后 GVN 扩展到跨块。这样满足 DAG 优化目标，又不重复 SSA 构造前后的数据结构。

### 6.2 32 位语义必须显式实现

常量折叠不能直接依赖宿主 C++ 的有符号溢出。统一提供：

```cpp
std::int32_t wrapAdd(std::int32_t a, std::int32_t b);
std::int32_t wrapSub(std::int32_t a, std::int32_t b);
std::int32_t wrapMul(std::int32_t a, std::int32_t b);
std::optional<std::int32_t> foldBinary(IROp op, std::int32_t a,
                                       std::int32_t b);
```

加减乘先 `bit_cast<uint32_t>` 运算，再转回 `int32_t`。除法和取模：

- 除数为 0：返回 `nullopt`，不折叠；
- `INT_MIN / -1`：返回 `nullopt`，保持现有运行行为，不在优化器引入宿主 UB；
- `INT_MIN % -1`：可折叠为 0；
- 比较结果固定为 0 或 1；
- `LogicalNot(x)` 固定返回 `x == 0 ? 1 : 0`。

该函数由 InstCombine、LocalDAG 和 SCCP 共用，禁止三份不同实现。

### 6.3 副作用和可移动性集中定义

新增统一 traits：

```cpp
bool producesValue(IROp op, const IRInstruction& inst,
                   const SemanticResult& semantic);
bool hasSideEffects(const IRInstruction& inst);
bool readsMemory(const IRInstruction& inst);
bool mayTrap(const IRInstruction& inst);
bool isPure(const IRInstruction& inst);
bool isSafeToSpeculate(const IRInstruction& inst);
bool isCommutative(IROp op);
```

第一版固定口径：

| 指令 | pure | side effect | safe to speculate | 可进入 GVN | 可 LICM |
|---|---:|---:|---:|---:|---:|
| Constant/Copy/加减乘/比较/not | 是 | 否 | 是 | 是 | 是 |
| SDiv/SRem | 是 | 否 | 否 | 是，但不改变执行路径 | 否 |
| LoadGlobal | 否 | 否 | 否 | 否 | 否 |
| StoreGlobal | 否 | 是 | 否 | 否 | 否 |
| Call | 否 | 是 | 否 | 否 | 否 |
| Phi/Param | 特殊 | 否 | 不移动 | 不作为普通表达式 | 否 |

第一版不做 memory version。任何 call 都是所有 global memory 的屏障。

### 6.4 变换后统一 canonicalize

所有会删除/增加 block、instruction 或 value 的 pass 最后调用统一 canonicalization：

```text
1. 从 terminator 重建 successors；
2. 从 successors 重建并排序 predecessors；
3. 删除不存在边对应的 Phi 输入；
4. 按 predecessor 顺序重排 Phi 输入；
5. 删除不可达块并重映射 BlockId；
6. 重映射所有 ValueId；
7. 连续重编号 InstId；
8. 更新 valueCount/instructionCount；
9. verifyIR。
```

pass 不得各自实现一套 ID 压紧逻辑。

### 6.5 默认模式与 `-opt` 模式分离

默认模式只运行保证 IR 规范、减少后端无意义负担的清理：

```text
CanonicalizeIR
TrivialPhiElimination
DeadInstructionElimination（保守模式）
```

`-opt` 模式运行：

```text
CanonicalizeIR
LocalDAG
InstCombine

最多 4 轮：
  SCCP
  SimplifyCFG
  InstCombine
  DCE
  若本轮无变化则提前停止

GVN
DCE
LoopAnalysis
LICM
GVN
DCE
SimplifyCFG
CanonicalizeIR
```

固定最大轮数，防止 pass bug 导致无限循环。每轮次序不得由容器迭代顺序决定。

### 6.6 `--verify-each` 的定义

开启时：

- 初始 SSA 构造后验证一次；
- 每个 pass 修改 IR 后立即 canonicalize 并验证；
- 即使 pass 报告 `changed == false`，Debug 构建也至少在 pass pipeline 边界验证；
- MIR 每个既有阶段继续验证；
- verifier 失败返回码保持 2；
- 所有 dump、pass 名称和统计写 stderr，stdout 只含汇编。

---

## 7. 执行步骤 0：冻结第二阶段基线

### 7.1 操作内容

1. 把 `scripts/` 纳入版本控制，或明确解释为何不提交；推荐提交。
2. 从新目录构建 Windows Debug/Release。
3. 在 WSL 重新配置 Release，执行完整 CTest。
4. 随机差分至少跑 seed 0..999。
5. 保存当前 benchmark 输出和编译器提交号。
6. 保存下列专项输入的 normal IR、normal assembly，作为回归对照：
   - constant branch；
   - repeated expression；
   - global load/store + call；
   - loop invariant；
   - register pressure/spill。

建议创建不提交的日志目录 `build-stage3-baseline/metrics/`，不要把机器相关绝对路径写入源码。

### 7.2 基线命令

```powershell
cmake -S . -B build-stage3-win -DBUILD_TESTING=ON
cmake --build build-stage3-win --config Debug -j 2
ctest --test-dir build-stage3-win -C Debug --output-on-failure
cmake --build build-stage3-win --config Release -j 2
ctest --test-dir build-stage3-win -C Release --output-on-failure
```

```sh
cmake --preset wsl-release
cmake --build --preset wsl-release -j2
ctest --preset wsl-release
./scripts/differential_test.sh \
  --compiler ./build-wsl/compiler --seed-start 0 --count 1000
./scripts/benchmark.sh \
  ./build-wsl/compiler tests/cases/bench/*.tc
```

### 7.3 本步骤完成定义

- 新构建目录全部成功；
- Windows 11/11、WSL 12/12 通过；
- 1000 个随机程序无 mismatch；
- baseline 命令、环境、提交号和输出已记录；
- `git status` 不再因遗漏测试脚本而出现无法解释的 `?? scripts/`。

---

## 8. 执行步骤 1：建立 IR 变换工具层

这是第三阶段最重要的基础步骤。没有这一层，不得开始 SCCP 或 SimplifyCFG。

### 8.1 `ir_utils.hpp` 建议接口

```cpp
#pragma once

#include <cstddef>
#include <optional>
#include <vector>

#include "toyc/ir/ir.hpp"

namespace toyc {

struct IRUse {
    BlockId block{};
    std::optional<InstId> instruction;
    std::size_t operandIndex{};
    bool phiEdge = false;
    bool terminator = false;
};

using IRUseLists = std::vector<std::vector<IRUse>>;

IRUseLists buildUseLists(const IRFunction& function);
std::vector<bool> computeReachable(const IRFunction& function);
std::vector<BlockId> computeReversePostOrder(const IRFunction& function);

bool replaceAllUses(IRFunction& function, ValueId from, ValueId to);
void rebuildIRControlFlow(IRFunction& function);
bool removeUnreachableIR(IRFunction& function);
bool eliminateTrivialPhis(IRFunction& function);
void compactIR(IRFunction& function);

IRInstruction* findDefinition(IRFunction& function, ValueId value);
const IRInstruction* findDefinition(const IRFunction& function, ValueId value);

} // namespace toyc
```

若与 `cfg_utils.hpp` 的函数重载产生可读性问题，可以命名为 `computeIRReachable`；但不要靠模板错误地让 CFG/IR 共用 mutation 逻辑。

### 8.2 use list 必须覆盖的使用点

`buildUseLists` 必须记录：

- 普通 instruction operands；
- Phi 的每个 `PhiInput::value`，并记录 predecessor edge；
- `BranchValue::condition`；
- `ReturnValue::value`。

StoreGlobal 和 Call 参数已经位于普通 operands。不能遗漏 terminator，否则 DCE/GVN 可能删除 return 或 branch 使用的值。

### 8.3 `replaceAllUses`

替换范围与 use list 相同。禁止替换 definition 的 `result`。完成后允许原定义暂时成为 dead instruction，后续 DCE/compact 清除。

必须覆盖测试：

- 普通二元操作数；
- branch condition；
- return value；
- Phi edge input；
- 一个 value 多次出现在同一指令中，如 `add %x, %x`。

### 8.4 `rebuildIRControlFlow`

严格由 terminator 推导 successor：

```text
IRJump       -> 一个 successor
BranchValue  -> true/false；相同目标只保留一次
ReturnValue  -> 空
Unreachable  -> 空
```

随后清空全部 predecessor，再从 successor 反向建立并排序。重建完成后，逐个 Phi：

1. 删除 predecessor 已不存在的输入；
2. 拒绝缺少新 predecessor 输入的情况；
3. 按 predecessor 升序排序。

“拒绝缺输入”很重要：通用工具不能猜测新边上 Phi 应取哪个值；创建新边的具体 pass 必须先提供正确输入。

### 8.5 `removeUnreachableIR`

实现顺序：

1. 从 entry 标记 reachable；
2. 为保留块建立 `oldBlock -> newBlock`；
3. 拷贝/移动保留块；
4. 重写 entry、terminator target、successor、predecessor；
5. 删除 Phi 中来自不可达 predecessor 的输入并重写 predecessor ID；
6. 调用 `rebuildIRControlFlow`；
7. 调用 `compactIR`。

不得先 erase blocks 再用旧 BlockId 访问 vector。

### 8.6 `compactIR`

分两遍完成：

第一遍按 block ID、块内 instruction 顺序扫描所有现存 `result`，生成：

```text
old ValueId -> new dense ValueId
old InstId  -> new dense InstId（实际只需顺序赋值）
```

第二遍重写：

- instruction.result；
- instruction.operands；
- PhiInput.value；
- BranchValue.condition；
- ReturnValue.value。

最后设置 `valueCount` 和 `instructionCount`。若任何 use 没有映射，立即抛出 `CompileError(..., "IR transformation", ...)`，不要让错误拖到 isel。

### 8.7 trivial Phi 清理

Phi 可删除的条件：忽略对自身 result 的输入后，其余输入全部等于同一个 Value。处理：

```text
%p = phi [bb1: %x] [bb2: %x]       -> replace %p with %x
%p = phi [bb1: %p] [bb2: %x]       -> replace %p with %x
```

若除自引用外没有输入，不得随意生成 0；保留并让 verifier/上游暴露问题。使用 worklist 反复处理，因为删除一个 Phi 可能让另一个 Phi 变 trivial。

### 8.8 工具层测试

新建手写 IR builder，至少构造：

- 直线块；
- diamond + Phi；
- loop header + 自引用 Phi；
- 不可达块引用独立 Value；
- 删除一条 branch edge 后的 Phi；
- value/inst/block 均有编号洞的 IR。

每个 mutation 测试后调用 `verifyIR`，并检查 dump 连续编号。

### 8.9 本步骤完成定义

- 工具层测试全部通过；
- 删除不可达块后 Phi 正确；
- RAUW 覆盖 instruction/Phi/terminator；
- `compactIR` 后三类 ID 连续；
- 同一 IR canonicalize 两次，第二次 dump 不变；
- 现有 12 个 WSL CTest 无回归。

---

## 9. 执行步骤 2：Pass runner、Driver 与观测接口

### 9.1 Pass 结果

不必先建立复杂继承体系。建议用轻量接口：

```cpp
struct PassResult {
    bool changed = false;
    std::size_t instructionsRemoved = 0;
    std::size_t instructionsReplaced = 0;
    std::size_t blocksRemoved = 0;
};

struct OptimizationOptions {
    bool enabled = false;
    bool verifyEach = false;
    bool dumpBefore = false;
    bool dumpAfterEach = false;
    bool printStats = false;
    unsigned maxFixpointIterations = 4;
};

void runOptimizationPipeline(IRModule& module,
                             const SemanticResult& semantic,
                             const OptimizationOptions& options,
                             std::ostream& diagnostics);
```

每个 pass 接受 `IRFunction&` 或 `IRModule&`，返回 `PassResult`。Module runner 汇总函数结果。

### 9.2 Driver 选项

在现有选项上新增：

```text
--dump-ir-before-opt
--dump-ir-after-each
--print-pass-stats
```

保留 `--dump-ir`，但定义为“优化流水线完成后的最终 SSA IR”。流水线顺序改为：

```cpp
auto ir = SSABuilder(semantic).build(cfg);
if (verifyIntermediate) verifyIR(ir, semantic);

if (options.dumpIrBeforeOpt) printIR(std::cerr, ir, semantic);
runOptimizationPipeline(ir, semantic, ...);
if (verifyIntermediate) verifyIR(ir, semantic);
if (options.dumpIr) printIR(std::cerr, ir, semantic);

auto machine = InstructionSelector(...).lower(ir);
```

删除 `(void)options.optimize`。

### 9.3 dump 格式

逐 pass dump 在 stderr 加稳定边界：

```text
*** IR before SCCP (iteration 1) ***
...
*** IR after SCCP: changed=1, inst_removed=4, blocks_removed=1 ***
...
```

测试不要依赖耗时，只依赖 pass 名称、changed 和结构计数。输出函数、block 和 map 时必须排序。

### 9.4 Pass runner 测试

- 无 `-opt` 时不运行收益 pass；
- `-opt` 时顺序固定；
- `--verify-each` 能在故意损坏 pass 后立即失败；
- fixpoint 在无变化时提前结束；
- fixpoint 最多 4 轮；
- dump/统计在 stderr，stdout 仍可直接交给汇编器。

### 9.5 本步骤完成定义

- `-opt` 已真实进入 pipeline；
- `--dump-ir` 代表最终 IR；
- before/after-each/stats 可独立使用；
- pass 顺序有单测；
- stdout 无任何新增调试文字。

---

## 10. 执行步骤 3：统一常量折叠、InstCombine 与块内 DAG

### 10.1 先实现共享 constant folder

建议在 `ir_utils.cpp` 或单独 `constant_fold.cpp` 中提供：

```cpp
std::optional<std::int32_t>
foldUnary(IROp op, std::int32_t operand);

std::optional<std::int32_t>
foldBinary(IROp op, std::int32_t lhs, std::int32_t rhs);
```

只接受相应 unary/binary opcode。非法 opcode 应 assert 或返回 nullopt，不能静默得到错误常量。

### 10.2 InstCombine 第一版规则

安全规则：

```text
x + 0 -> x          0 + x -> x
x - 0 -> x
x * 1 -> x          1 * x -> x
x * 0 -> 0          0 * x -> 0
x / 1 -> x
x % 1 -> 0
x - x -> 0
x == x -> 1
x != x -> 0
x <  x -> 0
x >  x -> 0
x <= x -> 1
x >= x -> 1
not(constant) -> constant
binary(constant, constant) -> constant（除零/INT_MIN/-1 除外）
copy x -> x
```

`x * 0` 在 SSA IR 中可以替换结果，因为 `x` 的求值及其副作用已经由产生 `x` 的指令独立表达；但不能因此删除产生 x 的 side-effect instruction。DCE 只会删除纯定义。

### 10.3 常量物化策略

不要把位于 Phi 前缀中的 Phi 直接改成 Constant，否则会出现“普通指令位于后续 Phi 之前”。统一使用：

```cpp
ValueId getOrCreateEntryConstant(IRFunction& function, std::int32_t value);
```

常量插入 entry 的普通指令序列最前部，按数值稳定复用。完成所有替换后重排 Phi/普通指令并 `compactIR`。

### 10.4 块内 DAG/Local Value Numbering

临时 key：

```cpp
struct DAGKey {
    IROp op{};
    ValueId lhs{};
    std::optional<ValueId> rhs;
    std::optional<std::int32_t> immediate;
    auto operator<=>(const DAGKey&) const = default;
};
```

每进入一个 block 清空表，顺序扫描普通指令：

1. Copy 直接把 result 的代表值设为 operand 代表；
2. Constant 按 immediate 合并；
3. 纯运算先把 operands 替换为当前代表；
4. 对 Add/Mul/EQ/NE 按 ValueId 排序；
5. key 已存在则 RAUW 当前 result 到已有 value；
6. key 不存在则记录；
7. LoadGlobal/StoreGlobal/Call/Phi/Param 不进入表；
8. block 结束后让 DCE 删除冗余定义。

这里的 DAG 生命周期只覆盖单块；不得跨块复用一个不支配当前 use 的值。

### 10.5 测试

至少覆盖：

- `(a+b) + (a+b)` 在同一块只保留一个内层 add；
- `a+b` 与 `b+a` 合并；
- `a-b` 与 `b-a` 不合并；
- 两个相同表达式位于不同分支时，LocalDAG 不跨块合并；
- call 两侧的表达式仅纯局部操作可按 Value 处理，global load 不合并；
- 所有算术边界：0、-1、INT_MIN、INT_MAX、wrap-around；
- 除零不折叠；
- pass 连跑两次，第二次 `changed == false`。

### 10.6 本步骤完成定义

- constant folder 只有一份实现；
- InstCombine 规则全部有正/负向测试；
- LocalDAG 消除块内冗余且不越过内存规则；
- 优化后 IR 通过 verifier；
- `-opt` 对专项输入产生可见变化。

---

## 11. 执行步骤 4：SCCP

### 11.1 格值

```cpp
enum class LatticeKind { Unknown, Constant, Overdefined };

struct LatticeValue {
    LatticeKind kind = LatticeKind::Unknown;
    std::int32_t constant = 0;
};
```

meet 规则：

```text
Unknown meet X                  -> X
Constant(a) meet Constant(a)    -> Constant(a)
Constant(a) meet Constant(b)    -> Overdefined, a != b
Overdefined meet X              -> Overdefined
```

状态只能单调从 Unknown 向 Constant/Overdefined 演进，不能从 Overdefined 退回 Constant。

### 11.2 可执行块和边

同时维护：

```cpp
std::vector<bool> executableBlocks;
std::set<std::pair<BlockId, BlockId>> executableEdges;
```

入口块一开始可执行。Jump 标记唯一边；Branch：

- condition Constant(0)：只标 false edge；
- condition Constant(nonzero)：只标 true edge；
- condition Overdefined：两条边都标；
- condition Unknown：暂不新增边，等待值变化。

Phi 只 meet 已标记为 executable 的 predecessor edge，不能把不可执行分支的值混进来。

### 11.3 指令 transfer function

```text
Param                  -> Overdefined
Constant(c)            -> Constant(c)
Copy(x)                -> lattice(x)
纯 unary/binary        -> 常量则 fold；有 Overdefined 则 Overdefined；否则 Unknown
Phi                    -> meet 可执行输入
LoadGlobal             -> Overdefined
Call with int result   -> Overdefined
StoreGlobal/void call  -> 无结果，只保留可执行性
```

使用两个 worklist 更清楚：

- CFG worklist：新可执行块/边；
- SSA worklist：lattice 发生变化的 Value，重新访问其 uses。

项目规模不大，也允许确定性全扫描到收敛，但必须设置内部断言并保证状态单调。worklist 版本更符合 SCCP，推荐实现。

### 11.4 分析与改写分离

第一阶段只分析，不边遍历边 erase。收敛后统一改写：

1. 把 Constant lattice 的可替换 value RAUW 到 entry 常量；
2. 把已知 branch 改成 IRJump；
3. 删除不可执行块；
4. 修复 Phi 输入；
5. eliminateTrivialPhis；
6. DCE；
7. compact + verify。

这样避免 worklist 中保存的引用因 vector erase 失效。

### 11.5 SCCP 必测图形

1. 直线跨指令常量传播；
2. constant diamond，只保留 taken arm；
3. Phi 一条 executable edge，Phi 变常量；
4. 参数条件，两条边都 executable；
5. 循环初值 + backedge，格值最终 Overdefined；
6. 永不执行块中的 call 被整体删除；
7. 可执行路径中的 call 即使结果不用也保留；
8. global load 绝不当常量；
9. 同值常量 Phi 保持 Constant；
10. 两个不同常量 Phi 变 Overdefined。

### 11.6 本步骤完成定义

- 格值单调，有 lattice 单测；
- executable edge 驱动 Phi；
- constant branch 删除不可达块；
- call/global 不被错误传播；
- SCCP 后 verifier 通过；
- SCCP 连跑第二次无变化；
- 随机差分至少 1000 个 seed 通过。

---

## 12. 执行步骤 5：SimplifyCFG

### 12.1 第一版变换顺序

每轮按 block ID 顺序执行：

1. `branch c, X, X -> jump X`；
2. branch condition 定义为 Constant 时折叠；
3. 删除不可达块；
4. 清理 trivial Phi；
5. 穿透无指令、只有 jump 的 trampoline block；
6. 合并 `A -> B`，要求 A 单后继且 B 单前驱；
7. canonicalize。

最多循环 4 轮，或无变化提前结束。

### 12.2 删除一条边时如何更新 Phi

假设删除 `pred -> target`：

```text
1. 先改 pred terminator；
2. 从 target 每个 Phi 删除 predecessor == pred 的输入；
3. rebuildIRControlFlow；
4. eliminateTrivialPhis；
```

不能只改 successors，因为 verifier 以 terminator 为真源。

### 12.3 穿透空块

只处理满足以下条件的 block B：

- B 不是 entry；
- B 无 Phi、无普通 instruction；
- terminator 是 `jump C`；
- 把每个 `P -> B` 改为 `P -> C` 不会产生重复且语义不明的边。

C 的 Phi 原来若有 `[B: v]`，需为 B 的每个 predecessor P 复制为 `[P: v]`。因为 B 无定义，v 必须支配这些新边；变换后 verifier 会再次确认。

### 12.4 合并直线块

对 `A -> B`：

- A terminator 必须是无条件 jump；
- A.successors 只有 B；
- B.predecessors 只有 A；
- B 不是 entry；
- B 中每个 Phi 应只有 `[A: v]`，先 RAUW 后删除；
- 移除 A 的 jump，把 B 普通指令和 terminator 移入 A；
- 所有原来指向 B 的边/Phi predecessor 改为 A；
- 删除 B 并 compact block IDs。

不要在 range-for 持有引用时 push/erase `function.blocks`。

### 12.5 SimplifyCFG 测试

- 相同 true/false target；
- constant branch + Phi；
- 多层空 jump 链；
- 空块通向带 Phi 的 merge；
- 直线块合并；
- loop header 不被错误合并；
- critical edge 仍合法；
- entry 永不被删除；
- 同一 pass 第二次运行稳定。

### 12.6 本步骤完成定义

- 所有第一版规则实现；
- 每种 CFG mutation 后 Phi 正确；
- 不可达块为 0；
- block ID 连续；
- verifier 和 e2e 全通过。

---

## 13. 执行步骤 6：DCE

### 13.1 根集合

从以下对象开始反向标记：

- `StoreGlobal`；
- 所有 `Call`，包括返回值未使用的 int call；
- 保守阶段的 `SDiv/SRem`，因为 `mayTrap == true`；
- BranchValue.condition；
- ReturnValue.value。

Jump/Return 本身是 terminator，不参与 instruction 删除。

LoadGlobal 若结果无用可删除，因为 ToyC 没有 volatile；但不能跨 store/call 合并或移动。

### 13.2 反向标记

建立 `ValueId -> defining instruction`。根 instruction 入 worklist；terminator operand 对应 definition 入 worklist。每弹出一个活指令，把其普通 operands 和 Phi inputs 的 definitions 标活。

最后删除：

```text
没有被标活 && 没有副作用 && 不属于保守 mayTrap 根
```

然后 compact。不要以“result 没 use”做单轮 erase，因为删除一层会暴露更深层 dead chain；标记法一次即可得到闭包。

### 13.3 DCE 测试

- 多层 dead arithmetic chain 全删；
- unused constant/param/copy 全删；
- return 依赖链全保留；
- branch 依赖链全保留；
- unused int call 保留，但其纯参数计算只要被 call 使用就保留；
- unused LoadGlobal 删除；
- StoreGlobal 及其 value 链保留；
- SDiv/SRem 按保守口径保留；
- Phi 仅被 dead value 使用时删除，CFG 不受损。

### 13.4 本步骤完成定义

- dead chain 完整删除；
- 副作用根无遗漏；
- terminator use 无遗漏；
- DCE 幂等；
- verifier、e2e、1000 seed 差分通过。

---

## 14. 执行步骤 7：GVN

### 14.1 为 IR 增加支配分析入口

扩展 `DominatorInfo`：

```cpp
DominatorInfo::DominatorInfo(const IRFunction& function);
```

两种函数类型都提取：entry、predecessors、successors、block count、location，交给同一内部构造逻辑。新增测试用同一图分别建立 CFG/IR，比较 idom、children、frontier。

### 14.2 表达式 key

```cpp
struct ExpressionKey {
    IROp op{};
    ValueId lhs{};
    std::optional<ValueId> rhs;
    std::optional<std::int32_t> immediate;
    auto operator<=>(const ExpressionKey&) const = default;
};
```

第一版可进入 GVN：Constant、Copy、Add/Sub/Mul/SDiv/SRem、比较、LogicalNot。排除 Phi、Param、LoadGlobal、StoreGlobal、Call。

对交换律操作规范化 operands：

```text
Add, Mul, ICmpEQ, ICmpNE
```

LT/GT/LE/GE 不直接排序；可由 InstCombine 先做方向规范化，但非必须。

### 14.3 支配树遍历

从 entry 沿 dominator tree DFS。表达式表必须具有作用域：

- 进入 block：记录本块新插入的 key；
- 访问指令：key 已存在时，表中 value 必然来自支配块，可 RAUW；
- 递归 children；
- 离开 block：撤销本块插入。

可以复制父 map 给 child，工程简单但成本更高；当前项目规模允许。推荐 scoped undo，复杂度和意图更清楚。

### 14.4 value representative

维护 union/representative 或直接在访问每条指令前解析 operands 的替代链。必须做路径压缩或在 pass 后统一 RAUW，避免：

```text
%3 -> %2 -> %1 -> %0
```

形成长链。替换后 DCE 删除冗余定义。

### 14.5 GVN 测试

- 同块冗余表达式；
- entry 表达式在 dominated child 中复用；
- sibling blocks 不能互相复用；
- diamond 两臂相同表达式不能直接让一臂使用另一臂值；
- merge block 可复用 entry 的表达式；
- commutative 合并；
- non-commutative 不合并；
- 两个 LoadGlobal 不合并；
- call 前后 global load 不合并；
- 相同 SDiv 在严格支配位置可合并，但不能移动到新执行路径；
- GVN + DCE 后 verifier 通过且幂等。

### 14.6 本步骤完成定义

- GVN 只使用支配当前点的 value；
- sibling/diamond 负向测试通过；
- memory/call 被排除；
- 冗余表达式在 IR 和汇编中减少；
- 全量 correctness suite 通过。

---

## 15. 执行步骤 8：循环分析与 LICM

### 15.1 Natural loop

对每条 CFG edge `tail -> header`，若 `header dominates tail`，它是 backedge。natural loop block 集合：

```text
初始 {header, tail}
从 tail 逆向遍历 predecessors
直到所有能到 tail 且不越过 header 的块加入
```

多个 backedge 指向同一 header 时合并 block 集合。记录：

```cpp
struct Loop {
    BlockId header{};
    std::vector<BlockId> blocks;
    std::vector<BlockId> latches;
    std::optional<BlockId> preheader;
    std::optional<std::size_t> parent;
    unsigned depth = 1;
};
```

所有 vector 排序，loop 按 header 排序，保证 dump 和测试稳定。

### 15.2 Preheader

header 的 outside predecessors 是不在 loop.blocks 中的前驱。

- 恰有一个 outside predecessor P，且 P 只有 header 一个 successor：P 已是 preheader；
- 恰有一个 outside predecessor但 P 有多个 successor：调用 `splitEdge(IRFunction, P, header)` 创建 preheader；
- 多个 outside predecessors：第一版可新建 preheader 并把外部边汇总，但必须重写 header Phi；若工期紧，先跳过该 loop 并在统计中报告 `skipped_no_preheader`。

CFGBuilder 生成的普通 while 通常有可处理的单一外部前驱，先覆盖该主路径。

### 15.3 可外提指令

第一版只允许：

```text
Constant, Copy,
Add, Sub, Mul,
ICmpLT/GT/LE/GE/EQ/NE,
LogicalNot
```

条件：

1. `isSafeToSpeculate(inst)`；
2. 所有 operands 定义在循环外，或已被本轮判定 invariant；
3. 不是 Phi/Param；
4. 不读取/写入 global memory；
5. 不是 Call；
6. 不外提 SDiv/SRem；
7. preheader 存在；
8. 移动后 definition 仍支配所有 uses。

因为只外提 safe-to-speculate 指令，即使原 block 不是每次迭代必执行，也不会引入 trap 或副作用。

### 15.4 移动方法

按循环内 RPO 反复扫描到无新 invariant：

1. 收集待移动 InstId，不在遍历 vector 时直接 erase；
2. 保持依赖拓扑顺序；
3. 从原块移除；
4. 插入 preheader terminator 前，即 `instructions` 尾部；
5. compact InstId；
6. verify。

ValueId 可保持到统一 compact；移动定义不创建新 Value。

### 15.5 LICM 测试

- `while` 内 `a+b`，a/b 在循环外：外提；
- invariant chain 两条均按依赖顺序外提；
- 依赖 induction Phi 的表达式不外提；
- loop 内 LoadGlobal 不外提；
- loop 内 StoreGlobal/Call 不外提；
- SDiv/SRem 不外提；
- nested loops 的表达式外提到正确层级；
- break/continue CFG 不破坏；
- 无 preheader 时安全跳过；
- LICM 后 Phi、dominance、e2e 全正确。

### 15.6 本步骤完成定义

- natural loop/backedge/depth 分析有专项测试；
- 普通 while 可识别 preheader；
- 只外提白名单纯指令；
- 不跨内存/call/trap 边界；
- `loop_invariant.tc` 的循环体静态指令减少；
- 1000 seed 差分通过。

---

## 16. 执行步骤 9：比较-分支融合

### 16.1 识别条件

在优化模式的 instruction selection 前建立 IR use count。若 BranchValue.condition 的 definition：

- 位于同一 block；
- opcode 为六种 ICmp；
- result 只有该 branch 一个 use；
- 不被 Phi/return/其他 instruction 使用；

则跳过布尔结果物化，直接生成融合 MIR branch。

### 16.2 MIR opcode

建议新增：

```cpp
BEQ, BNE, BLT, BGE
```

统一 operands：

```text
uses[0] lhs register
uses[1] rhs register
uses[2] true MachineBlockRef
uses[3] false MachineBlockRef
```

映射：

```text
EQ -> beq lhs, rhs
NE -> bne lhs, rhs
LT -> blt lhs, rhs
GE -> bge lhs, rhs
GT -> blt rhs, lhs
LE -> bge rhs, lhs
```

AsmPrinter 仍输出一条条件跳转到 true，再一条 `j false`。后续块布局优化再消除多余 jump，本阶段不做布局。

### 16.3 Verifier 与测试

- 新 opcode 必须恰有 4 operands；
- 前两项在 PreRA 是 VReg/PhysReg，PostRA 后只能 PhysReg；
- 后两项必须是合法 block ref，且与 successor 列表一致；
- 比较有额外 use 时不得融合；
- 六种比较全部有正向汇编测试；
- normal 与 opt 的运行结果一致。

### 16.4 本步骤完成定义

- 六种比较可融合；
- 单用途规则严格；
- MIR verifier 三阶段通过；
- 专项汇编少一次比较结果物化。

---

## 17. 执行步骤 10：Copy-aware RA 与常量重物化

### 17.1 先增加选项，不改变默认行为

```cpp
struct RegAllocOptions {
    bool enableCopyHints = false;
    bool enableRematerialization = false;
};

explicit LinearScanRegisterAllocator(RegAllocOptions options = {});
```

Driver 只在 `-opt` 时打开。这样可直接比较 normal/opt，出现问题也能单独关闭某项定位。

### 17.2 Copy hint

扫描 PreRA MIR 的：

```text
COPY vdst, vsrc
```

记录双向 preference。分配 current interval 时：

1. 若 hint 对端已经分配物理寄存器；
2. 该寄存器属于 current 可用 pool；
3. 当前点不与仍活跃 interval 冲突；
4. 跨 call 限制仍满足；

优先选择该寄存器。否则退回原线性扫描，不强制合并。

第一版不要直接 union 两个 VReg；不完整 interference 判断会把同时活跃值错误合并。分配偏好收益稍小但安全。

### 17.3 常量 rematerialization

PreRA 扫描：若 VReg 的唯一 def 是 `LI immediate`，标记：

```cpp
struct Rematerialization {
    MOpcode opcode = MOpcode::LI;
    Immediate immediate{};
};
```

当该 VReg 被选择 spill：

- 不分配 StackSlot；
- 删除/跳过原 LI 对 spill slot 的 store；
- 每个 use 前在 scratch reg 中重新发出 `LI immediate`；
- 该 instruction 的 operand 改成 scratch reg；
- 若同一 instruction 多次使用同一 VReg，只 rematerialize 一次。

第一版只支持 LI，不支持 LA/global address，不跨定义不唯一的 COPY 链推导。

### 17.4 scratch 限制

当前 spill rewrite 轮换使用 `t5/t6`。必须保留断言：单条待重写 instruction 同时需要的不同 spilled/remat 输入数不能超过可用 scratch 数。现有二元运算最多两个，call 参数已在 isel 中拆为独立 COPY/SW。若未来 opcode 超过限制，应明确报错或预留额外 scratch，不能覆盖前一个 operand。

### 17.5 测试

- COPY 两端无冲突时获得相同物理寄存器，COPY 被删；
- COPY 两端冲突时不强制合并；
- 跨 call VReg 不因 hint 分到 caller-saved；
- 高压常量用例触发 rematerialization，spillSlotCount 小于关闭时；
- 常量 use 前出现 LI 而非 LW；
- 非常量 spill 行为不变；
- 大 frame、many args、recursive、parallel copy cycle 全部回归。

### 17.6 本步骤完成定义

- normal 模式保持原 RA 行为；
- opt 模式 copy 数或 spill load/store 可测下降；
- ABI、caller/callee-saved 规则不变；
- PostRA/AfterFrameLowering verifier 全通过。

---

## 18. 执行步骤 11：测试体系升级

### 18.1 优化单测必须以手写 IR 为主

源程序经过 CFGBuilder/SSA 后形状可能随前端清理变化。每个 pass 的精确算法测试应使用 `ir_test_builder.hpp` 手工构图并调用 verifier；源程序测试负责端到端语义。

建议 builder 支持：

```cpp
FunctionBuilder fn(returnType);
auto b0 = fn.block();
auto c1 = fn.constant(b0, 1);
auto x  = fn.binary(b0, IROp::Add, c1, c1);
fn.jump(b0, b1);
fn.branch(...);
fn.phi(...);
fn.ret(...);
auto ir = fn.finish(); // 自动建边、压 ID
```

### 18.2 CTest 目标

在 `tests/CMakeLists.txt` 加入：

```text
ir_utils_tests
local_dag_tests
instcombine_tests
sccp_tests
simplify_cfg_tests
dce_tests
gvn_tests
loop_tests
licm_tests
backend_opt_tests
```

不要把所有断言塞入一个 `optimizer_tests`，否则失败定位困难。

### 18.3 端到端专项用例

每个 `.tc` 同时跑 normal 与 opt：

| 用例 | 目的 |
|---|---|
| `opt_constant_branch.tc` | SCCP 删除分支，Phi 正确 |
| `opt_dead_code.tc` | DCE 删除纯 dead chain，保留 call/store |
| `opt_redundant_expr.tc` | LocalDAG/GVN 消除同块和支配块冗余 |
| `opt_global_barrier.tc` | call/store 两侧 load 不错误合并 |
| `opt_loop_invariant.tc` | LICM 不改变循环结果 |
| `short_circuit*.tc` | CFG 简化后短路副作用仍正确 |
| `phi_loop.tc` | SCCP/SimplifyCFG 不破坏 loop Phi |
| `parallel_copy_cycle.tc` | 优化后 Phi lowering 仍正确 |
| `spill_and_many_args.tc` | 后端优化不破坏 ABI/spill |

### 18.4 结构断言

不能只比较退出码。专项测试还应断言：

- constant branch 后 IR 无 `branch`；
- dead code 指令数下降；
- repeated expression 的目标 opcode 数下降；
- loop invariant instruction 位于 preheader，不在 loop body；
- compare branch 的 opt MIR/assembly 出现 `beq/bne/blt/bge`；
- rematerialization 用例的 spill `lw/sw` 数下降；
- `--dump-ir-after-each` 中每个 pass dump 可重复。

### 18.5 随机差分升级

现有生成器主要生成一个固定形状。第三阶段增加可控变化：

- 常量/参数条件；
- 重复纯表达式；
- dead expression statement；
- 两层 if；
- 1～2 层有界循环；
- global read/write；
- call 位于表达式之间；
- 不同寄存器压力。

仍需约束：循环终止、除数非零、无参考 C 未定义行为、递归有界。失败时继续保存 seed、输入、normal/opt/GCC assembly 和三个退出码。

### 18.6 Pipeline 幂等性测试

对同一 IR：

```text
runOptimizationPipeline(ir)
dump1 = printIR(ir)
runOptimizationPipeline(ir)
dump2 = printIR(ir)
assert dump1 == dump2
```

这能快速发现 pass 顺序不稳定、ID compaction 不确定或错误 changed 报告。

### 18.7 本步骤完成定义

- 每个 pass 有独立 CTest；
- 每个核心优化有结构断言；
- normal/opt 共用全部 e2e；
- 1000 seed 扩展随机差分通过；
- pipeline 幂等且 dump 确定。

---

## 19. 执行步骤 12：性能测量与文档更新

### 19.1 benchmark 输出补充

在现有 `text_bytes/median_ms/vs_gcc_o2` 基础上增加静态计数：

```text
ir_inst
asm_inst
branches
loads
stores
calls
frame_size（可从汇编序言或 pass stats 得到）
```

记录：编译器提交、构建类型、WSL/工具链版本、iterations、samples。wall time 仍取中位数；至少 7 个 sample，比较时在同一机器同一次运行完成。

### 19.2 第三阶段专项 benchmark

- `constant_branch.tc`：主要观察 blocks/branches/text；
- `redundant_expr.tc`：主要观察 arithmetic inst/text；
- `loop_invariant.tc`：主要观察循环体静态指令和 wall time；
- `stateful_loop.tc`：观察真实循环与全局状态，不允许错误 hoist load；
- `spill_pressure.tc`：观察 load/store/frame 和 rematerialization。

### 19.3 收益判断

每个 pass 的报告至少包含：

```text
pass name
输入用例
IR 指令 before/after
block before/after
汇编指令 before/after
text bytes before/after
median time normal/opt
correctness suite 结果
```

不要求所有程序都变快，但必须满足：

- 专项目标结构确实改善；
- correctness 0 回归；
- benchmark 几何平均时间无超过 5% 的稳定回退；
- 若 text 或时间回退，有基于汇编/寄存器压力的解释。

### 19.4 README 更新

明确写出：

- normal pipeline；
- `-opt` pipeline；
- `--dump-ir-before-opt/--dump-ir/--dump-ir-after-each` 区别；
- `--print-pass-stats` 输出位置；
- Windows/WSL 测试命令；
- 差分和 benchmark 命令；
- 性能数据只在相同环境比较。

### 19.5 本步骤完成定义

- benchmark 能区分 normal/opt 的结构；
- 至少三个专项用例有明确改善；
- 文档能复现数据；
- README 与实际 CLI 一致。

---

## 20. 推荐的最终流水线代码形态

`main.cpp` 不应知道每个 pass 的内部细节，只负责阶段接线：

```cpp
auto ir = toyc::SSABuilder(semantic).build(cfg);
if (verifyIntermediate) toyc::verifyIR(ir, semantic);

toyc::OptimizationOptions opt;
opt.enabled = options.optimize;
opt.verifyEach = verifyIntermediate;
opt.dumpBefore = options.dumpIrBeforeOpt;
opt.dumpAfterEach = options.dumpIrAfterEach;
opt.printStats = options.printPassStats;
toyc::runOptimizationPipeline(ir, semantic, opt, std::cerr);

if (options.dumpIr) toyc::printIR(std::cerr, ir, semantic);

toyc::ISelOptions iselOptions;
iselOptions.fuseCompareBranches = options.optimize;
auto machine = toyc::InstructionSelector(semantic, iselOptions).lower(ir);
toyc::resolveParallelCopies(machine);

for (auto& function : machine.functions) {
    toyc::RegAllocOptions raOptions;
    raOptions.enableCopyHints = options.optimize;
    raOptions.enableRematerialization = options.optimize;
    toyc::LinearScanRegisterAllocator(raOptions).run(function);
    // verify + frame lowering 保持现有顺序
}
```

`pipeline.cpp` 内部大致为：

```cpp
runMandatoryCanonicalization(module);

if (!options.enabled) {
    runDCE(module, Conservative);
    return;
}

runLocalDAG(module);
runInstCombine(module);

for (unsigned i = 0; i < options.maxFixpointIterations; ++i) {
    bool changed = false;
    changed |= runSCCP(module).changed;
    changed |= runSimplifyCFG(module).changed;
    changed |= runInstCombine(module).changed;
    changed |= runDCE(module, Conservative).changed;
    if (!changed) break;
}

runGVN(module);
runDCE(module, Conservative);
runLICM(module);
runGVN(module);
runDCE(module, Conservative);
runSimplifyCFG(module);
canonicalize(module);
```

runner 负责每次调用后的 verify/dump/stats，pass 本身不打印日志。

---

## 21. 分步验收命令

### 21.1 Windows 构建与单测

```powershell
cmake -S . -B build-stage3-win -DBUILD_TESTING=ON
cmake --build build-stage3-win --config Debug -j 2
ctest --test-dir build-stage3-win -C Debug --output-on-failure
cmake --build build-stage3-win --config Release -j 2
ctest --test-dir build-stage3-win -C Release --output-on-failure
```

### 21.2 WSL Release 与端到端

```sh
cmake --preset wsl-release
cmake --build --preset wsl-release -j2
ctest --preset wsl-release
```

### 21.3 查看优化前后 IR

```sh
./build-wsl/compiler \
  --dump-ir-before-opt --dump-ir --print-pass-stats \
  -opt < tests/cases/e2e/opt_redundant_expr.tc \
  > /tmp/opt_redundant_expr.s \
  2> /tmp/opt_redundant_expr.log

riscv64-linux-gnu-gcc \
  -march=rv32im -mabi=ilp32 -nostdlib -static -no-pie \
  -Wl,--no-relax -Wl,-e,_start \
  tests/runtime/rv32_linux_start.S \
  /tmp/opt_redundant_expr.s -o /tmp/opt_redundant_expr.out
qemu-riscv32 /tmp/opt_redundant_expr.out
```

### 21.4 逐 pass verifier

```sh
./build-wsl/compiler -opt --verify-each --dump-ir-after-each \
  < tests/cases/e2e/opt_constant_branch.tc \
  > /tmp/verified.s 2> /tmp/verified.log
```

检查 stdout：

```sh
riscv64-linux-gnu-gcc -c -march=rv32im -mabi=ilp32 \
  /tmp/verified.s -o /tmp/verified.o
```

### 21.5 随机差分

```sh
./scripts/differential_test.sh \
  --compiler ./build-wsl/compiler --seed-start 0 --count 1000

./scripts/differential_test.sh \
  --compiler ./build-wsl/compiler --seed-start 1000 --count 1000
```

建议每个核心 pass 合并前至少 1000，阶段完成前累计至少 5000 个不重复 seed。

### 21.6 性能与结构

```sh
TOYC_BENCH_ITERATIONS=500 TOYC_BENCH_SAMPLES=9 \
  ./scripts/benchmark.sh \
  ./build-wsl/compiler tests/cases/bench/*.tc
```

专项结构可用：

```sh
grep -E '^[[:space:]]+(add|sub|mul|div|rem|slt|xor|bnez|beq|bne|blt|bge|lw|sw|call)' \
  /tmp/opt_redundant_expr.s
```

最终统计应由脚本完成，grep 只用于开发定位。

### 21.7 仓库卫生

```sh
git status --short
git diff --check
git ls-files scripts tests/cases | sort
```

确认没有把 build 目录、临时汇编、差分失败产物或本机绝对路径提交。

---

## 22. 第三阶段总完成定义（Definition of Done）

只有以下门禁全部满足，第三阶段才算完成。

### 22.1 构建与接口门禁

- Windows Debug/Release 从新目录构建成功；
- WSL Release 构建成功；
- C++20 新增代码在现有警告级别下无警告；
- stdin/stdout 接口不变；
- `-opt` 真实运行优化；
- `--dump-ir-before-opt`、`--dump-ir`、`--dump-ir-after-each` 可用；
- `--print-pass-stats` 可用且只写 stderr；
- 未知选项仍明确报错。

### 22.2 IR 工具与 verifier 门禁

- IR use list 覆盖普通、Phi、branch、return use；
- CFG mutation 后 predecessor/successor/Phi 一致；
- block/value/instruction ID 始终可压成连续编号；
- canonicalize 幂等；
- verifier 能拒绝不可达块、坏 Param、坏 Phi、新字段错误；
- 每个 pass 后 `--verify-each` 通过。

### 22.3 优化 pass 门禁

- LocalDAG 能消除块内 CSE；
- InstCombine 实现已列安全规则；
- SCCP 使用 executable edge 正确处理 Phi；
- SimplifyCFG 能折叠分支、删不可达、穿透/合并安全块；
- DCE 保留 call/store/terminator 依赖和保守 mayTrap；
- GVN 只复用支配当前点的纯表达式；
- LoopAnalysis 能识别普通和嵌套 natural loop；
- LICM 只外提 safe-to-speculate 白名单；
- 所有 pass 单独运行和整个 pipeline 都幂等。

### 22.4 内存和副作用门禁

- LoadGlobal 不进入第一版 GVN；
- call 前后的 global load 不合并；
- StoreGlobal 不删除、不重排；
- 未使用 call 仍保留；
- 短路 RHS call 不因 CFG 优化变为无条件执行；
- SDiv/SRem 不被 LICM 外提，非法常量情况不折叠。

### 22.5 后端优化门禁

- 单用途 compare-branch 可融合；
- 比较有其他 use 时保留布尔结果；
- copy hint 不覆盖活跃冲突和跨 call 约束；
- LI rematerialization 不分配无用 spill slot；
- PostRA 无 VReg；
- AfterFrameLowering 无 VReg/StackSlot/ParallelCopy；
- 全部 ABI 专项用例无回归。

### 22.6 正确性门禁

- 全部 C++/CTest 100% 通过；
- normal 与 opt 均通过所有 e2e；
- 至少 5000 个累计随机 seed 与 GCC 差分 0 mismatch；
- constant/global/call/loop/short-circuit/recursion/many-args/spill 均覆盖；
- 同一输入重复编译的 IR、MIR 和 assembly 确定一致。

### 22.7 工程效果门禁

- `-opt` 不再与 normal 恒等；
- constant-branch 专项的 branch/block 数下降；
- redundant-expression 专项的 IR/assembly 算术指令数下降；
- dead-code 专项的纯死指令被删除且副作用保留；
- loop-invariant 专项的目标计算移出循环体；
- 至少一个压力用例的 COPY 或 spill load/store 下降；
- benchmark 集合无超过 5% 的稳定几何平均回退，或有明确书面原因和后续任务；
- 性能报告包含环境、参数、before/after、GCC `-O2` 和正确性结果。

---

## 23. 推荐提交顺序

每个提交都必须构建并通过当时已有测试：

1. `test: freeze stage-2 baseline and track validation scripts`
2. `ir: add SSA mutation utilities and canonicalization`
3. `ir: strengthen verifier for optimization transforms`
4. `opt: add pass runner, dumps and statistics`
5. `opt: add RV32 constant folding and instcombine`
6. `opt: add block-local DAG value numbering`
7. `opt: add SCCP with executable-edge tracking`
8. `opt: add CFG simplification and trivial-phi cleanup`
9. `opt: add side-effect-aware dead code elimination`
10. `opt: add dominator-scoped global value numbering`
11. `opt: add loop analysis and conservative LICM`
12. `backend: fuse compare branches in optimized mode`
13. `backend: add copy hints and LI rematerialization`
14. `test: expand optimized e2e and random differential cases`
15. `bench: record stage-3 code-quality and runtime results`
16. `docs: document the real optimization pipeline`

不要把 SCCP、CFG mutation、LICM 和 RA 改动压在一个提交里。

---

## 24. 可直接执行的开发检查表

### 基线

- [ ] `scripts/` 已纳入版本控制
- [ ] Windows Debug/Release 基线通过
- [ ] WSL 12/12 基线通过
- [ ] 1000 seed 基线通过
- [ ] baseline benchmark 已保存

### IR 工具

- [ ] use list 覆盖四类 use
- [ ] RAUW 覆盖 Phi/terminator
- [ ] IR CFG 可重建
- [ ] unreachable 可删除
- [ ] trivial Phi 可递归清理
- [ ] block/value/inst ID 可压紧
- [ ] canonicalize 幂等
- [ ] verifier 已增强

### Pass 基础

- [ ] `OptimizationOptions` 已建立
- [ ] pass 顺序固定
- [ ] 最多 4 轮 fixpoint
- [ ] `--verify-each` 接到每个 pass
- [ ] before/final/after-each dump 可用
- [ ] stats 只写 stderr
- [ ] `(void)options.optimize` 已删除

### SSA 优化

- [ ] RV32 常量折叠共用实现
- [ ] InstCombine 正/负向规则齐全
- [ ] LocalDAG 不跨 block/memory
- [ ] SCCP 跟踪 executable edge
- [ ] SCCP 正确处理 Phi
- [ ] SimplifyCFG 正确维护 Phi
- [ ] DCE 保留副作用和 terminator 链
- [ ] GVN 使用 dominator scope
- [ ] GVN 排除 global load/call/store
- [ ] pipeline 幂等

### 循环

- [ ] backedge/natural loop 可识别
- [ ] nested loop/depth 可识别
- [ ] preheader 可识别或安全创建
- [ ] LICM 只有白名单
- [ ] SDiv/SRem/load/store/call 不外提
- [ ] break/continue/loop Phi 无回归

### 后端

- [ ] 六种 compare-branch 映射正确
- [ ] 融合只发生在单用途条件
- [ ] 新 MIR opcode verifier 完整
- [ ] copy hint 保守
- [ ] 跨 call 限制不变
- [ ] LI rematerialization 正确
- [ ] spill scratch 数有断言
- [ ] ABI 全量回归通过

### 最终验证

- [ ] Windows 全测 100%
- [ ] WSL 全测 100%
- [ ] normal e2e 100%
- [ ] opt e2e 100%
- [ ] 累计 5000 seed 0 mismatch
- [ ] dump/assembly 可重复
- [ ] 三个以上专项 benchmark 结构改善
- [ ] benchmark 无无法解释的稳定回退
- [ ] README 与真实命令一致
- [ ] `git diff --check` 通过
- [ ] 工作区无意外 build/临时产物

---

## 25. 常见失败点与定位顺序

### 25.1 verifier 报 Phi predecessor mismatch

优先检查最近的 CFG pass：

1. terminator target 是否已改；
2. successor 是否从 terminator 重建；
3. target Phi 是否删除旧 edge 输入；
4. 新 edge 是否提供 Phi 输入；
5. predecessor 和 Phi input 是否同序排序。

不要先关闭 verifier。

### 25.2 verifier 报 value lacks definition / use undefined

检查 `compactIR`：

- RAUW 是否覆盖 terminator；
- Phi input 是否遗漏；
- 删除 definition 前是否先替换 uses；
- old-to-new mapping 是否包含所有现存 result；
- 新 entry constant 是否增加 valueCount。

### 25.3 constant branch 正确，循环结果错误

优先检查 SCCP executable edge：

- loop backedge 是否在变为 executable 后重新求值 header Phi；
- Unknown 是否过早当作 0；
- Constant 是否错误地从 Overdefined 退回；
- 删除边后 Phi 是否只剩 taken 输入。

### 25.4 GVN 在 diamond 上出错

通常是表达式表没有按 dominator tree 作用域撤销，导致 sibling block 使用不支配它的 value。打印 idom tree 和 key 来源 block，禁止用普通 CFG DFS 代替 dominator DFS。

### 25.5 global/call 用例错误

检查是否把 LoadGlobal 放入 LocalDAG/GVN，或 LICM 外提了 load。第一版应完全排除 global load；call/store 必须保留且不可重排。

### 25.6 优化后 IR 正确、汇编错误

用以下顺序缩小：

```text
final IR
-> MIR before RA
-> MIR after RA
-> MIR after frame lowering
-> assembly
```

关闭 compare fusion、copy hint、rematerialization 中的单项，确认是中端还是后端优化导致。不要直接改汇编文本。

### 25.7 只有高压力程序错误

重点检查：

- copy hint 是否无视活跃冲突；
- remat use 是否超过 t5/t6 scratch 容量；
- 同一 spilled value 在一条指令中是否重复 load/LI 并互相覆盖；
- crossesCall 值是否仍限制在 callee-saved；
- rematerialized value 是否错误分配了/复用了 spill slot。

### 25.8 性能没提升

依次观察：

1. pass stats 是否真的 changed；
2. final IR 指令/块数是否减少；
3. isel 是否又把简单 IR 扩成多条 MIR；
4. live interval 是否变长、spill 是否增加；
5. frame/load/store 是否增加；
6. QEMU 时间是否只是噪声。

结构没有改善时先修 pass；结构改善但时间不变时再看后端和测量方法。

---

## 26. 第三阶段结束时的预期状态

第三阶段结束后，前端语言能力与第二阶段一致，但代码质量和工程可观察性发生明显变化：

- `-opt` 驱动真实、确定、可验证的 SSA 优化流水线；
- 常量分支和不可达块由 SCCP/SimplifyCFG 删除；
- 块内和跨支配块冗余表达式由 LocalDAG/GVN 消除；
- 纯死计算由 DCE 删除，call/store/global 语义保持；
- 普通循环中的安全不变量可移入 preheader；
- 常见 compare-branch 不再强制物化布尔 VReg；
- 寄存器分配能利用 copy hint，spilled 常量可以重物化；
- 每个 pass 都能独立测试、dump、统计和 verify；
- normal/opt/GCC 在大规模确定性差分中结果一致；
- 性能报告能解释 IR 改变如何传递到 MIR、spill、汇编大小和运行时间。

达到这些条件后，第四阶段才适合投入图着色寄存器分配、块布局、调度、内联、归纳变量和循环展开等性能冲刺工作。
