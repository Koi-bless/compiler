# ToyC 编译器第二阶段具体执行文档

> 文档状态：可执行版本 1.0
>
> 基线日期：2026-08-12
>
> 基线提交：`1bf6931 简单完整实现，不包含优化`
>
> 第二阶段主题：规范 SSA IR 与稳健 RISC-V32 后端

## 1. 文档用途

本文是第一阶段完成后的直接实施手册。开发者应当能够仅依据本文完成第二阶段代码改造，不需要重新决定模块边界、数据结构、算法顺序或验收口径。

本文依次回答以下问题：

1. 当前仓库已经实现了什么，真实代码处于什么状态；
2. 第一阶段实际修改了什么，还有哪些基线问题需要先收口；
3. 第二阶段要增加和替换哪些文件、接口和数据结构；
4. Dominator、SSA、Phi 消除、MIR、活跃性、寄存器分配、栈帧如何实现；
5. 每个改造步骤做到什么程度才算完成；
6. 如何用单元测试、端到端测试、随机差分和汇编检查验证结果。

除特别说明外，本文中的文件路径均相对于仓库根目录 `D:/Repo/compiler`。

---

## 2. 第二阶段范围

### 2.1 阶段目标

第二阶段对应《编译系统实践实现指南》中的“里程碑 2：规范 IR 与稳健后端”。完成后，编译流水线应从当前的：

```text
AST
  → 非 SSA TAC/CFG
  → AsmPrinter 直接把每个临时值放入栈槽
  → RV32 汇编
```

演进为：

```text
AST + SemanticResult
  → 规范化非 SSA CFG
  → RemoveUnreachable
  → Dominator / Dominance Frontier
  → SSA Construction
  → SSA Verifier
  → Critical-edge Split + Phi Lowering
  → RISC-V32 MIR Instruction Selection
  → MIR Verifier
  → Liveness
  → Linear-scan Register Allocation + Repeatable Spill Rewrite
  → Frame Lowering
  → Post-RA MIR Verifier
  → AsmPrinter
  → RV32IM/ILP32 汇编
```

本阶段的核心成果不是某个局部优化，而是建立三条可靠的工程边界：

- CFG 只表达非 SSA 控制流和可变虚拟变量；
- SSA IR 表达中端值关系、Phi 和显式全局内存操作；
- MIR 表达 RISC-V 指令、虚拟/物理寄存器、调用破坏和栈对象。

### 2.2 本阶段必须完成

- 规范化当前非 SSA TAC，消除局部变量专用的隐式栈语义；
- 实现可达块清理、逆后序、立即支配者、支配树和支配边界；
- 使用 dominance frontier 插入 Phi，并使用支配树 DFS 完成 SSA rename；
- 实现完整 SSA verifier；
- 拆分 critical edge，正确降低 Phi 和并行复制；
- 建立独立的 RISC-V32 MIR；
- 实现块级活跃性分析；
- 实现确定性的线性扫描寄存器分配和可重复 spill rewrite；
- 正确处理参数、返回值、caller/callee-saved、`ra`、栈对齐、栈上传参、大 frame 和大 offset；
- 把 `AsmPrinter` 限制为 MIR 的纯文本输出器；
- 增加 SSA、MIR、ABI、寄存器压力和随机差分测试；
- 让 `--verify-each`、`--dump-ir`、`--dump-mir` 成为真实可用的调试接口。

### 2.3 本阶段明确不做

以下内容留到第三、第四阶段，不得阻塞本阶段验收：

- 基本块 DAG、局部公共子表达式消除；
- SCCP、GVN、InstCombine、通用 DCE；
- 循环分析、LICM、强度削弱；
- 图着色寄存器分配；
- 指令调度、trace layout、基于 profile 的块布局；
- 函数内联、循环展开、尾调用优化；
- 激进的比较与分支融合。

`RemoveUnreachable`、trivial Phi elimination、复制清理和必要的 CFG 规范化属于保证 IR/后端可用的 canonicalization，不视为提前实现第三阶段优化。

---

## 3. 当前代码分析

### 3.1 当前工程结构

当前提交已包含完整的最小编译闭环：

| 层级 | 主要文件 | 当前职责 |
|---|---|---|
| Driver | `src/main.cpp` | 解析参数、读取 stdin、串联各阶段、向 stdout 输出汇编 |
| Lexer | `include/toyc/frontend/token.hpp`、`lexer.hpp`、`src/frontend/lexer.cpp` | Token 化、关键字、数字、位置与词法诊断 |
| Parser/AST | `ast.hpp`、`parser.hpp/.cpp`、`ast_printer.cpp` | 递归下降解析、AST 所有权和稳定 dump |
| Semantic | `semantic.hpp/.cpp` | 作用域、符号/函数 ID、类型、常量和返回路径检查 |
| 非 SSA IR | `ir/tac.hpp`、`ir/cfg.hpp`、`ir/cfg_builder.*` | 三地址指令、基本块、短路控制流、前驱/后继 |
| Verifier | `ir/verifier.*` | CFG 边、terminator、临时值 must-defined 检查 |
| Backend | `backend/riscv32.hpp`、`asm_printer.*` | 栈式值保存、RV32 指令文本、调用和全局对象 |
| Tests | `tests/*.cpp`、`tests/cases/e2e` | 五组单元测试和 16 组端到端输入 |

当前总代码规模约为：头文件 538 行、实现文件 1547 行、C++ 测试 188 行。规模仍适合使用清晰的迭代算法，不需要引入 LLVM 或复杂通用框架。

### 3.2 当前 CFG/TAC 的真实语义

`include/toyc/ir/tac.hpp` 当前定义了：

```cpp
enum class TacOp {
    LoadImm, ReadVar, WriteVar,
    Add, Sub, Mul, Div, Rem,
    CmpLT, CmpGT, CmpLE, CmpGE, CmpEQ, CmpNE,
    LogicalNot, Call
};
```

每条 `TacInst` 使用可选 `dst`、`inputs`、`immediate`、`symbol` 和 `callee`。`cfg.hpp` 使用 `BasicBlock`、`Jump`、`Branch`、`Return` 和 `Unreachable` 表达控制流。

该表示能够正确生成代码，但还不是 SSA，原因包括：

- `ReadVar/WriteVar` 保留了可变局部变量语义；
- 逻辑表达式在值上下文中，会在 true/false 两个块中定义同一个 `TempId`；
- `TempId` 只表示临时名称，不满足“每个值只有一个定义”；
- 没有 Phi、`ValueId`、def-use 和支配关系；
- 全局变量与局部变量通过同一 `ReadVar/WriteVar` 区分，优化和后端必须反复查询 `SemanticResult`。

短路控制流已经正确实现，这是第二阶段应保留的基础。`emitCondition` 会为 `&&` 和 `||` 建立独立 RHS 块，不得把它回退成普通按位/算术指令。

### 3.3 当前 verifier 的能力与缺口

现有 `src/ir/verifier.cpp` 已检查：

- entry 和块 ID 合法；
- 每个可达块有 terminator；
- terminator 与 successor 列表一致；
- predecessor/successor 双向一致且无重复；
- return 是否与函数返回类型匹配；
- 基于 must-defined 交汇的临时值“先定义后使用”。

现有 verifier 不检查：

- 单一定义；
- 定义是否支配普通使用；
- Phi 是否位于块首；
- Phi 输入是否与前驱一一对应；
- Phi 的边使用是否被定义支配；
- `void call` 是否错误地产生结果；
- 指令操作数数量和操作码约束；
- MIR 的虚拟寄存器、物理寄存器、frame index 和调用破坏。

因此不能只扩展当前函数中的几条 `if`。第二阶段应保留 CFG verifier，并新增独立的 SSA verifier 和 MIR verifier。

### 3.4 当前后端的真实行为

`src/backend/asm_printer.cpp` 当前同时承担了五种职责：

1. 扫描函数并规划 frame；
2. 给每个局部变量和每个 `TempId` 分配 4 字节栈槽；
3. 进行指令选择；
4. 实现调用约定和大 offset 物化；
5. 打印汇编文本。

所有计算主要借助 `t0`、`t1`、`t2`，几乎每个 TAC 结果都会 `sw`，每次使用都会 `lw`。该实现正确但会产生大量内存访问，也无法承载后续中端优化。

当前 ABI 已覆盖：

- `a0-a7` 前 8 个整数参数；
- 第 9 个及以后参数放在调用者栈上传参区；
- 返回值使用 `a0`；
- frame 按 16 字节对齐；
- 大 frame 和大 offset 使用临时寄存器物化；
- 所有 return 跳向共享 epilogue；
- 全局对象输出到 `.data` 或 `.bss`。

当前 ABI/后端仍有以下设计缺口：

- 每个函数都保存/恢复 `ra`，叶函数也不例外；
- 没有 callee-saved 寄存器分配和按需保存；
- 没有 call clobber 模型；
- 没有 MIR，打印器内部的改写无法单独验证；
- 没有活跃性、寄存器分配、spill 代价和确定性分配结果；
- `AsmPrinter` 不是纯 printer。

### 3.5 当前 Driver 行为

`src/main.cpp` 接受 `-opt` 和 `--verify-each`，但当前只是忽略这两个参数；`CompilerOptions` 中甚至没有对应字段。现有 `--dump-tokens`、`--dump-ast`、`--dump-cfg` 可用，dump 写入 `stderr`，最终汇编写入 `stdout`。

第二阶段必须让 `--verify-each` 真正控制阶段后校验，并增加：

```text
--dump-ir             SSA IR，写 stderr
--dump-mir            寄存器分配前 MIR，写 stderr
--dump-mir-after-ra   frame lowering 前的物理寄存器 MIR，写 stderr
```

### 3.6 2026-08-12 实测基线

在当前工作区实测结果如下：

| 检查 | 结果 |
|---|---|
| Windows Release CTest | 5/5 通过 |
| WSL Release CTest | 6/6 通过，包含 `e2e_tests` |
| `stateful_loop.tc` 普通输出与 `-opt` 输出 | 完全相同 |
| `stateful_loop.tc` 汇编文本长度 | 1479 字符（按 PowerShell 拼接口径） |
| `stateful_loop.tc` 静态 `lw` 数量 | 25 |
| `stateful_loop.tc` 静态 `sw` 数量 | 23 |
| `work` frame | 80 字节 |

Windows 使用 Visual Studio 多配置生成器，因此必须使用：

```powershell
ctest --test-dir build-release -C Release --output-on-failure
```

省略 `-C Release` 会显示 `Not Run` 和“Missing -C”，这不是代码测试失败。

### 3.7 当前仓库卫生问题

`git status --short` 当前显示：

```text
?? scripts/
```

即 `README.md` 和 `tests/CMakeLists.txt` 已引用的以下脚本尚未纳入提交：

- `scripts/run_e2e.sh`
- `scripts/run_e2e.ps1`
- `scripts/check_wsl_env.sh`
- `scripts/benchmark.sh`

当前 WSL 端到端测试能通过，是因为这些文件存在于本地工作区；从提交 `1bf6931` 全新 clone 后脚本并不存在。第二阶段开始前必须先提交这些脚本，否则“干净 clone 可验收”不成立。

---

## 4. 第一阶段实际修改了什么

相对于初始提交，第一阶段提交 `1bf6931` 的实质变更如下：

| 类别 | 已完成变更 |
|---|---|
| 工程 | 建立 CMake/C++20、`toyc_lib`、`compiler`、CTest 和 WSL preset |
| 目录 | 将根目录 Lexer 迁入 `include/toyc/frontend` 和 `src/frontend` |
| Lexer | 修复并完善 Token、位置、整数、操作符和词法错误处理 |
| AST/Parser | 新增 AST、递归下降 Parser 和 AST dump |
| Semantic | 新增符号表、作用域、稳定 ID、常量值、类型与返回路径分析 |
| CFG/TAC | 新增显式基本块、前驱/后继、terminator、短路 lowering 和 CFG dump |
| Verifier | 新增 CFG 结构和 must-defined 检查 |
| Backend | 新增直接从 CFG 输出 RV32IM 的栈式后端，覆盖调用、全局、大 frame |
| Driver | 新增 stdin/stdout 编译接口和调试参数 |
| Tests | 新增五个 C++ 测试程序、16 组 e2e case、benchmark case 和 RV32 启动代码 |
| 文档 | 新增 README、实现指南和第一阶段执行文档 |

第一阶段成功建立了正确性闭环。第二阶段不是重写前端，而是替换 `CFGBuilder` 之后的中后端，并保持所有既有合法输入行为不变。

---

## 5. 第二阶段完成后将修改什么

### 5.1 现有文件修改表

| 文件 | 第二阶段修改 |
|---|---|
| `include/toyc/support/ids.hpp` | 增加 `InstId`、`ValueId`、`VRegId`、`MBlockId`、`FrameIndex` 的强类型包装或独立别名 |
| `include/toyc/ir/tac.hpp` | 将局部读写规范为 `Param/Copy`，将全局读写改为显式 `LoadGlobal/StoreGlobal` |
| `include/toyc/ir/cfg.hpp` | 将当前结构明确命名为 `CFGModule/CFGFunction/CFGBlock`，避免与 SSA IR 混淆 |
| `include/toyc/ir/cfg_builder.hpp` | 增加 `SymbolId → TempId` 的局部变量映射和参数定义 |
| `src/ir/cfg_builder.cpp` | 局部变量直接使用可变 Temp 名；全局变量保留显式内存操作 |
| `include/toyc/ir/verifier.hpp` | 拆成 `verifyCFG`、`verifyIR`，不再使用含糊的 `verify` |
| `src/ir/verifier.cpp` | 保留 CFG 校验，并增加或迁出 SSA 校验 |
| `include/toyc/backend/riscv32.hpp` | 增加物理寄存器、寄存器集合、caller/callee-saved 信息 |
| `include/toyc/backend/asm_printer.hpp` | 输入改为 `MachineModule`，不再依赖 AST/CFG/Semantic 决策 |
| `src/backend/asm_printer.cpp` | 只格式化已经完成 frame lowering 的 MIR |
| `src/main.cpp` | 串联 CFG、SSA、MIR、RA、Frame；实现新 dump 和 verify 参数 |
| `tests/test_support.hpp` | 支持分别构建 CFG、SSA IR、pre-RA MIR 和 post-RA MIR |
| `tests/CMakeLists.txt` | 注册第二阶段新增测试 |
| `scripts/run_e2e.sh` | 同时测试普通模式和 `-opt` |
| `scripts/benchmark.sh` | 继续作为性能记录，不作为不稳定的单元测试门禁 |
| `CMakeLists.txt` | 加入所有新中端、后端源文件 |

### 5.2 新增文件表

推荐严格按以下结构新增文件：

```text
include/toyc/ir/
├── cfg_utils.hpp
├── dominator.hpp
├── ir.hpp
├── ir_printer.hpp
└── ssa_builder.hpp

src/ir/
├── cfg_utils.cpp
├── dominator.cpp
├── ir_printer.cpp
└── ssa_builder.cpp

include/toyc/backend/
├── mir.hpp
├── mir_printer.hpp
├── mir_verifier.hpp
├── phi_lowering.hpp
├── isel.hpp
├── liveness.hpp
├── regalloc.hpp
└── frame.hpp

src/backend/
├── mir_printer.cpp
├── mir_verifier.cpp
├── phi_lowering.cpp
├── isel.cpp
├── liveness.cpp
├── regalloc.cpp
└── frame.cpp

tests/
├── dominator_tests.cpp
├── ssa_tests.cpp
├── mir_tests.cpp
├── liveness_tests.cpp
├── regalloc_tests.cpp
└── abi_tests.cpp

scripts/
├── generate_random_cases.py
└── differential_test.sh
```

不要建立一个同时包含 dominance、SSA、优化、MIR 和寄存器分配的巨型 `optimizer.cpp`。每个算法必须可以由手工构造的输入单独测试。

---

## 6. 第二阶段固定设计决策

### 6.1 三层 IR 不混用

本阶段固定使用以下命名：

| 层 | 类型名 | 值标识 | 是否允许重复定义 | 是否允许 Phi |
|---|---|---|---|---|
| 非 SSA CFG | `CFGModule/CFGFunction/CFGBlock` | `TempId` | 允许 | 不允许 |
| SSA IR | `IRModule/IRFunction/IRBlock` | `ValueId` | 不允许 | 允许 |
| Machine IR | `MachineModule/MachineFunction/MachineBlock` | `VRegId`/`PhysReg` | VReg 单定义为主，COPY lowering 可重写 | 不允许 |

不允许用同一个 `ModuleIR` 类型在不同阶段“改变含义”。类型边界可以让编译器在 C++ 编译期发现错误的阶段连接。

### 6.2 ID 必须稳定且确定

- Block、Value、VReg 按创建顺序从 0 递增；
- 不使用对象地址作为 ID；
- dump 中按函数表顺序、块 ID、指令顺序输出；
- 前驱和后继在修改后排序并去重，或者严格保持确定的插入顺序；
- hash 容器不得直接决定 dump 顺序；
- 同一输入连续编译两次，CFG/IR/MIR dump 必须字节一致。

为避免 `BlockId`、`ValueId`、`VRegId` 都是 `uint32_t` 时误传，优先使用小型强类型包装：

```cpp
template <class Tag>
struct Id {
    std::uint32_t value{};
    auto operator<=>(const Id&) const = default;
};

using ValueId = Id<struct ValueTag>;
using VRegId = Id<struct VRegTag>;
using MBlockId = Id<struct MBlockTag>;
```

若为了减少第一轮改动继续使用别名，则每个层级的 API 参数名和容器类型必须足够明确，并在阶段完成后再强类型化。

### 6.3 全局内存副作用口径

ToyC 没有指针，但函数调用可以读写全局变量。本阶段固定规则：

- `LoadGlobal` 是内存读取，不跨 `StoreGlobal` 或 `Call` 随意移动；
- `StoreGlobal` 有副作用，不得删除；
- `Call` 无论返回值是否使用都保留；
- 局部变量进入 SSA 后不是内存，不生成 `lw/sw`；
- spill 引入的 `LoadStack/StoreStack` 属于后端栈对象，不回流到 SSA IR。

### 6.4 本阶段寄存器策略

为降低 ABI 和 spill rewrite 的耦合，第一版固定：

- 可分配 caller-saved：`t0-t4`；
- 可分配 callee-saved：`s1-s11`；
- 保留 `t5`、`t6` 给 spill、大立即数和大 offset 展开；
- `a0-a7` 只作为参数/返回固定寄存器，不进入通用分配池；
- `s0` 暂不作 frame pointer，也不进入通用分配池；
- `sp`、`ra`、`zero` 永不分配；
- 跨 call 活跃的区间只能使用 `s1-s11`，否则 spill；
- 非跨 call 区间优先使用 `t0-t4`，再使用 `s1-s11`。

这不是最终最高性能策略，但正确、容易验证，并为后续图着色保留空间。

---

## 7. 执行步骤 0：冻结并修复第一阶段基线

### 7.1 操作内容

1. 将当前 `scripts/` 下四个脚本纳入版本控制；
2. 修改 `scripts/run_e2e.sh`，对每个 `.tc` 分别运行普通模式和 `-opt`；
3. 确保两种模式生成的程序都与 `.expected` 一致；
4. 保存当前 `stateful_loop.tc` 的静态汇编统计作为 before 数据；
5. 从干净构建目录重新配置、构建和测试。

`run_e2e.sh` 的核心循环应变为：

```sh
for mode in normal opt; do
  if [ "$mode" = opt ]; then
    "$compiler" -opt < "$case_file" > "$asm_file"
  else
    "$compiler" < "$case_file" > "$asm_file"
  fi
  # assemble、link、qemu、compare expected
done
```

临时文件名必须包含 mode，避免第二次运行覆盖后无法诊断：

```text
recursive_factorial.normal.s
recursive_factorial.opt.s
```

### 7.2 基线记录

在 `docs` 目录尚未建立的情况下，可以先把数据写入本文件对应提交说明；若后续建立 `docs/perf-baseline.md`，记录：

- git commit；
- C++ 编译器、CMake、交叉 GCC、QEMU 版本；
- benchmark 参数；
- `text` bytes；
- 静态 `lw/sw` 数；
- median runtime；
- 原始输出日志。

### 7.3 本步骤完成定义

- `git status --short` 不再只因缺失测试脚本显示 `?? scripts/`；
- Windows 5/5 单元测试通过；
- WSL 6/6 测试通过；
- e2e 脚本实际覆盖普通和 `-opt` 两种模式；
- 当前基线数据可在后续第二阶段完成时复测。

---

## 8. 执行步骤 1：明确 CFG 与 SSA 的类型边界

### 8.1 重命名当前 CFG 类型

在 `include/toyc/ir/cfg.hpp` 中进行以下重命名：

```text
BasicBlock  → CFGBlock
FunctionIR  → CFGFunction
ModuleIR    → CFGModule
printCfg    → printCFG
verify      → verifyCFG
```

同时修改：

- `CFGBuilder::build` 返回 `CFGModule`；
- `TestPipeline::module` 改名为 `cfg`；
- 当前旧后端临时继续接受 `CFGModule`，保证此提交仍能构建和测试；
- 所有调用点完成机械迁移后再开始新增 SSA 类型。

这一提交只做类型和符号改名，不改变生成汇编。改名前后对同一 e2e 输入生成的汇编应完全一致。

### 8.2 新建 SSA IR 数据结构

新增 `include/toyc/ir/ir.hpp`：

```cpp
enum class IROp {
    Param, Constant, Copy,
    Add, Sub, Mul, SDiv, SRem,
    ICmpLT, ICmpGT, ICmpLE, ICmpGE, ICmpEQ, ICmpNE,
    LogicalNot,
    Phi,
    LoadGlobal, StoreGlobal,
    Call
};

struct PhiInput {
    BlockId predecessor{};
    ValueId value{};
};

struct IRInstruction {
    InstId id{};
    IROp op = IROp::Constant;
    std::optional<ValueId> result;
    std::vector<ValueId> operands;
    std::vector<PhiInput> phiInputs;
    std::optional<std::int32_t> immediate;
    std::optional<SymbolId> global;
    std::optional<FuncId> callee;
    SourceLocation location{};
};

using IRTerminator = std::variant<Jump, BranchValue, ReturnValue, Unreachable>;

struct IRBlock {
    BlockId id{};
    std::vector<IRInstruction> instructions;
    std::optional<IRTerminator> terminator;
    std::vector<BlockId> predecessors;
    std::vector<BlockId> successors;
};

struct IRFunction {
    FuncId function{};
    ValueType returnType = ValueType::Void;
    BlockId entry{};
    std::vector<IRBlock> blocks;
    std::uint32_t valueCount{};
    std::uint32_t instructionCount{};
    SourceLocation location{};
};

struct IRModule {
    std::vector<IRFunction> functions;
};
```

`BranchValue` 使用 `ValueId condition`；`ReturnValue` 使用 `optional<ValueId>`。不要复用仍引用 `TempId` 的 CFG terminator 类型。

### 8.3 IR 指令约束

| IROp | result | operands | 附加字段 |
|---|---:|---:|---|
| `Param` | 必须 | 0 | `immediate=参数序号` |
| `Constant` | 必须 | 0 | `immediate` 必须存在 |
| `Copy` | 必须 | 1 | 无 |
| 二元算术/比较 | 必须 | 2 | 无 |
| `LogicalNot` | 必须 | 1 | 无 |
| `Phi` | 必须 | 0 | `phiInputs` 与前驱一一对应 |
| `LoadGlobal` | 必须 | 0 | `global` 必须存在 |
| `StoreGlobal` | 禁止 | 1 | `global` 必须存在 |
| `Call` | int 函数必须，void 函数禁止 | 参数个数 | `callee` 必须存在 |

### 8.4 本步骤完成定义

- CFG 与 SSA 类型不能隐式互换；
- 所有既有测试继续通过；
- `ir.hpp` 可以被独立 include；
- 同一输入的旧 CFG dump 和汇编在机械重命名前后无变化；
- CMake 中新增的空 IR 模块可以通过所有编译警告。

---

## 9. 执行步骤 2：规范化非 SSA TAC

### 9.1 为什么先规范化

当前 `ReadVar/WriteVar` 同时承载局部变量和全局内存。SSA 只应提升局部变量，不能把全局变量错误地变成寄存器值。因此在 SSA 构造前先把两类语义拆开。

### 9.2 新 TacOp

将 `TacOp` 固定为：

```cpp
enum class TacOp {
    Param, LoadImm, Copy,
    Add, Sub, Mul, Div, Rem,
    CmpLT, CmpGT, CmpLE, CmpGE, CmpEQ, CmpNE,
    LogicalNot,
    LoadGlobal, StoreGlobal,
    Call
};
```

语义如下：

- `Param dst, index`：函数入口定义参数对应的可变 Temp 名；
- `Copy dst, src`：局部声明或赋值；
- `LoadGlobal dst, symbol`：显式全局读取；
- `StoreGlobal symbol, src`：显式全局写入；
- 其余产生值的指令定义 `dst`；
- `StoreGlobal` 和 void `Call` 没有 `dst`。

### 9.3 CFGBuilder 局部变量映射

在 `CFGBuilder` 增加：

```cpp
std::vector<std::optional<TempId>> localTemps_;

TempId localTemp(SymbolId symbol) const;
```

进入函数时：

1. `localTemps_.resize(semantic_.symbols.size())`；
2. 为当前函数每个非常量局部符号调用 `createTemp()`；
3. 将 `SymbolId → TempId` 保存到 `localTemps_`；
4. 在 entry 块开头按参数顺序发出 `Param`。

局部 `NameExpr` 不再产生 `ReadVar`，直接返回该符号对应的 `TempId`。局部声明和赋值发出 `Copy localTemp, valueTemp`。全局读写分别使用 `LoadGlobal/StoreGlobal`。

示例：

```c
int f(int a) {
    int x = a + 1;
    x = x * 2;
    return x;
}
```

规范化 CFG 应近似为：

```text
%0 = param 0          ; a 的可变名称
%2 = imm 1
%3 = add %0, %2
%1 = copy %3         ; x = ...
%4 = imm 2
%5 = mul %1, %4
%1 = copy %5         ; x 再定义
return %1
```

这里 `%1` 重复定义是合法的，因为这仍是非 SSA CFG。SSA 构造会为每个定义生成新的 `ValueId`。

### 9.4 逻辑值上下文

继续保留现有 true/false/merge 控制流，但两条路径对同一个结果 Temp 发出 `Copy`：

```text
bb.true:
  %t = copy 1
  jump bb.merge
bb.false:
  %t = copy 0
  jump bb.merge
bb.merge:
  ... use %t
```

SSA 阶段必须在 merge 插入 Phi。不得在 CFGBuilder 中提前用宿主条件选择结果，也不得无条件计算逻辑 RHS。

### 9.5 CFG verifier 增强

`verifyCFG` 在现有检查基础上增加：

- 操作数数量与 `TacOp` 匹配；
- 产生值的指令必须有 `dst`；
- `StoreGlobal`、void `Call` 不得有 `dst`；
- `Param` 只能位于 entry，参数序号不重复且连续；
- 局部 Temp 的每个 use 在所有可达路径上 must-defined；
- `LoadGlobal/StoreGlobal` 的符号必须为非常量全局变量；
- `Call` 参数个数、返回类型与 `SemanticResult` 一致。

### 9.6 迁移期旧后端适配

为了保证“规范化 CFG”提交本身仍能通过 e2e，临时旧后端按以下方式适配：

- `Param dst,index`：从 `a0-a7` 或 incoming stack 读取后写入 `dst` 的临时栈槽；
- `Copy dst,src`：从 `src` 临时栈槽读取后写入 `dst` 临时栈槽；
- `LoadGlobal/StoreGlobal`：沿用当前 `la + lw/sw`；
- frame 不再为局部 `SymbolId` 单独分配长期栈槽，只需为非 SSA Temp 分配槽；
- 删除或停用当前按 `parameterSymbols` 复制到 symbol slot 的 `copyParameters()` 逻辑，避免参数初始化两次。

该适配只是迁移桥梁。新 MIR 后端接通后必须删除，不允许它成为第二套长期后端。

### 9.7 本步骤完成定义

- CFG 中不再出现 `ReadVar/WriteVar`；
- 局部变量不再隐式要求后端栈槽；
- 全局访问在 CFG dump 中可直接识别；
- 短路、shadow、全局状态、递归和多参数 e2e 全部通过旧后端兼容路径；
- 新增 CFG 测试明确断言局部重复定义和逻辑 merge 的形状。

---

## 10. 执行步骤 3：CFG 工具与支配关系

### 10.1 cfg_utils

新增以下通用 API：

```cpp
std::vector<bool> computeReachable(const CFGFunction& function);
std::vector<BlockId> computeReversePostOrder(const CFGFunction& function);
bool removeUnreachable(CFGFunction& function);
bool removeUnreachable(CFGModule& module);
void rebuildPredecessors(CFGFunction& function);
void replaceSuccessor(CFGFunction& function,
                      BlockId from, BlockId oldTarget, BlockId newTarget);
BlockId splitEdge(IRFunction& function, BlockId from, BlockId to);
```

`removeUnreachable` 必须同时：

- 删除不可达块；
- 重新建立稠密 BlockId；
- 重写 terminator target；
- 重建 predecessors/successors；
- 返回 old-to-new 映射，或在内部一次性完成所有引用重写。

不要一边遍历 vector 一边删除块并保留旧引用。

### 10.2 DominatorInfo 接口

新增 `include/toyc/ir/dominator.hpp`：

```cpp
class DominatorInfo {
public:
    explicit DominatorInfo(const CFGFunction& function);

    BlockId immediateDominator(BlockId block) const;
    bool dominates(BlockId a, BlockId b) const;
    const std::vector<BlockId>& children(BlockId block) const;
    const std::vector<BlockId>& frontier(BlockId block) const;
    const std::vector<BlockId>& reversePostOrder() const;
};
```

### 10.3 立即支配者算法

项目规模较小，固定使用 Cooper-Harvey-Kennedy 迭代算法：

```text
RPO = reverse(postorder DFS(entry))
idom[entry] = entry
repeat
  changed = false
  for b in RPO excluding entry
    newIdom = first predecessor p with known idom
    for remaining predecessor p with known idom
      newIdom = intersect(p, newIdom)
    if idom[b] != newIdom
      idom[b] = newIdom
      changed = true
until !changed
```

`intersect` 使用 RPO 序号沿 idom 链向上移动。运行前必须清理不可达块，或者明确忽略不可达前驱。

### 10.4 支配边界

对每个前驱数至少为 2 的块 `b`：

```text
for p in predecessors(b):
  runner = p
  while runner != idom[b]:
    DF[runner].insert(b)
    runner = idom[runner]
```

所有 frontier 输出按 BlockId 排序，保证 SSA Phi 插入顺序稳定。

### 10.5 必测图形

- 单块函数：entry 支配自身；
- 直线 `entry → a → b`；
- diamond：merge 的 idom 是 entry，then/else 的 DF 含 merge；
- while：header 支配 body，body 的 DF 含 header；
- 嵌套 if/while；
- 含提前 return 的多出口；
- 人工加入不可达块后先清理再分析。

### 10.6 本步骤完成定义

- `dominator_tests` 覆盖上述全部图形；
- `dominates(a,b)` 与显式期望一致；
- 支配树从 entry 可遍历所有可达块且无环；
- dominance frontier 稳定、无重复；
- AddressSanitizer 或 Debug iterator 检查下无悬空引用。

---

## 11. 执行步骤 4：SSA 构造

### 11.1 SSABuilder 接口

```cpp
class SSABuilder {
public:
    explicit SSABuilder(const SemanticResult& semantic)
        : semantic_(semantic) {}

    IRModule build(const CFGModule& cfg);
};
```

SSA 构造不得修改输入 `CFGModule`。这样测试可以同时打印 before CFG 和 after IR，也便于失败时对照。

### 11.2 收集定义块

对于每个 `TempId`，扫描所有块内指令：

```text
definitionBlocks[temp] = { 所有 dst == temp 的 block }
```

`Param`、`LoadImm`、算术、比较、`LogicalNot`、`LoadGlobal`、有返回值的 `Call`、`Copy` 都是定义。

同时对非 SSA Temp 计算一次块级活跃性。规则与后端活跃性相同：先按指令顺序建立每个块的 `use/def`，再迭代：

```text
liveOut[B] = union(liveIn[S])，S 属于 successors(B)
liveIn[B]  = use[B] union (liveOut[B] - def[B])
```

这里的对象是旧 `TempId`，不是 SSA `ValueId`。这一步用于构造 pruned SSA：只在变量确实 live-in 的合流块插入 Phi，避免给循环体内产生但从不跨 backedge 使用的表达式临时值制造无意义 Phi 或空输入。

### 11.3 Phi 插入

对每个 Temp 使用 Cytron worklist：

```text
work = definitionBlocks[temp]
while work not empty:
  x = pop(work)
  for y in DF[x]:
    if temp 属于 liveIn[y] 且 y 尚未为 temp 插入 phi:
      在 y 块首插入 phi(temp)
      if y 不在原 definitionBlocks[temp]:
        push y
```

初始 Phi 暂时保存“它代表哪个旧 TempId”，rename 完成后 Phi result 是新 `ValueId`，输入为 `(predecessor, ValueId)`。

只对实际有定义的 Temp 运行。没有定义的 Temp 若被使用，应由 `verifyCFG` 提前拒绝。使用 live-in 条件后，合法 ToyC CFG 的 rename 栈不应在读取时为空；若为空，说明 CFG must-defined、活跃性或 Phi 插入至少有一处错误，应报告内部错误，不能偷偷生成 `undef` 或常数 0。

### 11.4 支配树 DFS rename

为每个旧 `TempId` 维护 `vector<ValueId>` 栈：

1. 进入块时，先给所有 Phi 分配新 `ValueId` 并压栈；
2. 按顺序处理普通指令：
   - 每个 input 替换为对应 Temp 栈顶；
   - 每个 dst 分配新 `ValueId`，生成 SSA 指令并压栈；
3. terminator 的 condition/return value 使用当前栈顶；
4. 对每个 successor，为该 successor 中每个 Phi 追加当前块对应的栈顶；
5. 递归访问支配树孩子；
6. 离开块时，按本块 push 次数逆序 pop。

不要用一个“当前值 map”直接递归后再复制整个 map；栈方式更容易正确处理循环和嵌套分支。

### 11.5 Copy 与参数处理

第一版可以保留 SSA `Copy`：

```text
%3 = copy %2
```

随后运行安全的 `TrivialCopyElimination`：用 `%2` 替换 `%3` 的所有使用，再删除 copy。为减少第一版复杂度，也可先不删除，交给 MIR copy coalescing。无论选择哪种方式，dump 和测试口径必须固定。

`Param` 在 entry 中生成唯一 SSA Value：

```text
%0 = param 0
%1 = param 1
```

参数序号最终映射到 `a0-a7` 或 incoming stack slot，而不是在 SSA 中直接写物理寄存器。

### 11.6 Trivial Phi 清理

Phi 的所有非自引用输入若是同一个值，则 Phi 可替换为该值：

```text
%7 = phi [bb1: %3], [bb2: %3]  →  所有 %7 使用替换为 %3
```

循环 Phi 可能包含自身，判断时忽略等于 Phi result 的输入。清理应迭代到不再变化，因为删除一个 Phi 可能使另一个 Phi 变平凡。

不得删除有两个不同实际输入的循环 Phi。

### 11.7 SSA dump 格式

新增稳定文本输出：

```text
function @sum:
bb0: ; preds = []
  %0 = param 0
  %1 = param 1
  %2 = add %0, %1
  jump bb1
bb1: ; preds = [bb0, bb2]
  %3 = phi [bb0: %2], [bb2: %5]
  %4 = icmp_lt %3, 10
  branch %4, bb2, bb3
...
```

Phi 必须打印在普通指令之前，输入按 predecessor BlockId 排序。

### 11.8 SSA 专项用例

至少覆盖：

1. 直线局部变量连续赋值：不需要 Phi；
2. if/else 两侧赋值、merge 后读取：一个 Phi；
3. 只有一侧赋值：Phi 合并旧值和新值；
4. while 循环归纳变量：header Phi 含 entry 和 backedge；
5. 嵌套循环多个变量；
6. `a && b`、`a || b` 在值上下文中的结果 Phi；
7. 全局变量读写不被提升为 Phi；
8. void call 无 result，int call 有 result；
9. shadow 后的两个 `SymbolId/TempId` 不混合；
10. 多 return 函数。

### 11.9 本步骤完成定义

- 所有可达 CFG 都能生成 SSA IR；
- 每个 `ValueId` 只有一个定义；
- if/while/短路所需 Phi 正确出现；
- 全局变量仍为显式 load/store；
- SSA dump 连续运行两次字节一致；
- 既有 e2e 仍可通过临时 SSA interpreter 或新后端完成；如果新后端尚未接通，至少所有 SSA 单测和 verifier 通过。

---

## 12. 执行步骤 5：SSA Verifier

### 12.1 verifyIR 必须检查

结构检查：

- 函数有合法 entry；
- 所有块 ID、指令 ID、ValueId 在范围内；
- 每个可达块有一个 terminator；
- predecessor/successor 双向一致；
- terminator target 与 successor 一致；
- 不允许重复边。

指令检查：

- 操作码、result、operand、immediate、global、callee 字段组合合法；
- 每个 Value 恰好定义一次；
- void call 无 result，int call 有 result；
- call 参数数量与函数签名一致；
- return 类型匹配；
- global 操作只引用非常量全局符号；
- 比较和逻辑结果仍按 i32 表示。

Phi 检查：

- Phi 连续位于块首；
- 每个 Phi 对每个 predecessor 恰有一个输入；
- 没有额外 predecessor，也没有重复输入边；
- entry 不应含普通控制流 Phi；
- Phi 输入排序符合稳定 dump 约定。

支配检查：

- 普通指令使用：定义块支配使用块；若同块，定义位置早于使用位置；
- terminator 使用：定义块支配 terminator 所在块；
- Phi 边使用 `(pred, value)`：值的定义必须支配 `pred` 的末端，而不是只支配 Phi 所在块。

### 12.2 DefInfo

建议先建立：

```cpp
struct DefInfo {
    BlockId block;
    std::size_t instructionIndex;
    bool isPhi;
};

std::vector<std::optional<DefInfo>> definitions(valueCount);
```

对于 Phi 边使用，同一 predecessor 块中的定义只要出现在 terminator 前即可；普通指令天然都在 terminator 前。

### 12.3 verifier 负向测试

手工构造非法 IR，分别验证错误能被捕获：

- 两条指令定义同一 Value；
- 使用不存在的 Value；
- 同块先使用后定义；
- 分支定义不支配 merge 普通使用；
- Phi 缺一个前驱输入；
- Phi 多一个错误前驱；
- Phi 放在普通指令后；
- void call 有 result；
- int return 无值；
- CFG 边不对称。

测试只要求诊断包含稳定的错误类别，不要绑定整段内部容器文本。

### 12.4 `--verify-each`

Driver 行为固定为：

- Debug 构建默认在关键阶段验证；
- Release 构建默认至少验证 CFG 和最终 post-RA MIR；
- `--verify-each` 强制在每次变换后验证，不因构建类型关闭；
- verifier 失败返回内部错误码 2，并将阶段名写入 `stderr`；
- verifier 不向 `stdout` 输出任何内容。

### 12.5 本步骤完成定义

- 所有 SSA 正向用例通过；
- 每一种负向损坏至少有一个测试；
- verifier 能明确区分 CFG verification、SSA verification 和 MIR verification；
- `--verify-each` 不再是被忽略参数。

---

## 13. 执行步骤 6：Phi 消除与 critical edge

### 13.1 为什么需要拆边

Phi 的赋值语义发生在“前驱到当前块的边”上。如果前驱有多个后继，直接把 copy 放在前驱末尾会让 copy 在错误分支上也执行。因此满足以下条件的边必须拆分：

```text
pred.successors.size() > 1
&& succ.predecessors.size() > 1
```

### 13.2 splitCriticalEdges

对边 `A → B` 新建 `S`：

```text
A → S → B
```

必须同步完成：

- 将 A terminator 中的 B 替换为 S；
- A successors 中 B 替换为 S；
- B predecessors 中 A 替换为 S；
- S predecessors=`[A]`、successors=`[B]`；
- S terminator=`jump B`；
- B 中每个 Phi 的 predecessor A 改为 S；
- 重新运行 `verifyIR`。

不要只改 successor vector 而忘记 terminator 或 Phi。

### 13.3 IR Phi 到 MIR ParallelCopy

为每个 SSA Value 预先建立唯一 VReg。对 B 中所有 Phi，按 predecessor 分组：

```text
bb3:
  %x = phi [bb1: %a], [bb2: %b]
  %y = phi [bb1: %c], [bb2: %d]
```

在对应边末端生成：

```text
bb1: parallel_copy { vx <- va, vy <- vc }
bb2: parallel_copy { vx <- vb, vy <- vd }
```

MIR 不保留 Phi。`ParallelCopy` 必须在分支/跳转之前。

### 13.4 并行复制解环

删除 `dst == src` 后，重复执行：

1. 找到一个 `dst` 不再作为任何待处理 copy 的 `src`，发出普通 `COPY dst, src` 并删除；
2. 若找不到，说明存在环；选取 `dst <- src`，发出 `COPY temp, src`，然后把待处理 copy 中所有 `src` 替换为 `temp`；
3. 继续直到集合为空。

环用例：

```text
a <- b, b <- a
```

应生成等价于：

```text
tmp <- b
b   <- a
a   <- tmp
```

`temp` 是新 VReg，随后参与正常寄存器分配。

### 13.5 本步骤完成定义

- diamond、loop header 和嵌套控制流的 Phi 都能降低；
- 所有 critical edge 都已拆分；
- MIR 中不存在 Phi；
- 并行复制环不会覆盖旧值；
- 拆边前后 `verifyIR` 都通过；
- 用手工 Phi 测试确认每条边只执行自己的 copy。

---

## 14. 执行步骤 7：建立 RISC-V32 Machine IR

### 14.1 MIR 数据结构

新增 `include/toyc/backend/mir.hpp`。为避免 variant 中多个 `uint32_t` 类型冲突，操作数使用包装结构：

```cpp
struct Immediate { std::int32_t value; };
struct StackSlot { std::uint32_t index; };
struct GlobalSymbol { SymbolId id; };
struct FunctionSymbol { FuncId id; };
struct MachineBlockRef { MBlockId id; };

enum class PhysReg {
    Zero, Ra, Sp,
    T0, T1, T2, T3, T4, T5, T6,
    S0, S1, S2, S3, S4, S5, S6, S7, S8, S9, S10, S11,
    A0, A1, A2, A3, A4, A5, A6, A7
};

using MOperand = std::variant<VRegId, PhysReg, Immediate, StackSlot,
                              GlobalSymbol, FunctionSymbol, MachineBlockRef>;
```

最小操作码集合：

```cpp
enum class MOpcode {
    LI, LA, COPY,
    ADD, ADDI, SUB, MUL, DIV, REM,
    SLT, SLTU, XOR, XORI, SLTIU,
    LW, SW,
    CALL,
    BRCOND, JUMP, RET,
    PARALLEL_COPY
};
```

`MInstruction` 应显式区分 defs、uses 和隐式寄存器集合：

```cpp
struct MInstruction {
    MOpcode opcode;
    std::vector<MOperand> defs;
    std::vector<MOperand> uses;
    RegMask implicitDefs;
    RegMask implicitUses;
    SourceLocation location{};
};
```

分支目标、callee 等非寄存器 use 也可放在 `uses`，但 liveness 只提取其中的 VReg/PhysReg。

### 14.2 MIR 阶段状态

为避免 verifier 不知道当前应允许什么，定义状态：

```cpp
enum class MIRStage {
    PreRegisterAllocation,
    PostRegisterAllocation,
    AfterFrameLowering
};
```

- PreRA：允许 VReg、StackSlot pseudo、COPY，不允许 Phi；
- PostRA：普通指令中不允许 VReg，允许抽象 StackSlot；
- AfterFrameLowering：不允许 VReg 和 StackSlot，只允许物理寄存器、立即数、符号、块引用；允许 `LI/LA/BRCOND` 这三种具有固定汇编展开规则的 target pseudo。

### 14.3 指令选择规则

第二阶段先采用稳定的一对一规则：

| SSA IR | MIR |
|---|---|
| constant | `LI vdst, imm` |
| add/sub/mul/div/rem | 对应 RV32M/ALU 指令 |
| icmp lt | `SLT` |
| icmp gt | 交换输入后 `SLT` |
| icmp le/ge | `SLT` + `XORI 1` |
| icmp eq/ne | `XOR` + `SLTIU 1` 或规范化非零 |
| logical_not | `SLTIU src, 1` |
| load_global | `LA scratch/global-address-vreg` + `LW` |
| store_global | `LA` + `SW` |
| branch bool | `BRCOND cond, true, false`；打印时固定展开为 `bnez` + `j` |
| jump | `JUMP` |
| return value | `COPY a0, value` + `JUMP epilogue` |

大立即数保留 `LI` pseudo 到 frame/late lowering 展开或交给 GNU assembler。`LA` 和双目标 `BRCOND` 也作为 target pseudo 保留到 printer，且其展开规则是固定的一对一格式化，不允许在 printer 中做控制流决策。关键是 MIR verifier 和寄存器分配知道 `LI` 只有一个 def、没有寄存器 use，`BRCOND` 只有寄存器 use 和两个块目标。

### 14.4 函数调用 lowering

调用前：

- 前 8 个参数生成到固定 `a0-a7` 的 COPY；
- 第 9 个及以后生成 `SW value, outgoingStackSlot`；
- `CALL` 的 `implicitUses` 包含实际使用的参数寄存器；
- `CALL` 的 `implicitDefs` 包含所有 caller-saved：`a0-a7`、`t0-t6`、`ra`；
- 有返回值时在 CALL 后生成 `COPY resultVReg, a0`。

函数入口：

- `param 0..7` 生成 `COPY vreg, aN`；
- 栈上传入参数生成 `LW vreg, IncomingArg(index)` pseudo；
- incoming 参数最终 offset 为 `frameSize + (index - 8) * 4`。

### 14.5 MIR dump

推荐格式：

```text
machine @f stage=pre-ra:
mbb0: ; preds=[] succs=[mbb1]
  %v0 = COPY $a0
  %v1 = LI 1
  %v2 = ADD %v0, %v1
  JUMP mbb1
```

物理寄存器使用 `$a0`，虚拟寄存器使用 `%v0`，栈对象使用 `fi#0`。dump 不直接使用最终汇编语法，以免把 pseudo 与真实指令混淆。

### 14.6 本步骤完成定义

- 所有 SSA 操作码都有明确 lowering；
- `AsmPrinter` 之前存在完整 `MachineModule`；
- 每个 call 带有准确 implicit use/def；
- MIR dump 可重复；
- `mir_tests` 覆盖算术、比较、global、call、branch、return；
- PreRA MIR verifier 通过。

---

## 15. 执行步骤 8：MIR Verifier

### 15.1 通用检查

- Machine block ID 稠密且引用合法；
- predecessors/successors 双向一致；
- 每个块只有最后一条是 terminator；`BRCOND` 自身同时携带 true/false 两个目标，不在同块再附加第二条 `JUMP`；
- `RET/JUMP/branch` 的操作数数量合法；
- 每个 opcode 的 defs/uses 数量和操作数种类合法；
- `CALL` 的 callee 是函数符号，并具有 caller-saved clobber；
- `PARALLEL_COPY` 在 resolver 后不存在；
- 所有 VReg ID 小于 `vregCount`；
- 同一 VReg 的使用前存在定义，或由入口 live-in 明确定义。

### 15.2 分阶段检查

PreRA：

- 允许 VReg；
- Phi 不存在；
- COPY/LI 等 pseudo 合法；
- 物理 `a` 寄存器只出现在参数、返回、call 相关固定约束中；
- `t5/t6` 不作为普通 VReg 的预着色目标。

PostRA：

- 普通算术、比较、branch 中不存在 VReg；
- 使用的 `s1-s11` 记录在 `usedCalleeSaved`；
- spill slot index 合法；
- 跨 CALL 活跃值不位于 caller-saved 寄存器；
- CALL 的 fixed register 约束仍完整。

AfterFrameLowering：

- 不存在 VReg、StackSlot、IncomingArg、OutgoingArg pseudo；
- 所有 `addi/lw/sw` 立即数落在 `[-2048, 2047]`；
- frame size 为 16 的倍数；
- 非叶函数保存 `ra`，叶函数允许不保存；
- 每个 return 最终到共享 epilogue 或拥有等价恢复序列。

### 15.3 本步骤完成定义

- 三种 stage 均有正向和负向测试；
- 人工删除 call clobber 会被拒绝；
- 人工留下 VReg/FrameIndex 会在对应阶段被拒绝；
- verifier 错误包含函数、块、指令位置和阶段名。

---

## 16. 执行步骤 9：活跃性分析

### 16.1 块级 use/def

Phi 已消除，因此使用标准方程：

```text
liveOut[B] = union(liveIn[S])，S 属于 successors(B)
liveIn[B]  = use[B] union (liveOut[B] - def[B])
```

构造块 `use/def` 时按指令顺序：

- 一个 VReg 在本块首次定义前被使用，则加入 `use`；
- 每个显式 def 加入 `def`；
- implicit use/def 对物理寄存器约束单独记录；
- 寄存器分配的 VReg liveness 不把 block target、symbol、immediate 当寄存器。

使用动态 bitset 或 `vector<uint64_t>`，不要使用 `vector<bool>` 暴露复杂引用语义。项目规模小，第一版也可以使用排序后的 `vector<VRegId>`，但迭代和集合差运算必须有测试。

### 16.2 迭代顺序

按 reverse postorder 的逆序迭代，通常能更快收敛：

```text
repeat
  changed = false
  for B in reverse(RPO):
    recompute out/in
until !changed
```

### 16.3 指令位置和 live interval

为每条非调试指令分配偶数位置 `0,2,4,...`，预留奇数位置给 spill reload/store。对每个 VReg 记录：

```cpp
struct LiveInterval {
    VRegId vreg;
    std::uint32_t start;
    std::uint32_t end;
    std::vector<std::uint32_t> uses;
    bool crossesCall = false;
    double spillWeight = 0.0;
};
```

第二阶段可使用保守的单区间 `[start,end]`，暂不实现 lifetime holes。循环权重可以先固定为 1；loop depth 权重留到第四阶段。

`crossesCall` 必须基于“在 call 前定义且 call 后仍活跃”，不能仅凭函数中存在 call 就将所有值标记为跨调用。

### 16.4 活跃性测试

- 直线 def-use；
- diamond 两分支在 merge 使用；
- loop backedge 使 induction value live-out；
- 值在 call 前最后一次使用，不算跨 call；
- 值在 call 后使用，算跨 call；
- 未使用定义的 interval；
- 多块 fixed-point 至少迭代两轮的用例。

### 16.5 本步骤完成定义

- `liveness_tests` 的 liveIn/liveOut 与手算集合一致；
- 结果与块存储顺序无关；
- interval 顺序稳定；
- `crossesCall` 正确区分 call 前死亡和跨 call；
- PreRA MIR verifier 在分析前后都通过，分析不修改 MIR。

---

## 17. 执行步骤 10：线性扫描寄存器分配与 spill

### 17.1 分配接口

```cpp
class LinearScanRegisterAllocator {
public:
    AllocationResult run(MachineFunction& function);
};

struct AllocationResult {
    std::vector<std::optional<PhysReg>> registers;
    std::vector<std::optional<StackSlot>> spillSlots;
    std::vector<PhysReg> usedCalleeSaved;
};
```

### 17.2 线性扫描

按 `(start, vregId)` 排序 interval，维护按 end 排序的 active：

```text
for current in intervals:
  expire active intervals with end < current.start
  allowed = current.crossesCall ? calleeSaved : callerSaved + calleeSaved
  if allowed 中有空闲寄存器:
    按固定优先级分配
  else:
    victim = active 中 end 最远且寄存器属于 allowed 的 interval
    if victim.end > current.end:
      spill victim，current 接管寄存器
    else:
      spill current
```

固定寄存器优先级：

```text
非跨 call：t0,t1,t2,t3,t4,s1,...,s11
跨 call：s1,s2,...,s11
```

同一输入必须得到确定的分配结果，不能依赖 unordered_map 遍历顺序。

### 17.3 Spill rewrite

被 spill 的原 VReg 不允许直接留在算术指令中。对每个使用和定义改写：

```text
原：%v3 = ADD %v1, %v2

若 v1 spill 到 fi#0：
  %reload = LOAD_STACK fi#0
  %v3 = ADD %reload, %v2

若 v3 也 spill 到 fi#1：
  %tmp = ADD %reload, %v2
  STORE_STACK fi#1, %tmp
```

rewrite 后：

1. 原 spilled VReg 不再出现在普通指令中；
2. reload/store 使用新短生命 VReg；
3. 重新运行 MIR verifier、liveness 和 linear scan；
4. 最多迭代 8 次；
5. 若仍 spill 新建的 reload 临时值，报告内部错误并输出 MIR dump，不能静默生成错误代码。

`t5/t6` 保留策略提供最终兜底：frame lowering 展开 post-RA 栈访问时无需向分配器临时借寄存器。

### 17.4 COPY 处理

第二阶段不要求完整 coalescing，但至少：

- 若 COPY 两端最终分配到同一物理寄存器，删除 COPY；
- `COPY r,r` 删除；
- fixed register COPY 保留正确顺序；
- 不得跨 CALL 合并到 caller-saved 导致值被破坏。

### 17.5 压力测试

构造至少以下测试：

- 同时活跃值少于 5：全部使用 `t` 寄存器，无 spill；
- 6～16 个不跨 call 的值：使用 `t` 和 `s`；
- 12 个以上跨 call 值：至少一个 spill；
- spill value 在循环 backedge 使用；
- 多次 spill rewrite 后仍能完成分配；
- 参数准备和返回 COPY；
- 递归调用；
- 大量常量和二元表达式。

### 17.6 本步骤完成定义

- PostRA 普通指令中无 VReg；
- 跨 call 活跃值只位于 `s1-s11` 或 spill slot；
- 所有使用的 callee-saved 被准确记录；
- 人工高压力用例能够发生 spill 并正确运行；
- 分配结果可重复；
- 无 spill 的简单函数不产生无意义 stack load/store。

---

## 18. 执行步骤 11：Frame lowering 与完整 ABI

### 18.1 Frame 对象

Frame 只在寄存器分配后确定。固定布局：

```text
高地址
┌────────────────────────────┐
│ 调用者传入的第 9+ 个参数    │  old sp + 0, +4, ...
├────────────────────────────┤
│ 保存的 ra                   │  仅非叶函数
├────────────────────────────┤
│ 保存的 s1-s11               │  仅实际使用
├────────────────────────────┤
│ spill slots                 │
├────────────────────────────┤
│ outgoing argument area      │  new sp + 0, +4, ...
└────────────────────────────┘ ← new sp，16 字节对齐
低地址
```

实现结构：

```cpp
struct FrameLayout {
    std::int32_t frameSize{};
    std::int32_t outgoingBytes{};
    std::vector<std::int32_t> spillOffsets;
    std::vector<std::pair<PhysReg, std::int32_t>> calleeSavedOffsets;
    std::optional<std::int32_t> raOffset;
};
```

### 18.2 Frame 规划顺序

1. 扫描 call，取最大栈上传参数字节数，作为 outgoing area；
2. 为 spill slot 分配 4 字节对齐 offset；
3. 为实际使用的 callee-saved 分配保存槽；
4. 非叶函数为 `ra` 分配槽；
5. 总大小向上对齐到 16；
6. 将 incoming arg pseudo 重写为 `sp + frameSize + argOffset`；
7. 展开 prologue/epilogue；
8. 展开所有 frame index。

### 18.3 叶函数与非叶函数

- 没有 CALL 的函数是叶函数，不保存 `ra`；
- frameSize 为 0 时不调整 `sp`；
- 使用了 `s` 寄存器即使是叶函数也必须保存/恢复相应 `s`；
- 非叶函数必须保存/恢复 `ra`；
- 只保存实际使用的 callee-saved；
- 所有退出路径汇合到一个 epilogue block。

### 18.4 大 frame 与大 offset

RV32 `addi/lw/sw` 的立即数范围是 `[-2048,2047]`。超范围时使用保留的 `t6` 计算地址：

```asm
li  t6, offset
add t6, sp, t6
lw  t5, 0(t6)
```

store 的值若已经在 `t6`，地址必须改用 `t5`，不能覆盖待存值。建议封装两个 helper，根据 value register 选择另一个保留 scratch：

```cpp
PhysReg chooseAddressScratch(PhysReg valueReg);
void materializeFrameLoad(...);
void materializeFrameStore(...);
```

调整超大 frame：

```asm
li  t6, -frameSize
add sp, sp, t6
```

### 18.5 ABI 专项检查

- 0～8 个参数全部通过寄存器；
- 9～12 个参数同时覆盖 caller store 和 callee incoming offset；
- 嵌套调用时 outgoing area 不覆盖 spill/callee-save；
- 递归时每层 frame 独立；
- 跨 call 活跃值保持；
- 叶函数不保存 `ra`；
- 非叶函数保存 `ra`；
- `sp` 在每个 CALL 前为 16 字节对齐；
- int 返回写 `a0`，void return 不制造返回值；
- 大于 2047 的 frame/offset 不生成非法立即数。

### 18.6 本步骤完成定义

- `abi_tests` 和 e2e 的 many_args、recursive_factorial、large_frame 全部通过；
- AfterFrameLowering verifier 通过；
- 简单 `int main(){return 0;}` 可生成零 frame 或最小合法汇编；
- 叶函数不再无条件保存 `ra`；
- 每个已使用 `s` 寄存器恰好保存和恢复一次；
- 栈上传参与 spill 同时存在时结果正确。

---

## 19. 执行步骤 12：纯 AsmPrinter 与 Driver 接线

### 19.1 AsmPrinter 边界

最终接口：

```cpp
class AsmPrinter {
public:
    explicit AsmPrinter(std::ostream& output) : output_(output) {}
    void print(const MachineModule& module,
               const SemanticResult& semantic);
};
```

允许 `SemanticResult` 只用于把 `FuncId/SymbolId` 格式化为名字；printer 不得再：

- 计算活跃性；
- 分配栈槽；
- 选择寄存器；
- 判断是否保存 `ra/sN`；
- 拆 critical edge；
- 解决 parallel copy；
- 删除或重排有语义的指令。

AsmPrinter 可以展开 GNU pseudo 的文本形式，如 `mv`、`li`、`la`、`j`、`ret`，前提是 MIR 已明确表达对应语义。

### 19.2 Driver 最终顺序

`main.cpp` 应清晰显示阶段：

```cpp
auto cfg = CFGBuilder(semantic).build(*ast);
removeUnreachable(cfg);
verifyCFG(cfg, semantic);

auto ir = SSABuilder(semantic).build(cfg);
verifyIR(ir, semantic);

auto machine = InstructionSelector(semantic).lower(ir);
resolveParallelCopies(machine);
verifyMIR(machine, MIRStage::PreRegisterAllocation);

for (auto& function : machine.functions) {
    LinearScanRegisterAllocator().run(function);
    verifyMIR(function, MIRStage::PostRegisterAllocation);
    FrameLowering().run(function);
    verifyMIR(function, MIRStage::AfterFrameLowering);
}

AsmPrinter(std::cout).print(machine, semantic);
```

具体 verifier 是否每次都运行由构建类型和 `--verify-each` 决定，但测试构建必须全部开启。

### 19.3 CompilerOptions

```cpp
struct CompilerOptions {
    bool optimize = false;
    bool verifyEach = false;
    bool dumpTokens = false;
    bool dumpAst = false;
    bool dumpCfg = false;
    bool dumpIr = false;
    bool dumpMir = false;
    bool dumpMirAfterRA = false;
};
```

第二阶段中 `optimize` 可以暂时只选择预留的 pipeline 配置；普通模式和 `-opt` 使用同一个正确后端是允许的。第三阶段加入优化 pass 后再让两者产生不同 SSA。不得继续“解析到 `-opt` 后直接 continue 而不记录”。

### 19.4 旧后端处理

迁移期间可以把当前 `asm_printer.cpp` 临时改名为 `legacy_asm_printer.cpp`，用于对照测试。新后端全部 e2e 通过后：

- 删除 legacy 编译目标；
- 不保留两套默认后端；
- 如确需调试对照，只允许测试代码直接调用，不增加长期用户接口；
- 最终 `compiler` 必须只走 SSA/MIR 后端。

### 19.5 本步骤完成定义

- 正式 `compiler` 不再从 CFG 直接输出汇编；
- stdout 只含汇编；
- 所有 dump 和诊断只写 stderr；
- 新增参数真实生效，未知参数仍报错；
- legacy 路径不在最终生产目标中；
- 普通和 `-opt` 都通过完整 correctness suite。

---

## 20. 执行步骤 13：测试体系升级

### 20.1 C++ 测试目标

`tests/CMakeLists.txt` 最终至少注册：

```text
lexer_tests
parser_tests
semantic_tests
cfg_tests
dominator_tests
ssa_tests
mir_tests
liveness_tests
regalloc_tests
abi_tests
codegen_tests
e2e_tests（工具链可用时）
```

不要把所有新测试塞进 `codegen_tests.cpp`。失败测试名应能直接指出层级。

### 20.2 TestPipeline 改造

建议使用显式阶段方法，避免每个前端测试都无条件跑完整后端：

```cpp
struct TestPipeline {
    // source、diagnostics、ast、semantic

    CFGModule buildCFG();
    IRModule buildIR();
    MachineModule buildPreRAMIR();
    MachineModule buildFinalMIR();
    std::string emitAssembly();
};
```

也可缓存阶段结果，但必须避免返回悬空引用。前端单测继续只运行所需阶段。

### 20.3 端到端用例扩充

在现有用例上新增：

- `phi_diamond.tc`：if/else 后使用合并值；
- `phi_loop.tc`：循环变量和循环退出值；
- `nested_phi.tc`：嵌套 if + while；
- `live_across_call.tc`：多个局部值跨调用保持；
- `callee_saved.tc`：迫使使用多个 s 寄存器；
- `spill_pressure.tc`：同时活跃值超过可分配寄存器；
- `spill_and_many_args.tc`：spill 与 9+ 参数同现；
- `leaf_frame.tc`：叶函数与非叶函数组合；
- `parallel_copy_cycle.tc`：构造循环 Phi 搬运；
- `global_call_barrier.tc`：调用前后全局读写顺序。

每个 `.tc` 均有 `.expected`，普通和 `-opt` 都运行。

### 20.4 随机差分测试

新增 `scripts/generate_random_cases.py`，固定约束：

- seed 完全决定输出；
- 只生成已支持语法；
- 除数/模数保证非零；
- 避免 C 有符号溢出；
- 循环使用显式上界，保证终止；
- 递归深度有界；
- 所有 int 函数路径 return；
- 生成结果只通过 main 退出码观察。

`differential_test.sh` 对每个程序：

1. 使用 GCC 将同一 ToyC/C 子集程序编译为 RV32；
2. 使用 ToyC 普通模式生成 RV32 汇编；
3. 使用 ToyC `-opt` 生成 RV32 汇编；
4. 分别静态链接到相同 runtime；
5. 使用 QEMU 运行；
6. 比较三者退出码低 8 位；
7. 失败时保留 seed、源码、三份汇编和日志。

分层门禁：

- 每次提交：固定 20 个 seed；
- 合并前：固定 100 个 seed；
- 第二阶段最终验收：至少 1000 个 seed；
- 所有 seed 同时覆盖普通和 `-opt`。

### 20.5 汇编结构检查

`codegen_tests` 不只搜索某条字符串，还应断言：

- leaf 函数无 `sw ra`；
- non-leaf 函数有成对 `sw/lw ra`；
- 使用 `sN` 时有成对保存恢复；
- 简单算术不出现每个临时值一对 `sw/lw`；
- frame size 是 16 的倍数；
- 大 offset 展开后没有越界 `lw/sw offset(sp)`；
- 最终汇编无 `%v`、`fi#`、`parallel_copy`；
- `.globl/.type/.size` 和全局 section 正确。

### 20.6 本步骤完成定义

- 所有分层测试均加入 CTest；
- WSL e2e 同时跑普通和 `-opt`；
- 1000 个确定性随机程序差分为 0 mismatch；
- 失败 seed 可以单独重放；
- 测试临时文件使用临时目录，不污染仓库；
- 测试脚本全部纳入版本控制。

---

## 21. 分步验收命令

### 21.1 Windows 本地构建与单元测试

使用独立目录，避免旧缓存掩盖缺文件：

```powershell
cmake -S . -B build-stage2-win -DBUILD_TESTING=ON
cmake --build build-stage2-win --config Debug
ctest --test-dir build-stage2-win -C Debug --output-on-failure

cmake --build build-stage2-win --config Release
ctest --test-dir build-stage2-win -C Release --output-on-failure
```

若生成器是 Ninja 单配置，`--config`/`-C` 可以保留但不起决定作用；若是 Visual Studio，多配置时不能省略 `-C Debug/Release`。

### 21.2 WSL Release 与端到端

```sh
cd /mnt/d/Repo/compiler
cmake --preset wsl-release
cmake --build --preset wsl-release -j2
ctest --preset wsl-release
```

单独运行 e2e：

```sh
ctest --test-dir build-wsl -R '^e2e_tests$' --output-on-failure
```

### 21.3 Dump 检查

```sh
./build-wsl/compiler --dump-cfg \
  < tests/cases/e2e/phi_loop.tc \
  > /tmp/phi_loop.s 2> /tmp/phi_loop.cfg

./build-wsl/compiler --dump-ir --verify-each \
  < tests/cases/e2e/phi_loop.tc \
  > /tmp/phi_loop.s 2> /tmp/phi_loop.ir

./build-wsl/compiler --dump-mir --dump-mir-after-ra \
  < tests/cases/e2e/live_across_call.tc \
  > /tmp/live.s 2> /tmp/live.mir
```

核对：

- stdout 文件能直接交给交叉 GCC；
- stderr 中包含请求的 dump；
- IR 中有预期 Phi；
- pre-RA MIR 中有 VReg，无 Phi；
- post-RA MIR 中无 VReg；
- 最终汇编中无 IR/MIR 调试文本。

### 21.4 汇编、链接、运行

```sh
riscv64-linux-gnu-gcc \
  -march=rv32im -mabi=ilp32 \
  -nostdlib -static -no-pie \
  -Wl,--no-relax -Wl,-e,_start \
  tests/runtime/rv32_linux_start.S /tmp/phi_loop.s \
  -o /tmp/phi_loop.out

qemu-riscv32 /tmp/phi_loop.out
echo $?
```

### 21.5 随机差分

最终脚本接口建议固定为：

```sh
./scripts/differential_test.sh \
  --compiler ./build-wsl/compiler \
  --seed-start 0 \
  --count 100

./scripts/differential_test.sh \
  --compiler ./build-wsl/compiler \
  --seed-start 0 \
  --count 1000
```

### 21.6 性能和汇编统计

```sh
TOYC_BENCH_ITERATIONS=500 TOYC_BENCH_SAMPLES=9 \
  ./scripts/benchmark.sh \
  ./build-wsl/compiler tests/cases/bench/*.tc
```

静态 load/store 统计：

```sh
./build-wsl/compiler < tests/cases/bench/stateful_loop.tc > /tmp/stateful.s
grep -Ec '^[[:space:]]+lw[[:space:]]' /tmp/stateful.s
grep -Ec '^[[:space:]]+sw[[:space:]]' /tmp/stateful.s
```

第二阶段不是最终性能阶段，但新后端必须证明寄存器分配真正生效：

- `stateful_loop.tc` 静态 `lw + sw` 必须少于第一阶段基线 48；
- `work` 的 frame 必须小于第一阶段基线 80 字节，除非测试日志解释了额外保存槽的原因；
- QEMU median 不作为单次提交硬门禁，但同机 9 次中位数若回退超过 15%，必须分析汇编并记录原因。

### 21.7 仓库卫生

```powershell
git status --short
git diff --check
rg -n "D:\\Repo|/mnt/d/Repo|build-release" -g '!*.md'
```

允许文档中的示例路径，不允许源码、CMake 或脚本硬编码本机绝对路径。

---

## 22. 第二阶段总完成定义（Definition of Done）

只有以下门禁全部满足，第二阶段才算完成。

### 22.1 构建与接口门禁

- Windows Debug/Release 从新构建目录成功构建；
- WSL Release 成功构建；
- C++20 警告为 0；
- stdin/stdout 正式接口不变；
- 普通和 `-opt` 都可用；
- `--dump-ir`、`--dump-mir`、`--dump-mir-after-ra` 可用；
- `--verify-each` 真实执行 verifier；
- stdout 永不混入 dump 或诊断。

### 22.2 CFG/SSA 门禁

- 非 SSA CFG 与 SSA IR 使用不同 C++ 类型；
- 局部变量通过 `Param/Copy` 进入 SSA，全局通过显式 load/store；
- 所有可达块结构合法；
- dominator 和 dominance frontier 测试全部通过；
- 每个 SSA Value 恰好定义一次；
- 普通使用被定义支配；
- Phi 输入与每个前驱一一对应，且边使用支配关系正确；
- while、if/else、短路值均产生正确 Phi；
- SSA dump 稳定。

### 22.3 MIR/寄存器分配门禁

- SSA Phi 在 MIR 前正确降低；
- critical edge 全部正确拆分；
- parallel copy 环正确解开；
- PreRA、PostRA、AfterFrameLowering verifier 全部通过；
- PostRA 普通指令中无 VReg；
- AfterFrameLowering 中无 VReg、FrameIndex、ParallelCopy；
- 跨 CALL 活跃值不位于 caller-saved；
- 高寄存器压力用例发生 spill 后仍正确；
- 相同输入的寄存器分配和 MIR dump 可重复。

### 22.4 ABI 门禁

- 0～12 参数调用正确；
- int/void 返回正确；
- 递归和嵌套调用正确；
- 叶函数不强制保存 `ra`；
- 非叶函数保存恢复 `ra`；
- 使用的 callee-saved 成对保存恢复；
- 每个 CALL 前 `sp` 16 字节对齐；
- outgoing args、spill slots、callee-save slots 不重叠；
- incoming stack arg offset 正确包含 callee frameSize；
- 大 frame/offset 无非法 12 位立即数；
- 全局变量和 call 内存屏障语义正确。

### 22.5 测试门禁

- 所有 C++/CTest 测试 100% 通过；
- 所有既有 e2e 用例无回归；
- 新增 Phi、跨调用、callee-save、spill、large-frame 专项 e2e；
- 普通和 `-opt` 都运行同一 correctness suite；
- 至少 1000 个确定性随机程序与 GCC 差分 0 mismatch；
- 从干净 clone/新 build 目录复验成功；
- 测试脚本全部已提交。

### 22.6 工程效果门禁

- 正式后端不再从 CFG 直接打印汇编；
- `AsmPrinter` 只做格式化；
- `stateful_loop.tc` 静态 `lw+sw` 少于 48；
- `work` frame 小于 80 字节，或有书面解释；
- benchmark 日志包含环境、参数、before/after 与 GCC `-O2`；
- 未把 SCCP/GVN/LICM 等第三阶段内容混入本阶段关键路径。

---

## 23. 推荐提交顺序

每个提交都应能够构建；从切换正式后端开始，每个提交必须通过完整 e2e。

1. `test(e2e): track scripts and validate normal and opt modes`
2. `refactor(ir): separate cfg types from future ssa ir`
3. `refactor(cfg): normalize locals to param and copy operations`
4. `feat(ir): add cfg utilities and dominator analysis`
5. `feat(ir): construct and print ssa form`
6. `feat(ir): verify phi and dominance invariants`
7. `feat(backend): add machine ir and verifier`
8. `feat(backend): split critical edges and lower phi copies`
9. `feat(backend): select rv32im machine instructions`
10. `feat(backend): compute liveness and live intervals`
11. `feat(backend): add linear scan allocation and spill rewrite`
12. `feat(backend): lower frames and enforce ilp32 abi`
13. `refactor(backend): make asm printer consume final mir`
14. `feat(driver): wire ir and mir dumps and verify-each`
15. `test(diff): add deterministic random differential tests`
16. `test(stage2): add phi spill abi and clean-build gates`

如某一步需要临时 legacy backend，必须在第 13 个提交中移出正式目标。

---

## 24. 可直接执行的开发检查表

### 基线

- [ ] 提交 `scripts/` 中现有四个脚本
- [ ] e2e 同时覆盖 normal 和 `-opt`
- [ ] 记录第一阶段 benchmark 与汇编统计
- [ ] Windows/WSL 干净构建通过

### CFG 与 SSA

- [ ] 重命名 CFG 类型，消除 `ModuleIR` 含义混乱
- [ ] 新增独立 SSA IR 类型
- [ ] `ReadVar/WriteVar` 拆成局部 `Param/Copy` 和全局 load/store
- [ ] 实现 reachability、RPO、remove unreachable
- [ ] 实现 idom、dom tree、dominance frontier
- [ ] 实现 Phi 插入和支配树 rename
- [ ] 实现 trivial Phi 清理
- [ ] 实现稳定 IR dump
- [ ] 实现 SSA verifier 和负向测试

### MIR 与后端

- [ ] 新增 MIR operand/opcode/block/function/module
- [ ] 实现 critical-edge split
- [ ] 实现 Phi → ParallelCopy
- [ ] 实现 parallel copy 解环
- [ ] 实现 RV32IM instruction selection
- [ ] 明确 call implicit use/def
- [ ] 实现 MIR verifier 三阶段检查
- [ ] 实现块级活跃性和 live interval
- [ ] 实现确定性 linear scan
- [ ] 实现 repeatable spill rewrite
- [ ] 实现按需 callee-save 与 `ra` 保存
- [ ] 实现 outgoing/incoming args 和 16 字节对齐
- [ ] 实现大 frame/offset 展开
- [ ] 将 AsmPrinter 收缩为纯输出层

### Driver 与测试

- [ ] 实现 `optimize/verifyEach` 真实字段
- [ ] 增加 `--dump-ir`
- [ ] 增加 `--dump-mir`
- [ ] 增加 `--dump-mir-after-ra`
- [ ] 新增 dominator/ssa/mir/liveness/regalloc/abi 测试
- [ ] 新增 Phi、跨调用、spill e2e
- [ ] 新增可重放随机程序生成器
- [ ] 100 个 seed 合并前差分通过
- [ ] 1000 个 seed 阶段验收通过
- [ ] 普通和 `-opt` correctness 100% 通过
- [ ] 静态 `lw+sw` 少于第一阶段基线
- [ ] 从干净 clone 完成最终复验

---

## 25. 常见失败点与定位顺序

### 25.1 Phi 值错误

优先检查：

1. CFG predecessors 是否与 terminator 一致；
2. dominance frontier 是否漏掉 loop header；
3. rename 离开支配树节点时是否正确 pop；
4. Phi 输入记录的是 predecessor edge，而不是遍历时的“上一个块”；
5. critical-edge split 是否同步更新 Phi predecessor。

### 25.2 简单程序正确、循环错误

优先检查：

- header Phi 是否含 entry 和 backedge；
- liveness 是否沿 backedge 收敛；
- parallel copy 环是否覆盖旧值；
- spill reload 是否放在每次循环 use 前；
- callee-saved 恢复是否发生在共享 epilogue。

### 25.3 无调用正确、有调用错误

优先检查：

- CALL implicitDefs 是否包含 `t0-t6/a0-a7/ra`；
- 跨 call interval 是否误分到 `t`；
- 返回值是否在下一次参数搬运前从 `a0` copy 出来；
- 第 9+ 参数 offset 是否区分 caller new sp 和 callee old sp；
- CALL 前 `sp` 是否 16 字节对齐；
- `ra` 是否只在非叶函数但确实被保存。

### 25.4 只有大程序错误

优先检查：

- spill rewrite 是否仍残留 VReg；
- spill slot 与 outgoing args 是否重叠；
- 大 offset helper 是否覆盖待存值寄存器；
- frameSize 正负号是否正确；
- incoming arg 使用的是否为 `new sp + frameSize + offset`；
- 迭代分配是否在改写后重新计算 liveness。

### 25.5 汇编器拒绝但 MIR verifier 通过

优先检查：

- AfterFrameLowering verifier 是否遗漏立即数范围；
- printer 是否把 pseudo 打成了目标工具链不支持的形式；
- `-march=rv32im -mabi=ilp32` 是否与 `mul/div/rem` 一致；
- label、`.type/.size`、section 是否完整；
- stdout 是否混入 dump。

定位时始终从第一个失败层级开始：

```text
CFG verifier
  → SSA verifier
  → pre-RA MIR verifier
  → liveness/RA tests
  → post-RA verifier
  → frame verifier
  → assembler
  → QEMU result
```

不要在 QEMU 返回值错误时直接修改汇编字符串；先找出最早出现不变量破坏的阶段。

---

## 26. 第二阶段结束时的预期状态

第二阶段结束后，前端输入能力与第一阶段相同，但中后端应发生本质变化：

- 局部变量已经提升为 SSA Value；
- 分支和循环合流通过 Phi 明确表达；
- Phi 在边上正确降低为并行复制；
- RISC-V 约束存在于可验证的 MIR，而不是隐藏在打印器中；
- 大多数临时值停留在物理寄存器，不再每次写回栈；
- 跨调用值、callee-saved、spill 和栈参数遵守 ILP32 ABI；
- 所有结构都有稳定 dump 和 verifier；
- 随机差分能够持续证明重构前后语义一致；
- 第三阶段可以直接在 SSA IR 上实现 SCCP、SimplifyCFG、DCE 和 GVN，而不必再次重写后端。

达到上述状态后，才进入第三阶段高收益优化。第二阶段的最终价值是把“当前能运行的编译器”变成“可以安全承载优化、能够定位错误、后端行为可解释的编译器”。
