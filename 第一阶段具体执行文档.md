# ToyC 编译器第一阶段具体执行文档

> 对应《编译系统实践实现指南》的“里程碑 1：最小正确闭环”。本文既是设计说明，也是可逐项执行的开发清单。  
> 基线：`main` 分支提交 `ddd29ab`（Initial commit）；仓库检查日期：2026-08-11。  
> 重要说明：生成本文时只新增了本文档，尚未修改编译器代码。第 4～10 节描述的是第一阶段实施后应产生的代码变更。

## 1. 第一阶段目标

第一阶段交付一个能覆盖完整 ToyC 语言、从标准输入读取源码并向标准输出生成 RISC-V32 汇编的最小编译器：

```text
stdin 中的 ToyC
  → Lexer
  → Parser / AST
  → Semantic Analyzer
  → 非 SSA 三地址码与 CFG
  → 栈式 RV32IM 后端
  → stdout 中的汇编
```

这一阶段的关键词是“正确闭环”，不是优化。所有局部变量和 IR 临时值都可以放入栈槽，后端仅用 `t0`～`t2` 做短暂计算。这样会生成较慢的代码，但能避免第一版同时调试 SSA、Phi、活跃性和寄存器分配。

第一阶段必须做到：

- 支持 PDF 定义的全部 ToyC 语法：全局/局部变量与常量、`int`/`void` 函数、表达式、块、`if/else`、`while`、`break`、`continue`、`return` 和函数调用。
- 支持嵌套作用域、声明后使用、常量求值、类型/返回值检查和调用顺序约束。
- 正确实现 `&&`、`||` 的短路语义。
- 生成可组装、链接和运行的 RV32IM/ILP32 汇编。
- 正确处理递归、超过 8 个参数、全局变量以及 16 字节栈对齐。
- 接受 `-opt`，但第一阶段允许它与无参数模式使用同一条无优化流水线。
- 所有调试输出和错误信息进入 `stderr`；`stdout` 只能出现汇编。

第一阶段明确不做：SSA、Phi、DAG、SCCP、GVN、DCE、LICM、指令调度、图着色寄存器分配和性能冲刺。这些属于后续里程碑，不得混入本阶段的完成条件。

## 2. 当前代码分析

### 2.1 仓库现状

当前受版本控制的实现文件只有：

| 文件 | 现有职责 | 结论 |
|---|---|---|
| `token.hpp` | Token 枚举、字面量值、起始行列 | 可作为基础，但缺少独立的 `<cstddef>`、Token 名称转换和范围信息 |
| `lexer.hpp` | Lexer 状态和 `next()` 接口 | 主体可保留；Parser 的 lookahead 不应塞进 Lexer |
| `lexer.cpp` | 标识符、关键字、数字、空白、注释和运算符扫描 | 大部分可用，但 `!`/`!=` 返回类型错误 |
| `.gitignore` | 忽略 `.agents/`、`.codex/` | 未忽略 CMake 构建目录和测试产物 |

当前不存在 `CMakeLists.txt`、程序入口、AST、Parser、语义分析、IR/CFG、RISC-V 后端、测试或脚本。因此当前仓库不能构建可执行编译器，也无法做端到端验证。

### 2.2 Lexer 已有能力

当前 Lexer 已正确实现以下基础能力：

- `[_A-Za-z][_A-Za-z0-9]*` 风格的标识符以及 9 个关键字；
- 数字字符扫描和 `std::from_chars` 到 `int64_t`；
- 空白、`//` 注释和 `/* ... */` 注释；
- 行号、列号跟踪；
- `==`、`<=`、`>=`、`&&`、`||` 的最长匹配；
- 单独 `&`、`|`、未知字符和未闭合块注释报错；
- `Token::lexeme` 用 `string_view` 零拷贝引用 Lexer 持有的源码。

### 2.3 已确认问题与风险

| 位置 | 问题 | 影响 | 第一阶段处理 |
|---|---|---|---|
| `lexer.cpp:249-252` | `!=` 被返回为 `EqualEqual`，`!` 被返回为 `Assign` | 所有逻辑非和不等比较语义错误 | 分别改成 `NotEqual` 和 `Not` |
| `lexer.cpp:50` | 使用 `std::move` 但未显式包含 `<utility>` | 依赖传递包含，不具备可移植性 | 增加 `<utility>` |
| `token.hpp:52-53` | 使用 `std::size_t` 但未显式包含 `<cstddef>` | 依赖传递包含 | 增加 `<cstddef>` |
| `lexer.cpp:160-185` | 接受 `00`、`012` 等前导零数字 | 不符合 PDF 的 `0\|[1-9][0-9]*` | 若首位为 `0` 且后续仍为数字，给出词法错误 |
| `Token::lexeme` | 生命周期绑定到 `Lexer::source` | AST 若保存该 view，Lexer 销毁后会悬空 | Parser 将标识符复制成 `std::string`；AST 不保存 token view |
| 错误处理 | 只抛通用 `runtime_error` | 不利于统一诊断与测试 | 引入 `CompileError`/`DiagnosticEngine`，至少统一 `line:column: category: message` |
| 工程结构 | 无命名空间、构建、测试和入口 | 后续符号冲突，无法集成 | 迁移到 `toyc` 命名空间和标准目录 |

`-` 必须继续单独产生 `Minus`，不要让 Lexer 吞入负数字面量。Parser 将 `-2147483648` 建成“一元负号 + 宽字面量”，语义阶段再做 32 位范围判断。这能同时正确处理 `a-1`、`--1` 和最小 `int32_t`。

### 2.4 当前环境验证能力

当前机器检测到 CMake 4.1.2，但 PATH 中没有 `g++`、`clang++`、`cl`、RISC-V GCC 或 `qemu-riscv32`。所以本文生成时只能完成静态代码审阅，不能声称现有代码已编译通过。

开始编码前必须安装或接入：

1. 一个支持 C++20 的宿主编译器；
2. 能产生 RV32IM/ILP32 可执行文件的交叉工具链；
3. `qemu-riscv32` 或课程提供的等价运行环境。

宿主工具缺失不能降低完成标准；只表示前端单测和端到端测试需要在具备工具链的机器或 CI 上执行。

## 3. 第一阶段设计决策

### 3.1 目录和模块边界

第一阶段直接建立后续可扩展的目录，不继续把新文件堆在仓库根目录。现有三个文件用 `git mv` 迁移，便于保留历史：

```text
compiler/
├── CMakeLists.txt
├── include/toyc/
│   ├── support/
│   │   ├── diagnostic.hpp
│   │   └── ids.hpp
│   ├── frontend/
│   │   ├── token.hpp
│   │   ├── lexer.hpp
│   │   ├── ast.hpp
│   │   ├── ast_printer.hpp
│   │   ├── parser.hpp
│   │   └── semantic.hpp
│   ├── ir/
│   │   ├── tac.hpp
│   │   ├── cfg.hpp
│   │   ├── cfg_builder.hpp
│   │   └── verifier.hpp
│   └── backend/
│       ├── riscv32.hpp
│       └── asm_printer.hpp
├── src/
│   ├── main.cpp
│   ├── support/diagnostic.cpp
│   ├── frontend/
│   │   ├── lexer.cpp
│   │   ├── ast_printer.cpp
│   │   ├── parser.cpp
│   │   └── semantic.cpp
│   ├── ir/
│   │   ├── cfg_builder.cpp
│   │   └── verifier.cpp
│   └── backend/
│       └── asm_printer.cpp
├── tests/
│   ├── CMakeLists.txt
│   ├── test_support.hpp
│   ├── lexer_tests.cpp
│   ├── parser_tests.cpp
│   ├── semantic_tests.cpp
│   ├── cfg_tests.cpp
│   ├── codegen_tests.cpp
│   ├── cases/frontend/
│   └── cases/e2e/
└── scripts/
    ├── run_e2e.ps1
    └── run_e2e.sh
```

不要在第一阶段创建 `dag.*`、`ssa.*`、`regalloc.*` 等空壳。空模块不能带来可验证价值，还容易制造“已经实现”的错觉。

### 3.2 稳定 ID 和所有权

AST 是树，使用 `std::unique_ptr`；CFG 是图，实体间只引用整数 ID：

```cpp
namespace toyc {
using SymbolId = std::uint32_t;
using FuncId   = std::uint32_t;
using BlockId  = std::uint32_t;
using TempId   = std::uint32_t;
}
```

Parser 创建 AST 并复制所有名字。语义分析将 `NameExpr`、赋值左值和 `CallExpr` 分别解析为 `SymbolId` 或 `FuncId`。CFG 和后端不得再次按字符串做名称解析。

### 3.3 第一阶段 IR 口径

第一阶段使用显式基本块的非 SSA 三地址码。逻辑值在不同控制流分支上可以多次定义同一个 `TempId`；这是本阶段刻意允许的，第二阶段再通过支配边界和 Phi 转为 SSA。

建议最小指令集：

```cpp
enum class TacOp {
    LoadImm,
    ReadVar,
    WriteVar,
    Add, Sub, Mul, Div, Rem,
    CmpLT, CmpGT, CmpLE, CmpGE, CmpEQ, CmpNE,
    LogicalNot,
    Call
};

struct TacInst {
    TacOp op;
    std::optional<TempId> dst;
    std::vector<TempId> inputs;
    std::optional<std::int32_t> immediate;
    std::optional<SymbolId> symbol;
    std::optional<FuncId> callee;
    SourceLocation location;
};

struct Jump      { BlockId target; };
struct Branch    { TempId condition; BlockId trueTarget; BlockId falseTarget; };
struct Return    { std::optional<TempId> value; };
struct Unreachable {};
using Terminator = std::variant<Jump, Branch, Return, Unreachable>;
```

`ReadVar`/`WriteVar` 同时服务于局部和全局符号，后端根据符号表的 `isGlobal` 选择栈访问或 `la` + `lw/sw`。常量在语义阶段已求值，引用常量时直接生成 `LoadImm`，不生成存储。

### 3.4 后端口径

- 目标固定为 RV32IM，ABI 为 ILP32；使用 `mul/div/rem` 前必须在课程评测环境确认 M 扩展可用。
- 每个可写局部符号一个 4 字节栈槽，每个 `TempId` 一个 4 字节栈槽。
- `t0`、`t1` 载入操作数，`t2` 保存地址或中间结果；指令结束后立即写回目标栈槽。
- 不跨 TAC 指令保留 scratch 寄存器，因此 call clobber 不会破坏活跃值。
- 每个函数只有一个 epilogue label；所有 `return` 设置 `a0` 后跳到该 label。
- 函数调用前 8 个整数参数放 `a0`～`a7`，其余参数放调用者栈参数区。
- 函数体内 `sp` 保持不变，frame 大小始终是 16 的倍数。

该策略速度不快，但其状态模型简单，最适合建立正确性基线。

## 4. 执行步骤 0：建立可构建、可测试骨架

### 4.1 修改内容

1. 用 `git mv` 把现有头文件迁到 `include/toyc/frontend/`，把 `lexer.cpp` 迁到 `src/frontend/`。
2. 所有实现放进 `namespace toyc`。
3. 新建顶层 `CMakeLists.txt`，建立：
   - 静态库 `toyc_lib`：除 `main.cpp` 外的所有实现；
   - 可执行文件 `compiler`：只链接驱动；
   - `BUILD_TESTING` 开启时添加 `tests/`。
4. `.gitignore` 增加 `/build*/`、`Testing/`、`*.s`、测试生成的可执行文件；不要忽略 `tests/cases/**/*.tc` 和 golden 文件。
5. 测试使用 CTest 和一个轻量自研断言头，不引入需要联网下载的测试框架。

顶层 CMake 的目标状态如下。步骤 0 刚落地时，`toyc_lib` 只列出已经存在且可编译的源文件；后续每完成一个模块再把对应 `.cpp` 加入列表，不要为了凑齐下表而提交无法链接的空实现。

```cmake
cmake_minimum_required(VERSION 4.0)
project(toyc_compiler LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

include(CTest)

add_library(toyc_lib
    src/support/diagnostic.cpp
    src/frontend/lexer.cpp
    src/frontend/ast_printer.cpp
    src/frontend/parser.cpp
    src/frontend/semantic.cpp
    src/ir/cfg_builder.cpp
    src/ir/verifier.cpp
    src/backend/asm_printer.cpp)
target_include_directories(toyc_lib PUBLIC include)
target_compile_options(toyc_lib PRIVATE
    $<$<CXX_COMPILER_ID:GNU,Clang>:-Wall;-Wextra;-Wpedantic;-Wconversion>
    $<$<CXX_COMPILER_ID:MSVC>:/W4>)

add_executable(compiler src/main.cpp)
target_link_libraries(compiler PRIVATE toyc_lib)

if(BUILD_TESTING)
    add_subdirectory(tests)
endif()
```

### 4.2 本步骤完成定义

- `cmake -S . -B build -DBUILD_TESTING=ON` 成功；
- `cmake --build build --config Debug` 成功且没有新增 warning；
- `ctest --test-dir build -C Debug --output-on-failure` 能启动测试；
- `compiler` 可执行文件存在，哪怕驱动暂时只报告“pipeline not implemented”；
- 后续模块都链接 `toyc_lib`，测试不通过包含 `.cpp` 文件复用实现。

## 5. 执行步骤 1：修复并冻结 Lexer 契约

### 5.1 逐文件修改

`include/toyc/frontend/token.hpp`：

- 增加 `<cstddef>`；
- 保留现有 `TokenType`，不要合并 `Not`/`NotEqual` 或 `Assign`/`EqualEqual`；
- 增加 `tokenTypeName(TokenType)`，用于错误信息和 golden test；
- 可增加 `SourceRange {begin, end}`，但至少保留精确起始位置；
- `intValue` 保持 `int64_t`，不要在 Lexer 截断成 `int32_t`。

`include/toyc/frontend/lexer.hpp`：

- 接口保持 `explicit Lexer(std::string source)` 和 `Token next()`；
- 将 `skip_whitespace` 重命名为 `skipWhitespace` 以统一风格；
- Lexer 不增加字符回退和 Token lookahead；lookahead 属于 Parser。

`src/frontend/lexer.cpp`：

- 增加 `<utility>`；
- 修正：

```cpp
case '!':
    if (match('='))
        return makeToken(TokenType::NotEqual);
    return makeToken(TokenType::Not);
```

- 数字以 `0` 开头且后面紧跟数字时报告 `leading zero is not allowed in decimal literal`；
- `from_chars` 后同时检查 `ec` 和 `ptr == end`；
- 错误统一经诊断设施抛出；
- 保持单独 `&`、`|` 报错，保持 `-` 为独立 Token。

### 5.2 必测用例

`tests/lexer_tests.cpp` 至少覆盖：

| 类别 | 输入 | 核心期望 |
|---|---|---|
| 关键字 | `const int void if else while break continue return` | 依次产生 9 种关键字和 End |
| 标识符 | `_ a0 return_value` | 均为 Identifier |
| 算术符号 | `+ - * / %` | 对应 5 种 Token |
| 关系与赋值 | `= == ! != < <= > >=` | 类型全部精确匹配，尤其检查 4 个易混 Token |
| 逻辑符号 | `&& ||` | `LogicalAnd`、`LogicalOr` |
| 分隔符 | `( ) { } , ;` | 类型和 lexeme 正确 |
| 最长匹配 | `a!=b==c<=d` | 不拆成错误单字符 Token |
| 注释位置 | `/*a\nb*/\nint` | `int` 的行列正确 |
| 空注释 | `/**/x//tail` | 只产生 Identifier、End |
| 错误 | `&`、`|`、`@`、未闭合注释、`012`、超大整数 | 报错类别和起始位置正确 |
| 负数 | `a-1 -2147483648` | `-` 始终独立，数字保留宽值 |

### 5.3 本步骤完成定义

- Lexer 测试覆盖每个 `TokenType`；
- `!x` 和 `x != y` 的回归测试通过；
- 每个错误用例都验证行号和列号，不只验证“抛异常”；
- 连续调用 `next()` 在 End 后仍稳定返回 End；
- 在开启警告的 C++20 构建中无 warning。

## 6. 执行步骤 2：AST、Parser 与稳定 AST dump

### 6.1 AST 数据模型

AST 至少包含以下节点，所有节点携带 `SourceRange`：

```text
CompUnit {vector<TopLevel>}
TopLevel = Declaration | FunctionDecl
Declaration {isConst, name, init, resolvedSymbol?}
FunctionDecl {returnType, name, params, body, resolvedFunc?}
ParamDecl {name, resolvedSymbol?}

Stmt
├── BlockStmt {items}
├── EmptyStmt
├── ExprStmt {expr}
├── AssignStmt {name, resolvedSymbol?, value}
├── DeclStmt {declaration}
├── IfStmt {condition, thenStmt, elseStmt?}
├── WhileStmt {condition, body}
├── BreakStmt
├── ContinueStmt
└── ReturnStmt {value?}

Expr
├── IntegerExpr {int64_t value}
├── NameExpr {name, resolvedSymbol?}
├── UnaryExpr {Plus | Negate | LogicalNot, operand}
├── BinaryExpr {operator, lhs, rhs}
└── CallExpr {calleeName, resolvedFunc?, arguments}
```

名字字段用 `std::string`，不能保存 `Token::lexeme` 的 `string_view`。解析结果字段先用 `std::optional<SymbolId/FuncId>`，语义分析成功后必须全部有值。

### 6.2 Parser Token 缓冲

Parser 维护两个 Token：

```cpp
class Parser {
public:
    Parser(Lexer& lexer, DiagnosticEngine& diagnostics);
    std::unique_ptr<CompUnit> parseCompUnit();

private:
    Lexer& lexer_;
    DiagnosticEngine& diagnostics_;
    Token current_;
    Token next_;

    void advance();
    bool consume(TokenType type);
    Token expect(TokenType type, std::string_view message);
    // parseDecl / parseFunction / parseStmt / parse*Expr ...
};
```

两个需要 lookahead 的歧义点：

- 顶层 `int ID (` 是函数，`int ID =` 是变量；`const` 只能开始声明，`void` 只能开始函数。
- 语句开头 `Identifier Assign` 是赋值语句，其余 Identifier 开头内容按表达式语句解析。

### 6.3 严格按 PDF 实现的表达式层级

```text
parseExpr
  → parseLogicalOr
  → parseLogicalAnd
  → parseRelational
  → parseAdditive
  → parseMultiplicative
  → parseUnary
  → parsePrimary
```

注意：PDF 将 `< > <= >= == !=` 全放在同一个 `RelExpr` 层，本文第一阶段以 PDF 的产生式为准，并统一做左结合。不要自行拆成 C 的 equality/relational 两层，否则 AST golden 可能与课程文法不一致。

`parseLogicalOr` 到 `parseMultiplicative` 都用循环建立左结合树；`parseUnary` 递归处理 `!!x`、`-+x`；`parsePrimary` 在 Identifier 后检查 `(` 来区分名字引用和调用。

Parser 只构树，不做：名称查找、常量折叠、短路求值、返回路径判断或代码生成。

### 6.4 AST dump 格式

AST printer 使用稳定、与指针地址无关的 S-expression，示例：

```text
(function int main ()
  (block
    (decl var x (add 1 (mul 2 3)))
    (return x)))
```

必须规定：每级缩进 2 空格、节点名称固定、参数顺序固定、不打印地址、不打印 unordered_map 遍历结果。稳定 dump 是 Parser 的阶段契约，也是后续多人并行开发的接口。

### 6.5 Parser 验证清单

| 输入片段 | AST 关键形状 |
|---|---|
| `1 + 2 * 3` | `(add 1 (mul 2 3))` |
| `a - b - c` | `(sub (sub a b) c)` |
| `!a == b` | `(eq (not a) b)` |
| `a || b && c` | `(or a (and b c))` |
| `f(1, g(2, 3))` | 外层调用第 2 个参数是内层调用 |
| `if(a) if(b) x=1; else x=2;` | `else` 绑定最近的内层 `if` |
| `{ int x=1; { int x=2; } }` | 两个嵌套 Block 均保留 |
| `return;` / `return 1;` | value 分别为空/存在 |
| `int f()` / `int x = 1;` | 顶层函数与声明正确分流 |

### 6.6 本步骤完成定义

- PDF 的每条产生式至少有一个 Parser 测试；
- 五个优先级/结合性 golden test 精确通过；
- AST 中没有指向 Lexer 源码的 view 或裸指针所有权；
- Parser 遇到缺失 `)`、`;`、`}` 时能报告实际 Token 的位置和期望内容；
- `--dump-ast` 只向 `stderr` 打印，且相同输入多次输出完全一致。

## 7. 执行步骤 3：语义分析和常量求值

### 7.1 符号与作用域

建议数据结构：

```cpp
enum class ValueType { Int, Void };

struct Symbol {
    SymbolId id;
    std::string name;
    bool isConst;
    bool isGlobal;
    std::optional<std::int32_t> constValue;
};

struct FunctionSymbol {
    FuncId id;
    std::string name;
    ValueType returnType;
    std::vector<ValueType> parameterTypes;
};

struct Scope {
    Scope* parent;
    std::unordered_map<std::string, SymbolId> values;
};
```

所有 `Symbol`、`FunctionSymbol` 由 `SemanticResult` 中的 `vector` 按 ID 稳定持有。作用域只保存 ID，不保存可能被 vector 扩容破坏的指针。

作用域执行顺序：

1. 创建全局作用域。
2. 顺序处理顶层项；函数签名在分析自身函数体前插入，以支持递归，但未出现的其他函数不可调用。
3. 函数体创建函数作用域，先插入参数；每个 Block 再创建子作用域。
4. 声明初始化式先在当前可见环境中分析，再把新名字插入当前作用域。这样 `int x = x;` 会引用外层 `x`，不会错误地引用自己。
5. 当前作用域禁止重名，内层允许遮蔽外层。

为保持 ToyC 可直接当作 C 编译，建议全局对象名和函数名也禁止冲突。

### 7.2 必须执行的语义规则

- 存在且只存在一个 `int main()`；
- 函数只在全局定义，函数名唯一；
- 函数调用发生在定义出现之后，但允许直接递归；
- 实参数量与形参数量一致；
- `void` 表达式只能作为表达式语句，不能参与运算、条件、变量初始化、赋值或有值返回；
- `return` 与当前函数返回类型一致；
- `break`/`continue` 的 `loopDepth > 0`；
- 变量/常量必须先声明后使用；
- 当前作用域内无重复声明；
- 常量不可赋值；
- 常量初始化必须能在编译期求值；
- 全局变量初始化也必须静态可求值，以满足 ToyC 是 C 子集的约束；
- 所有运行时值按 32 位有符号整数解释，比较和逻辑结果规范化为 0/1；
- `int` 函数不能有可达的无值退出路径。

最后一条在 CFG 构建后做最终判定：如果 `int` 函数存在从 entry 可达的隐式函数尾出口，则报告错误。语义阶段先完成 return 类型检查，CFG 阶段完成路径完整性检查，避免仅靠语法形状误判复杂分支。

### 7.3 常量求值

`ConstEvaluator` 只接受：数字、已经解析且有值的常量、一元运算、二元算术/关系/逻辑运算。拒绝变量读取和函数调用。

实现要求：

- `&&`/`||` 在常量求值中也必须短路，例如 `0 && (1 / 0)` 合法求得 0；
- 除零和余零给出源位置；
- 不在宿主 C++ 中执行可能溢出的有符号运算；需要 32 位回绕时用 `uint32_t` 做位运算再转换；
- 单独处理 `-2147483648`，Lexer/AST 先保留 `2147483648` 的宽值，再由一元负号得到 `INT32_MIN`；
- 其他超出允许范围的字面量应报错；
- 源程序承诺无未定义行为，但分析器仍不应依赖宿主机 64 位语义替代 RV32 语义。

### 7.4 语义测试

正例至少包含：递归、自身调用、内层遮蔽、常量链、短路常量、void 调用表达式语句、8 个以上参数、所有分支 return。

反例至少包含：缺少 main、main 类型/参数错误、重复声明、使用未声明名字、先调用后定义、实参数量错误、给 const 赋值、常量依赖变量、void 值参与运算、循环外 break/continue、int return 无值、void return 有值、int 函数存在可达落空路径。

### 7.5 本步骤完成定义

- 每条语义规则都有至少一个正例或反例测试；
- 合法 AST 中所有名称和调用均写入稳定 ID；
- 诊断包含类别、源行列和可理解的信息；
- 常量引用进入 CFG 时已变成立即数；
- 语义测试可独立运行，不依赖 RISC-V 工具链。

## 8. 执行步骤 4：生成非 SSA TAC/CFG

### 8.1 CFG 不变量

```cpp
struct BasicBlock {
    BlockId id;
    std::string label;
    std::vector<TacInst> instructions;
    std::optional<Terminator> terminator;
    std::vector<BlockId> predecessors;
    std::vector<BlockId> successors;
};
```

Verifier 必须检查：

- 每个可达块恰好有一个 terminator；
- terminator 后没有普通指令；
- predecessor/successor 双向一致且不重复；
- Jump/Branch 目标存在于同一函数；
- 每个读取的 `TempId` 在到达该路径前已经定义；
- Return 是否与函数返回类型匹配；
- `int` 函数没有可达的隐式落空出口。

CFGBuilder 用 `std::optional<BlockId> currentBlock_` 表示当前位置。发出 `return`、`break`、`continue` 后将其置空，禁止继续向已经终结的块追加指令。

### 8.2 控制流降低

`if`：创建 `then`、可选 `else` 和 `merge`。只有未终结分支才连到 merge；若 merge 没有前驱，将其删除或标记不可达。

`while`：固定建立 `condition`、`body`、`exit`：

```text
current → condition ─false→ exit
                │true
                ↓
              body ───────→ condition
```

循环目标栈保存 `{continueTarget = condition, breakTarget = exit}`。嵌套循环只访问栈顶目标。

### 8.3 短路实现

条件上下文统一走：

```cpp
void emitCondition(const Expr& expr, BlockId trueBlock, BlockId falseBlock);
```

- `A && B`：A 真才进入 B；A 假直接进入 falseBlock。
- `A || B`：A 真直接进入 trueBlock；A 假才进入 B。
- `!A`：交换 trueBlock 和 falseBlock。
- 其他表达式：计算一个 Temp，发出 Branch。

逻辑表达式用于值时，建立 true/false/merge 三个块：

```text
trueBlock:   result = 1; jump merge
falseBlock:  result = 0; jump merge
merge:       后续使用 result
```

第一阶段允许 `result` 在互斥分支被定义两次。不得为了简化而先计算 A 和 B 后发普通 `and/or`，因为 B 可能调用有副作用的函数。

### 8.4 CFG dump 与测试

dump 格式至少打印函数、块、前驱、指令和 terminator，例如：

```text
function @main:
bb0.entry: ; preds = []
  %0 = imm 1
  branch %0, bb1.then, bb2.else
bb1.then: ; preds = [bb0]
  %1 = imm 3
  return %1
```

专项用例：嵌套 if、无 else、双分支 return、while 中 break/continue、嵌套循环、条件中函数调用、值上下文短路、return 后不可达语句。

### 8.5 本步骤完成定义

- 每个 CFG 测试在 dump 前后都通过 verifier；
- `0 && f()` 和 `1 || f()` 的 CFG 中，`f()` 只位于条件允许到达的块；
- 嵌套循环的 break/continue 跳转目标正确；
- CFG 中没有隐式 fall-through，所有边均由 terminator 明确表达；
- `--dump-cfg` 仅写 `stderr`。

## 9. 执行步骤 5：栈式 RISC-V32 后端

### 9.1 Frame 规划

在输出函数前先扫描整个函数，计算：

```text
outgoingArgBytes = 4 * max(0, 所有 call 的最大实参数量 - 8)
localBytes       = 4 * 可写局部变量数
tempBytes        = 4 * TempId 数量
saveBytes        = 4  // 第一版统一保存 ra，便于递归和 call
frameSize        = alignUp(outgoingArgBytes + localBytes + tempBytes + saveBytes, 16)
```

建议从低 offset 到高 offset 依次放 outgoing args、局部变量、临时值、`ra`。整个函数体不改变 `sp`，所有 offset 稳定。

被调用函数的第 9 个及以后参数位于“进入函数时的旧 sp”上。当前函数执行 `sp -= frameSize` 后，应从 `frameSize + 4 * (index - 8)` 读取这些参数，再写入自己的参数符号栈槽。

### 9.2 指令选择

| TAC | 第一阶段汇编策略 |
|---|---|
| `LoadImm dst, n` | `li t0, n`，再 `sw t0, dstSlot(sp)` |
| `ReadVar dst, local` | `lw t0, localSlot(sp)`，写 `dstSlot` |
| `WriteVar local, src` | 从 `srcSlot` load，再写 `localSlot` |
| 全局读 | `la t1, symbol; lw t0, 0(t1)` |
| 全局写 | `la t1, symbol; sw t0, 0(t1)` |
| `Add/Sub/Mul/Div/Rem` | load 到 `t0/t1`，执行真实指令，写回 dst |
| `<`/`>` | `slt`，必要时交换操作数 |
| `==`/`!=` | `xor` 后用 `seqz`/`snez` 伪指令 |
| `<=`/`>=` | `slt` 后 `xori result, result, 1` |
| `LogicalNot` | `seqz`，结果保证 0/1 |
| `Branch` | load condition；`bnez` 到 true；`j` 到 false |
| `Jump` | `j label` |
| `Return value` | load 到 `a0`，跳 epilogue |

如果栈 offset 或 frame 调整超出 12 位立即数，必须用 `li` + `add` 物化地址，不能直接输出非法 `lw t0, 5000(sp)` 或 `addi sp, sp, -5000`。地址 helper 应明确传入不会覆盖数据的 scratch 寄存器。

### 9.3 函数调用

1. 先按源代码顺序求值所有实参，并把结果保存到各自 Temp 栈槽；这保证调用副作用顺序稳定。
2. 从已保存的 Temp 载入 `a0`～`a7`。
3. 第 9 个以后的实参写到当前 frame 的 outgoing argument 区 `0(sp)`、`4(sp)`……。
4. 发出 `call functionName`。
5. 非 void 返回值立即把 `a0` 写入目标 Temp 栈槽。

所有 live 值都已经在栈里，因此无需额外保存 caller-saved 寄存器。递归调用与普通调用走完全相同路径。

### 9.4 全局数据与符号

- 全局常量直接内联，无需分配对象；
- 初始化为 0 的全局变量可放 `.bss`，其他静态值放 `.data`；
- 对外函数和全局变量输出 `.globl`、`.type`、`.size`；
- 内部基本块 label 使用 `.L<function>_<blockId>`，避免跨函数重名；
- 输出 `.text` 后再输出函数，数据段顺序固定以保证 golden 稳定。

### 9.5 本步骤完成定义

- `return 0` 生成的汇编可组装、链接，运行退出码为 0；
- 算术、比较、if/while、全局变量、函数调用、递归均有运行用例；
- 9～12 个参数的专项用例通过；
- 非叶函数保存/恢复 `ra`，调用边界 `sp` 保持 16 字节对齐；
- 大量临时值测试不会输出超范围的 12 位 load/store offset；
- `AsmPrinter` 只格式化既定 IR/布局，不在打印过程中改变 CFG 语义。

## 10. 执行步骤 6：顶层驱动与命令行契约

`src/main.cpp` 只负责阶段编排：

```cpp
int main(int argc, char** argv) {
    try {
        const CompilerOptions options = parseOptions(argc, argv);
        const std::string source{
            std::istreambuf_iterator<char>(std::cin),
            std::istreambuf_iterator<char>()};

        toyc::DiagnosticEngine diagnostics;
        toyc::Lexer lexer(source);
        toyc::Parser parser(lexer, diagnostics);
        auto ast = parser.parseCompUnit();

        toyc::SemanticAnalyzer sema(diagnostics);
        auto semantic = sema.analyze(*ast);
        auto module = toyc::CFGBuilder(semantic).build(*ast);
        toyc::verify(module, semantic);

        if (options.dumpAst)  toyc::printAst(std::cerr, *ast);
        if (options.dumpCfg)  toyc::printCfg(std::cerr, module);
        toyc::AsmPrinter(std::cout).print(module, semantic);
        return 0;
    } catch (const toyc::CompileError& error) {
        std::cerr << error.what() << '\n';
        return 1;
    }
}
```

参数约定：

| 参数 | 第一阶段行为 |
|---|---|
| 无参数 | 正常编译，无优化 |
| `-opt` | 接受并正常编译；暂时与无参数相同 |
| `--dump-tokens` | Token dump 到 `stderr` |
| `--dump-ast` | AST dump 到 `stderr` |
| `--dump-cfg` | CFG dump 到 `stderr` |
| `--verify-each` | 显式启用每阶段 verifier；Debug 构建默认启用 |
| 未知参数 | `stderr` 报错，退出码非 0，stdout 为空 |

不得把算法实现写进 `main.cpp`。在产生任何 stdout 前完成词法、语法、语义和 CFG 验证，以保证失败编译不会输出半份汇编。

本步骤完成定义：两种正式调用 `compiler < input.tc > output.s` 与 `compiler -opt < input.tc > output.s` 都能工作；对同一输入，两者第一阶段可产生完全相同的汇编。

## 11. 执行步骤 7：端到端验收

### 11.1 最小端到端用例集

`tests/cases/e2e/` 至少包含：

| 文件建议名 | 覆盖内容 | 期望退出码 |
|---|---|---:|
| `return_zero.tc` | 最小 main | 0 |
| `precedence.tc` | 算术优先级、括号、负数 | 手算固定值 |
| `comparisons.tc` | 6 个关系运算和逻辑非 | 手算固定值 |
| `short_circuit_and.tc` | 右侧函数修改全局，左侧为假 | 证明右侧未执行 |
| `short_circuit_or.tc` | 右侧函数修改全局，左侧为真 | 证明右侧未执行 |
| `nested_if.tc` | dangling else、分支 return | 手算固定值 |
| `while_break_continue.tc` | 循环、break、continue | 手算固定值 |
| `scope_shadow.tc` | 三级作用域遮蔽 | 手算固定值 |
| `global_state.tc` | 跨函数读写全局变量 | 手算固定值 |
| `void_call.tc` | void 函数副作用 | 手算固定值 |
| `call_chain.tc` | 多函数和参数求值 | 手算固定值 |
| `recursive_factorial.tc` | 递归与 ra 保存 | 120 |
| `many_args.tc` | 至少 12 个 int 参数 | 参数和的低 8 位 |
| `int_min.tc` | `-2147483648` 的解析和运算 | 选取可观察低 8 位 |
| `large_frame.tc` | 超过 12 位 offset 的 frame | 手算固定值 |

每个用例必须同时：

1. 用参考 C 编译器运行；
2. 用本项目编译器生成 RV32 汇编；
3. 用交叉工具链组装、链接并在模拟器运行；
4. 比较两个退出码的低 8 位。

### 11.2 构建和前端测试命令

跨平台的标准命令：

```text
cmake -S . -B build -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Debug
cmake --build build --config Debug
ctest --test-dir build -C Debug --output-on-failure
```

Release 构建再执行一次：

```text
cmake -S . -B build-release -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Release
cmake --build build-release --config Release
ctest --test-dir build-release -C Release --output-on-failure
```

### 11.3 端到端命令模板

具体交叉编译器名称由评测环境确认，脚本通过参数或环境变量接收，不把本机绝对路径写入仓库。Linux/QEMU 示例：

```text
./build/compiler < tests/cases/e2e/recursive_factorial.tc > out.s
riscv32-linux-gnu-gcc -march=rv32im -mabi=ilp32 out.s -o out
qemu-riscv32 ./out
```

若实际只有带 RV32 multilib 的 `riscv64-linux-gnu-gcc`，使用同样的 `-march=rv32im -mabi=ilp32`。若运行环境要求静态链接，在脚本配置中加入 `-static`，不要在编译器中硬编码链接策略。

PowerShell 脚本必须在每个外部命令后保存并检查 `$LASTEXITCODE`，不能让后续命令覆盖失败状态。

## 12. 实施后“修改了什么”清单

完成第一阶段后，变更摘要应与下表一致。若实际实现偏离，需要在 PR/提交说明中解释原因。

| 类别 | 变更 |
|---|---|
| 现有 Lexer | 修复 `!`/`!=`；补显式 include；严格数字规则；统一诊断；迁入命名空间和标准目录 |
| Token | 增加必要 include、Token 名称打印、可选 SourceRange；仍保留宽整数值和 string_view |
| 构建 | 新增 CMake 4.0/C++20 配置、`toyc_lib`、`compiler` 和 CTest |
| 驱动 | 新增 stdin/stdout 接口、`-opt` 和 dump/verify 调试参数 |
| 前端 | 新增完整 AST、递归下降 Parser、稳定 AST printer |
| 语义 | 新增作用域、符号/函数 ID、名称解析、类型检查、常量求值和语义诊断 |
| IR | 新增非 SSA TAC、显式 CFG、短路降低、CFG dump/verifier |
| 后端 | 新增栈槽 frame 规划、RV32IM 指令选择、调用约定、全局数据和汇编输出 |
| 测试 | 新增 Lexer/Parser/Semantic/CFG/Codegen 单测和端到端差分脚本 |
| 仓库卫生 | `.gitignore` 增加构建与测试产物，不提交本机路径和生成汇编 |

## 13. 第一阶段总完成定义（Definition of Done）

只有下面所有门禁都满足，第一阶段才算完成。

### 13.1 构建门禁

- Debug 和 Release 均能从干净目录配置并构建；
- 使用 C++20，不依赖 LLVM/Clang 编译框架；
- 编译警告为 0；
- 不需要联网下载测试依赖；
- 仓库中没有提交 `build/`、临时汇编、可执行文件或本机绝对路径。

### 13.2 前端门禁

- 每种 Token、每条文法产生式均有测试；
- 五个指定表达式优先级 golden 全部通过；
- 所有语义约束均有正/反用例；
- 所有合法名称引用均解析为稳定 ID；
- Lexer/AST/CFG dump 可重复且只输出到 stderr。

### 13.3 IR/CFG 门禁

- 所有可达块有且仅有一个 terminator；
- 前驱与后继双向一致；
- `&&`/`||` 的右操作数只在正确路径执行；
- break/continue/return 后不会追加指令；
- 每个 CFG 单测和端到端输入在代码生成前都通过 verifier。

### 13.4 后端与 ABI 门禁

- 输出能被目标汇编器接受；
- 函数调用、递归、0～12 个参数均正确；
- 非叶函数 `ra` 正确保存恢复；
- 每个调用点 `sp` 16 字节对齐；
- 全局变量读写正确，常量无需运行时存储；
- 大 frame 不产生越界立即数；
- main 的 `a0` 低 8 位与参考 C 程序退出码一致。

### 13.5 测试门禁

- `ctest --output-on-failure` 在 Debug、Release 均为 100% 通过；
- 第 11.1 节的端到端用例全部通过；
- 无参数与 `-opt` 两条接口都通过同一套 correctness suite；
- 至少做一次从全新 clone/干净构建目录开始的验收；
- 测试日志记录宿主编译器、CMake、交叉 GCC、`-march/-mabi` 和模拟器版本。

## 14. 建议提交顺序

每个提交都应能构建，避免一次提交整个编译器：

1. `build: establish CMake and test skeleton`
2. `fix(lexer): correct bang tokens and add exhaustive tests`
3. `feat(parser): add AST, recursive descent parser and dump`
4. `feat(sema): add scopes, name resolution and const evaluation`
5. `feat(ir): lower AST to verified non-SSA CFG`
6. `feat(backend): emit stack-based RV32IM assembly`
7. `feat(driver): wire stdin/stdout, -opt and debug dumps`
8. `test(e2e): add ABI, recursion and differential cases`

每个提交执行与其层级相符的测试；从第 6 个提交起，每个提交都必须运行完整端到端用例。

## 15. 可直接执行的开发检查表

- [ ] 安装/接入 C++20 编译器、RV32 工具链和模拟器
- [ ] 迁移现有 Lexer/Token 文件并建立 CMake/CTest
- [ ] 修复 `!`/`!=` 和 include 问题
- [ ] 完成全 Token 和错误位置测试
- [ ] 定义 AST 所有权、SourceRange 和稳定 dump
- [ ] 按 PDF 文法完成递归下降 Parser
- [ ] 完成作用域、符号 ID、函数 ID 和所有语义检查
- [ ] 完成短路常量求值和 32 位整数规则
- [ ] 定义非 SSA TAC 与 CFG 不变量
- [ ] 完成 if/while/break/continue/return/短路降低
- [ ] 实现 CFG verifier 和稳定 dump
- [ ] 完成 frame 预扫描、栈槽分配与大 offset helper
- [ ] 完成算术、比较、分支、全局和调用的 RV32IM 输出
- [ ] 完成 `main.cpp`、`-opt`、dump 参数和错误出口
- [ ] Debug/Release 单测 100% 通过
- [ ] 端到端差分用例 100% 通过
- [ ] 从干净 clone 复验构建、编译、链接和运行

第一阶段结束时，应当能用一个命令把任意合法 ToyC 输入变成可运行的 RISC-V32 汇编，并能用分层测试明确回答失败发生在 Lexer、Parser、Semantic、CFG 还是后端。达到这个状态后，再开始第二阶段的 SSA、完整 MIR 和寄存器分配改造。
